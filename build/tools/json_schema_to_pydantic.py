TOOL = {
    "slug": "json-schema-to-pydantic",
    "category": "developer",
    "icon": "🔄",
    "tags": ["json-schema", "pydantic", "python", "convert", "generator", "model", "validation", "llm", "structured-output"],
    "i18n": {
        "en": {
            "name": "JSON Schema → Pydantic",
            "tagline": "Convert JSON Schema to Pydantic v2 model code, in your browser. Paste schema, get a ready-to-paste Python class. Useful for LLM structured-output workflows.",
            "description": "Free JSON Schema → Pydantic v2 code generator. Paste a JSON Schema object, get a Pydantic BaseModel class with type hints, Field() defaults, validators, and Optional/Union resolved. Useful when you've designed a structured-output schema for an LLM (OpenAI / Claude / etc.) and need the matching Pydantic class. Browser-only, no upload.",
        },
        "de": {
            "name": "JSON-Schema → Pydantic",
            "tagline": "Wandle JSON Schema in Pydantic-v2-Modellcode um, direkt im Browser. Schema einfügen, fertige Python-Klasse erhalten. Nützlich für LLM-Structured-Output.",
            "description": "Kostenloser JSON-Schema → Pydantic-v2-Codegenerator. Füge ein JSON-Schema-Objekt ein, erhalte eine Pydantic-BaseModel-Klasse mit Type-Hints, Field()-Defaults, Validatoren und aufgelösten Optional/Union. Nützlich, wenn du ein Structured-Output-Schema für ein LLM (OpenAI / Claude / etc.) entworfen hast und die passende Pydantic-Klasse brauchst. Reine Browser-Verarbeitung, kein Upload.",
        },
        "es": {
            "name": "JSON Schema → Pydantic",
            "tagline": "Convierte JSON Schema a código de modelos Pydantic v2 en tu navegador. Pega el schema, obtén una clase Python lista para pegar. Útil para flujos LLM con structured output.",
            "description": "Generador gratuito de código JSON Schema → Pydantic v2. Pega un objeto JSON Schema y obtén una clase Pydantic BaseModel con type hints, valores por defecto Field(), validadores y Optional/Union resueltos. Útil cuando has diseñado un schema de structured output para un LLM (OpenAI / Claude / etc.) y necesitas la clase Pydantic correspondiente. Solo en el navegador, sin subida.",
        },
        "fr": {
            "name": "JSON Schema → Pydantic",
            "tagline": "Convertissez JSON Schema en code de modèle Pydantic v2, dans votre navigateur. Collez le schema, obtenez une classe Python prête à coller. Utile pour les workflows LLM de structured output.",
            "description": "Générateur gratuit JSON Schema → code Pydantic v2. Collez un objet JSON Schema et obtenez une classe Pydantic BaseModel avec type hints, valeurs par défaut Field(), validateurs et Optional/Union résolus. Utile quand vous avez conçu un schema de structured output pour un LLM (OpenAI / Claude / etc.) et qu'il vous faut la classe Pydantic correspondante. Navigateur uniquement, pas d'upload.",
        },
        "it": {
            "name": "JSON Schema → Pydantic",
            "tagline": "Converti JSON Schema in codice modello Pydantic v2, nel tuo browser. Incolla lo schema, ottieni una classe Python pronta da incollare. Utile per flussi LLM con structured output.",
            "description": "Generatore gratuito JSON Schema → codice Pydantic v2. Incolla un oggetto JSON Schema e ottieni una classe Pydantic BaseModel con type hint, default Field(), validatori e Optional/Union risolti. Utile quando hai progettato uno schema di structured output per un LLM (OpenAI / Claude / etc.) e ti serve la classe Pydantic corrispondente. Solo browser, niente upload.",
        },
        "pt": {
            "name": "JSON Schema → Pydantic",
            "tagline": "Converte JSON Schema em código de modelo Pydantic v2 no seu navegador. Cole o schema, obtenha uma classe Python pronta. Útil pra workflows LLM de structured output.",
            "description": "Gerador grátis de código JSON Schema → Pydantic v2. Cole um objeto JSON Schema e obtenha uma classe Pydantic BaseModel com type hints, defaults Field(), validadores e Optional/Union resolvidos. Útil quando você desenhou um schema de structured output pra um LLM (OpenAI / Claude / etc.) e precisa da classe Pydantic correspondente. Só no navegador, sem upload.",
        },
        "pl": {
            "name": "JSON Schema → Pydantic",
            "tagline": "Konwertuj JSON Schema na kod modelu Pydantic v2 w przeglądarce. Wklej schema, dostań klasę Pythona gotową do wklejenia. Przydatne dla LLM-owych workflowów ze structured output.",
            "description": "Darmowy generator kodu JSON Schema → Pydantic v2. Wklej obiekt JSON Schema i dostań klasę Pydantic BaseModel z type hintami, defaultami Field(), walidatorami i rozwiniętymi Optional/Union. Przydatne, kiedy zaprojektowałeś schema structured-outputu dla LLM-a (OpenAI / Claude / etc.) i potrzebujesz dopasowanej klasy Pydantic. Tylko w przeglądarce, bez uploadu.",
        },
        "ja": {
            "name": "JSON Schema → Pydantic",
            "tagline": "ブラウザ内で JSON Schema を Pydantic v2 のモデルコードに変換。スキーマを貼ると、貼り付けられる Python クラスがそのまま得られます。LLM の構造化出力ワークフローに便利。",
            "description": "JSON Schema → Pydantic v2 コードジェネレータ（無料）。JSON Schema を貼り付けると、型ヒント・Field() のデフォルト・バリデータ付きの Pydantic BaseModel クラスを生成します。Optional / Union も解決済み。LLM（OpenAI、Claude など）の構造化出力スキーマを設計したあと、対応する Pydantic クラスが必要なときに便利。ブラウザ内処理のみ、アップロードなし。",
        },
        "nl": {
            "name": "JSON Schema → Pydantic",
            "tagline": "Converteer JSON Schema naar Pydantic v2 modelcode, in je browser. Plak schema, krijg een ready-to-paste Python class. Handig voor LLM structured-output workflows.",
            "description": "Gratis JSON Schema → Pydantic v2 code-generator. Plak een JSON Schema-object, krijg een Pydantic BaseModel class met type hints, Field()-defaults, validators en Optional/Union opgelost. Handig als je een structured-output schema hebt ontworpen voor een LLM (OpenAI / Claude / etc.) en de matching Pydantic class nodig hebt. Alleen in de browser, geen upload.",
        },
        "tr": {
            "name": "JSON Schema → Pydantic",
            "tagline": "JSON Schema'yı tarayıcında Pydantic v2 model koduna dönüştür. Şemayı yapıştır, yapıştırılmaya hazır Python sınıfı al. LLM yapılandırılmış çıktı iş akışları için yararlıdır.",
            "description": "Ücretsiz JSON Schema → Pydantic v2 kod üreteci. Bir JSON Schema nesnesi yapıştır, type hint'leri, Field() varsayılanları, doğrulayıcıları ve çözülmüş Optional/Union ile Pydantic BaseModel sınıfı al. Bir LLM (OpenAI / Claude / vb.) için yapılandırılmış çıktı şeması tasarladığında ve eşleşen Pydantic sınıfına ihtiyaç duyduğunda yararlıdır. Sadece tarayıcı, upload yok.",
        },
        "id": {
            "name": "JSON Schema → Pydantic",
            "tagline": "Konversi JSON Schema ke kode model Pydantic v2 di browser-mu. Tempel schema, dapatkan class Python siap tempel. Berguna untuk workflow LLM structured output.",
            "description": "Generator kode JSON Schema → Pydantic v2 gratis. Tempel objek JSON Schema, dapatkan class Pydantic BaseModel dengan type hint, default Field(), validator, dan Optional/Union yang sudah diselesaikan. Berguna saat kamu mendesain schema structured output untuk LLM (OpenAI / Claude / dll.) dan butuh class Pydantic yang cocok. Hanya di browser, tanpa upload.",
        },
        "vi": {
            "name": "JSON Schema → Pydantic",
            "tagline": "Chuyển đổi JSON Schema sang code model Pydantic v2, trong trình duyệt của bạn. Dán schema, nhận về một class Python sẵn sàng để dán. Hữu ích cho workflow LLM với structured output.",
            "description": "Trình tạo code JSON Schema → Pydantic v2 miễn phí. Dán một đối tượng JSON Schema và nhận về một class Pydantic BaseModel với type hint, default Field(), validator, và Optional/Union đã được xử lý. Hữu ích khi bạn đã thiết kế schema structured output cho một LLM (OpenAI / Claude / v.v.) và cần class Pydantic tương ứng. Chỉ trong trình duyệt, không upload.",
        },
        "hi": {
            "name": "JSON Schema → Pydantic",
            "tagline": "अपने browser में JSON Schema को Pydantic v2 model code में convert करें। Schema paste करें, ready-to-paste Python class पाएं। LLM structured-output workflows के लिए उपयोगी।",
            "description": "मुफ़्त JSON Schema → Pydantic v2 code generator। एक JSON Schema object paste करें और type hints, Field() defaults, validators, और Optional/Union resolved के साथ एक Pydantic BaseModel class पाएं। उपयोगी जब आपने एक LLM (OpenAI / Claude / etc.) के लिए structured-output schema design किया हो और matching Pydantic class चाहिए। केवल browser में, कोई upload नहीं।",
        },
        "sk": {
            "name": "JSON Schema → Pydantic",
            "tagline": "Konvertuj JSON Schema na kód modelu Pydantic v2 priamo v prehliadači. Vlož schému, dostaneš pripravenú Python triedu. Užitočné pre LLM workflow so štruktúrovaným výstupom.",
            "description": "Bezplatný generátor kódu JSON Schema → Pydantic v2. Vlož objekt JSON Schema, dostaneš triedu Pydantic BaseModel s type hintami, defaultmi Field(), validátormi a vyriešeným Optional/Union. Užitočné, keď si navrhol schému štruktúrovaného výstupu pre LLM (OpenAI / Claude / atď.) a potrebuješ k nej pasujúcu Pydantic triedu. Iba v prehliadači, žiadny upload.",
        },
        "cs": {
            "name": "JSON Schema → Pydantic",
            "tagline": "Převeď JSON Schema na kód modelu Pydantic v2 přímo v prohlížeči. Vlož schéma, dostaneš připravenou Python třídu. Užitečné pro LLM workflow se strukturovaným výstupem.",
            "description": "Bezplatný generátor kódu JSON Schema → Pydantic v2. Vlož objekt JSON Schema, dostaneš třídu Pydantic BaseModel s type hinty, defaulty Field(), validátory a vyřešeným Optional/Union. Užitečné, když jsi navrhl schéma strukturovaného výstupu pro LLM (OpenAI / Claude / atd.) a potřebuješ k němu pasující Pydantic třídu. Pouze v prohlížeči, žádný upload.",
        },
    },
    "body": """
<div class="tool-card">
  <label for="js-input">JSON Schema</label>
  <textarea id="js-input" oninput="jsRun()" spellcheck="false" placeholder='{ "type": "object", "properties": { "name": {"type": "string"} } }'></textarea>
  <div class="row-2col" style="margin-top:0.7rem">
    <div>
      <label>Pydantic version</label>
      <select id="js-version" onchange="jsRun()">
        <option value="v2">Pydantic v2 (default)</option>
        <option value="v1">Pydantic v1</option>
      </select>
    </div>
    <div>
      <label>Sample schema</label>
      <select id="js-sample" onchange="jsLoadSample()">
        <option value="">— Load an example —</option>
        <option value="user">User profile</option>
        <option value="address">Address</option>
        <option value="nested">Nested API response</option>
        <option value="enums">Status with enum</option>
      </select>
    </div>
  </div>
  <div>
    <label for="js-name" style="margin-top:0.7rem">Root class name</label>
    <input type="text" id="js-name" value="Model" oninput="jsRun()" style="font-family:ui-monospace,monospace">
  </div>
</div>
<div class="tool-card">
  <label>Generated Pydantic code</label>
  <div class="output-row">
    <pre class="output" id="js-output">{LBL_NO_INPUT}</pre>
    <button class="copy-btn secondary" onclick="jsCopy(this)">{LBL_COPY}</button>
  </div>
  <div class="meta" id="js-meta"></div>
</div>
""",
    "script": """
<style>
#js-output{white-space:pre;overflow:auto;font-family:ui-monospace,monospace;font-size:0.88rem;tab-size:4}
#js-output.error{color:#f85149}
</style>
<script>
const JS_SAMPLES = {
  user: {
    "title": "User",
    "type": "object",
    "properties": {
      "id":   {"type": "integer", "description": "Unique user id"},
      "name": {"type": "string"},
      "email":{"type": "string", "format": "email"},
      "age":  {"type": "integer", "minimum": 0},
      "tags": {"type": "array", "items": {"type": "string"}, "default": []}
    },
    "required": ["id", "name", "email"]
  },
  address: {
    "title": "Address",
    "type": "object",
    "properties": {
      "street":      {"type": "string"},
      "city":        {"type": "string"},
      "postal_code": {"type": "string", "description": "ZIP or postal code"},
      "country":     {"type": "string", "enum": ["US", "GB", "DE", "FR"]}
    },
    "required": ["street", "city", "country"]
  },
  nested: {
    "title": "APIResponse",
    "type": "object",
    "properties": {
      "ok":      {"type": "boolean"},
      "data":    {
        "type": "object",
        "properties": {
          "users": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id":   {"type": "integer"},
                "name": {"type": "string"}
              },
              "required": ["id", "name"]
            }
          }
        }
      },
      "created_at": {"type": "string", "format": "date-time"}
    },
    "required": ["ok"]
  },
  enums: {
    "title": "Job",
    "type": "object",
    "properties": {
      "id":     {"type": "string"},
      "status": {"type": "string", "enum": ["pending", "running", "done", "failed"]},
      "result": {"oneOf": [{"type": "string"}, {"type": "null"}]}
    },
    "required": ["id", "status"]
  }
};
function jsLoadSample(){
  const sel = document.getElementById('js-sample').value;
  if(!sel) return;
  const sample = JS_SAMPLES[sel];
  document.getElementById('js-input').value = JSON.stringify(sample, null, 2);
  document.getElementById('js-name').value = sample.title || 'Model';
  jsRun();
}
function jsToPascal(s){
  if(!s) return 'Model';
  return s.replace(/[^a-zA-Z0-9]+/g, ' ').trim().split(/\\s+/).map(w => w.charAt(0).toUpperCase() + w.slice(1)).join('') || 'Model';
}
function jsEscString(s){
  return '"' + String(s).replace(/\\\\/g, '\\\\\\\\').replace(/"/g, '\\\\"').replace(/\\n/g, '\\\\n') + '"';
}
function jsPyRepr(v){
  if(v === null) return 'None';
  if(v === true) return 'True';
  if(v === false) return 'False';
  if(typeof v === 'number') return String(v);
  if(typeof v === 'string') return jsEscString(v);
  if(Array.isArray(v)) return '[' + v.map(jsPyRepr).join(', ') + ']';
  if(typeof v === 'object'){
    const entries = Object.entries(v).map(([k,vv]) => jsEscString(k) + ': ' + jsPyRepr(vv));
    return '{' + entries.join(', ') + '}';
  }
  return 'None';
}
function jsGenerate(schema, rootName, v2){
  const imports = { typing: new Set(), datetime: new Set(), uuid: false, pydantic: new Set(['BaseModel']) };
  const classes = [];
  const seenNames = new Set();
  function uniqueName(base){
    let n = base;
    let i = 2;
    while(seenNames.has(n)){ n = base + i; i++; }
    seenNames.add(n);
    return n;
  }
  function resolveRef(ref){
    return ref.split('/').pop();
  }
  function typeFor(s, parentName, fieldName){
    if(s === true) return 'Any';
    if(!s || typeof s !== 'object') return 'Any';
    if(s.$ref){
      return resolveRef(s.$ref);
    }
    if(Array.isArray(s.oneOf) || Array.isArray(s.anyOf)){
      imports.typing.add('Union');
      const variants = (s.oneOf || s.anyOf).map(sub => typeFor(sub, parentName, fieldName));
      if(variants.includes('None')){
        imports.typing.add('Optional');
        const others = variants.filter(v => v !== 'None');
        if(others.length === 1) return 'Optional[' + others[0] + ']';
        return 'Optional[Union[' + others.join(', ') + ']]';
      }
      return 'Union[' + variants.join(', ') + ']';
    }
    if(Array.isArray(s.allOf) && s.allOf.length === 1){
      return typeFor(s.allOf[0], parentName, fieldName);
    }
    if(s.const !== undefined){
      imports.typing.add('Literal');
      return 'Literal[' + jsPyRepr(s.const) + ']';
    }
    if(Array.isArray(s.enum)){
      imports.typing.add('Literal');
      return 'Literal[' + s.enum.map(jsPyRepr).join(', ') + ']';
    }
    let t = s.type;
    if(Array.isArray(t)){
      // type: ["string", "null"] -> Optional[str]
      const nonNull = t.filter(x => x !== 'null');
      if(nonNull.length === 1 && t.includes('null')){
        imports.typing.add('Optional');
        const inner = typeFor({...s, type: nonNull[0]}, parentName, fieldName);
        return 'Optional[' + inner + ']';
      }
      imports.typing.add('Union');
      const variants = t.map(tt => typeFor({...s, type: tt}, parentName, fieldName));
      return 'Union[' + variants.join(', ') + ']';
    }
    if(t === 'string'){
      const f = s.format;
      if(f === 'date-time'){ imports.datetime.add('datetime'); return 'datetime'; }
      if(f === 'date'){ imports.datetime.add('date'); return 'date'; }
      if(f === 'time'){ imports.datetime.add('time'); return 'time'; }
      if(f === 'email'){ imports.pydantic.add('EmailStr'); return 'EmailStr'; }
      if(f === 'uri' || f === 'url'){ imports.pydantic.add('HttpUrl'); return 'HttpUrl'; }
      if(f === 'uuid'){ imports.uuid = true; return 'UUID'; }
      return 'str';
    }
    if(t === 'integer') return 'int';
    if(t === 'number') return 'float';
    if(t === 'boolean') return 'bool';
    if(t === 'null') return 'None';
    if(t === 'array'){
      imports.typing.add('List');
      const itemType = s.items ? typeFor(s.items, parentName, (fieldName || 'Item') + 'Item') : 'Any';
      return 'List[' + itemType + ']';
    }
    if(t === 'object' || s.properties){
      // Nested class
      const nestedBase = s.title ? jsToPascal(s.title) : jsToPascal((fieldName || 'Nested'));
      const nestedName = uniqueName(nestedBase);
      buildClass(s, nestedName);
      return nestedName;
    }
    imports.typing.add('Any');
    return 'Any';
  }
  function buildClass(s, name){
    const props = s.properties || {};
    const required = new Set(s.required || []);
    const fieldLines = [];
    for(const [propName, propSchema] of Object.entries(props)){
      const t = typeFor(propSchema, name, propName);
      const isRequired = required.has(propName);
      const hasDefault = propSchema && propSchema.default !== undefined;
      const desc = propSchema && typeof propSchema === 'object' ? propSchema.description : null;
      let typeAnn = t;
      if(!isRequired && !t.startsWith('Optional[') && !hasDefault){
        imports.typing.add('Optional');
        typeAnn = 'Optional[' + t + ']';
      }
      let defaultExpr;
      if(hasDefault){
        defaultExpr = jsPyRepr(propSchema.default);
      } else if(!isRequired){
        defaultExpr = 'None';
      } else {
        defaultExpr = '...';
      }
      const safeName = /^[a-zA-Z_][a-zA-Z0-9_]*$/.test(propName) && !JS_PY_KW.has(propName) ? propName : (propName.replace(/[^a-zA-Z0-9_]/g, '_') + '_');
      const aliasNeeded = safeName !== propName;
      const fieldArgs = [];
      if(defaultExpr === '...' && (desc || aliasNeeded)){
        fieldArgs.push('...');
      } else if(defaultExpr !== '...'){
        fieldArgs.push('default=' + defaultExpr);
      }
      if(aliasNeeded) fieldArgs.push('alias=' + jsEscString(propName));
      if(desc) fieldArgs.push('description=' + jsEscString(desc));
      if(propSchema && propSchema.minimum !== undefined) fieldArgs.push('ge=' + propSchema.minimum);
      if(propSchema && propSchema.maximum !== undefined) fieldArgs.push('le=' + propSchema.maximum);
      if(propSchema && propSchema.minLength !== undefined) fieldArgs.push('min_length=' + propSchema.minLength);
      if(propSchema && propSchema.maxLength !== undefined) fieldArgs.push('max_length=' + propSchema.maxLength);
      let rhs;
      if(fieldArgs.length === 0){
        rhs = '';
      } else if(fieldArgs.length === 1 && fieldArgs[0] === '...'){
        rhs = ' = Field(...)';
        imports.pydantic.add('Field');
      } else if(fieldArgs.length === 1 && fieldArgs[0].startsWith('default=') && !aliasNeeded && !desc){
        rhs = ' = ' + fieldArgs[0].slice('default='.length);
      } else {
        rhs = ' = Field(' + fieldArgs.join(', ') + ')';
        imports.pydantic.add('Field');
      }
      fieldLines.push('    ' + safeName + ': ' + typeAnn + rhs);
    }
    const todoLines = [];
    if(s.patternProperties) todoLines.push('    # TODO: patternProperties not yet supported');
    if(s.additionalProperties && typeof s.additionalProperties === 'object') todoLines.push('    # TODO: additionalProperties as schema not yet supported');
    if(Array.isArray(s.allOf) && s.allOf.length > 1) todoLines.push('    # TODO: allOf composition (multi-base) not yet supported');
    let body;
    if(fieldLines.length === 0 && todoLines.length === 0){
      body = '    pass';
    } else {
      body = [...todoLines, ...fieldLines].join('\\n');
    }
    let configBlock = '';
    if(fieldLines.some(l => l.includes('alias='))){
      if(v2){
        imports.pydantic.add('ConfigDict');
        configBlock = '    model_config = ConfigDict(populate_by_name=True)\\n\\n';
      } else {
        configBlock = '    class Config:\\n        allow_population_by_field_name = True\\n\\n';
      }
    }
    classes.push('class ' + name + '(BaseModel):\\n' + configBlock + body);
  }
  // root
  const rootClassName = uniqueName(jsToPascal(rootName));
  if(schema && typeof schema === 'object' && (schema.type === 'object' || schema.properties)){
    buildClass(schema, rootClassName);
  } else {
    // Top-level is a primitive / array / union — wrap with a single-field model
    const t = typeFor(schema, rootClassName, 'value');
    classes.push('class ' + rootClassName + '(BaseModel):\\n    root: ' + t);
  }
  // Definitions ($defs / definitions)
  const defs = schema.$defs || schema.definitions;
  if(defs && typeof defs === 'object'){
    for(const [name, defSchema] of Object.entries(defs)){
      if(seenNames.has(name)) continue;
      seenNames.add(name);
      buildClass(defSchema, name);
    }
  }
  // Build header
  const lines = [];
  if(imports.typing.size){
    lines.push('from typing import ' + [...imports.typing].sort().join(', '));
  }
  if(imports.datetime.size){
    lines.push('from datetime import ' + [...imports.datetime].sort().join(', '));
  }
  if(imports.uuid){
    lines.push('from uuid import UUID');
  }
  lines.push('from pydantic import ' + [...imports.pydantic].sort().join(', '));
  lines.push('');
  lines.push('');
  // Classes are pushed after their fields are built, so nested ones land
  // earlier in the array than their parents — which is exactly the order
  // Python needs (definition before reference).
  lines.push(classes.join('\\n\\n\\n'));
  return lines.join('\\n') + '\\n';
}
const JS_PY_KW = new Set(['False','None','True','and','as','assert','async','await','break','class','continue','def','del','elif','else','except','finally','for','from','global','if','import','in','is','lambda','nonlocal','not','or','pass','raise','return','try','while','with','yield']);
function jsRun(){
  const raw = document.getElementById('js-input').value.trim();
  const out = document.getElementById('js-output');
  const meta = document.getElementById('js-meta');
  if(!raw){
    out.classList.remove('error');
    out.textContent = '{LBL_NO_INPUT}';
    meta.textContent = '';
    return;
  }
  let schema;
  try { schema = JSON.parse(raw); }
  catch(e){
    out.classList.add('error');
    out.textContent = '✗ JSON parse error: ' + e.message;
    meta.textContent = '';
    return;
  }
  const v2 = document.getElementById('js-version').value === 'v2';
  const name = document.getElementById('js-name').value || 'Model';
  try {
    const code = jsGenerate(schema, name, v2);
    out.classList.remove('error');
    out.textContent = code;
    const classCount = (code.match(/^class /gm) || []).length;
    meta.textContent = classCount + ' class' + (classCount === 1 ? '' : 'es') + ' generated · ' + (v2 ? 'Pydantic v2' : 'Pydantic v1');
  } catch(e){
    out.classList.add('error');
    out.textContent = '✗ ' + e.message;
    meta.textContent = '';
  }
}
function jsCopy(btn){
  const text = document.getElementById('js-output').textContent;
  navigator.clipboard.writeText(text).then(() => {
    const orig = btn.textContent;
    btn.textContent = '✓';
    setTimeout(() => btn.textContent = orig, 1400);
  });
}
document.addEventListener('DOMContentLoaded', jsRun);
</script>
""",
    "help": {
        "en": """
<h2>What is this for?</h2>
<p>JSON Schema and Pydantic are the two main ways to describe a structured object — JSON Schema is the lingua-franca for OpenAPI specs, LLM function-calling, and structured outputs; Pydantic is Python's de-facto data-validation library. Pretty often you have one and you need the other. This tool does the conversion in one direction: paste a JSON Schema, get a Pydantic class you can drop into a Python file.</p>

<h3>When to use it</h3>
<ul>
  <li><strong>LLM structured outputs.</strong> OpenAI's <code>response_format</code> and Anthropic's tool-use both take a JSON Schema. After you've designed and tested the schema, you usually want a Pydantic model to validate and access fields in Python.</li>
  <li><strong>OpenAPI client generation.</strong> An OpenAPI spec gives you JSON Schema for each request/response body; this is a quick way to get matching Pydantic models without pulling a full code-generator.</li>
  <li><strong>Schema → Python migrations.</strong> You've inherited a system that uses JSON Schema for validation and you want to move it to Pydantic. Paste each schema, get a starting class, refine.</li>
  <li><strong>Quick scaffolding.</strong> You sketched the shape in JSON in a notebook; turn it into a real model with one paste.</li>
</ul>

<h3>What this tool handles</h3>
<ul>
  <li><code>type: object</code> with <code>properties</code> + <code>required</code> — generates a nested BaseModel class.</li>
  <li><code>type: array</code> with <code>items</code> — generates <code>List[X]</code>.</li>
  <li>Primitives: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> on string: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; a null variant folds into <code>Optional[...]</code>.</li>
  <li><code>type: ["string", "null"]</code> arrays → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → field default.</li>
  <li><code>minimum</code>/<code>maximum</code>/<code>minLength</code>/<code>maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Field names that aren't valid Python identifiers (hyphens, leading digits, keywords) are sanitised, and an <code>alias=</code> is added to preserve the original wire name. The model is configured with <code>populate_by_name=True</code> so both work.</li>
  <li>Nested objects become nested classes, named after the field if no <code>title</code> is provided.</li>
  <li><code>$defs</code> / <code>definitions</code> are emitted as separate classes.</li>
</ul>

<h3>What this tool doesn't handle (yet)</h3>
<ul>
  <li>External <code>$ref</code> (URL refs). Local refs to <code>$defs</code> work.</li>
  <li><code>patternProperties</code>. Emits a TODO comment.</li>
  <li><code>additionalProperties</code> with a schema. Emits a TODO comment.</li>
  <li>Multi-base <code>allOf</code> composition. Single-element <code>allOf</code> is unwrapped.</li>
  <li>Custom validators based on <code>pattern</code> — emit the field but skip the regex. Add a Pydantic <code>@field_validator</code> after pasting if you need it.</li>
</ul>

<h3>Common gotchas</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Default output is v2 (current). Switch via the dropdown if you're on a v1 codebase. Big differences: <code>Field(...)</code> for required, <code>model_config</code> replaces <code>class Config:</code>, deprecated validator decorators.</li>
  <li><strong>Output is a starting point, not the final word.</strong> Review it. Especially for complex unions, recursive types, and anything with custom validation rules.</li>
  <li><strong>Names matter.</strong> The "Root class name" field controls the top-level class name. Sub-classes are named after their <code>title</code> property when present, otherwise after the field that contains them.</li>
  <li><strong>Required vs Optional in v2.</strong> A required field with no default uses <code>...</code>: <code>name: str = Field(...)</code>. Optional fields default to <code>None</code> and are wrapped in <code>Optional[T]</code>.</li>
</ul>
""",
        "de": """
<h2>Wozu ist das gut?</h2>
<p>JSON Schema und Pydantic sind die beiden Hauptarten, ein strukturiertes Objekt zu beschreiben — JSON Schema ist die Lingua Franca für OpenAPI, LLM Function Calling und Structured Outputs; Pydantic ist Pythons De-facto-Validierungsbibliothek. Oft hast du eines und brauchst das andere. Dieses Tool macht die Umwandlung in eine Richtung: JSON Schema einfügen, Pydantic-Klasse rausziehen.</p>

<h3>Wann nützlich</h3>
<ul>
  <li><strong>LLM Structured Outputs.</strong> OpenAIs <code>response_format</code> und Anthropics Tool-Use nehmen beide JSON Schema. Nach dem Entwurf willst du meist eine Pydantic-Klasse zum Validieren und Zugreifen in Python.</li>
  <li><strong>OpenAPI-Client-Generierung.</strong> Eine OpenAPI-Spec liefert JSON Schema für Request/Response Bodies; schnellster Weg zu passenden Pydantic-Modellen ohne vollen Codegenerator.</li>
  <li><strong>Schema → Python Migrationen.</strong> System mit JSON-Schema-Validierung soll auf Pydantic umziehen. Schema einfügen, Startklasse, verfeinern.</li>
  <li><strong>Schnelles Scaffolding.</strong> Form im Notebook in JSON skizziert, ein Paste macht daraus ein echtes Model.</li>
</ul>

<h3>Was das Tool unterstützt</h3>
<ul>
  <li><code>type: object</code> mit <code>properties</code> + <code>required</code> — verschachtelte BaseModel-Klasse.</li>
  <li><code>type: array</code> mit <code>items</code> — <code>List[X]</code>.</li>
  <li>Primitive: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> bei String: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; Null-Variante wird zu <code>Optional[...]</code>.</li>
  <li><code>type: ["string", "null"]</code> → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → Feld-Default.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Ungültige Python-Bezeichner werden saniert; <code>alias=</code> erhält den Original-Wire-Namen. <code>populate_by_name=True</code> wird konfiguriert.</li>
  <li>Verschachtelte Objekte werden eigene Klassen, benannt nach Feld oder <code>title</code>.</li>
  <li><code>$defs / definitions</code> kommen als separate Klassen.</li>
</ul>

<h3>Was (noch) nicht</h3>
<ul>
  <li>Externe <code>$ref</code> (URLs). Lokale Refs auf <code>$defs</code> klappen.</li>
  <li><code>patternProperties</code> — TODO-Kommentar.</li>
  <li><code>additionalProperties</code> als Schema — TODO-Kommentar.</li>
  <li>Mehrstufige <code>allOf</code>-Komposition. Einzelnes <code>allOf</code> wird entpackt.</li>
  <li>Custom Validatoren via <code>pattern</code> — Feld bleibt, Regex weg. Bei Bedarf <code>@field_validator</code> nachreichen.</li>
</ul>

<h3>Typische Stolperfallen</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Default ist v2. Bei v1-Codebase umschalten. Großer Unterschied: <code>Field(...)</code> für Required, <code>model_config</code> statt <code>class Config:</code>.</li>
  <li><strong>Output ist Startpunkt, nicht Endpunkt.</strong> Reviewen, besonders bei Unions und rekursiven Typen.</li>
  <li><strong>Namen zählen.</strong> „Root class name" steuert die Top-Level-Klasse. Sub-Klassen erben aus <code>title</code> oder Feldnamen.</li>
  <li><strong>Required vs Optional in v2.</strong> Required ohne Default → <code>...</code>. Optional → <code>None</code> in <code>Optional[T]</code>.</li>
</ul>
""",
        "es": """
<h2>¿Para qué sirve?</h2>
<p>JSON Schema y Pydantic son las dos formas principales de describir un objeto estructurado — JSON Schema es la lingua franca para OpenAPI, function calling de LLMs y structured outputs; Pydantic es la librería de validación de facto en Python. A menudo tienes uno y necesitas el otro. Esta herramienta hace la conversión en una dirección: pega un JSON Schema, obtén una clase Pydantic lista para soltar en un .py.</p>

<h3>Cuándo usarla</h3>
<ul>
  <li><strong>Structured outputs de LLM.</strong> El <code>response_format</code> de OpenAI y el tool-use de Anthropic toman JSON Schema. Tras diseñarlo, normalmente quieres un modelo Pydantic para validar y acceder a campos.</li>
  <li><strong>Generación de clientes OpenAPI.</strong> Una spec OpenAPI te da JSON Schema por body; esto es una vía rápida a modelos Pydantic sin un generador completo.</li>
  <li><strong>Migración Schema → Python.</strong> Heredas un sistema con validación por JSON Schema y quieres pasar a Pydantic. Pega cada schema, obtén clase de partida, refina.</li>
  <li><strong>Scaffolding rápido.</strong> Esbozaste la forma en JSON en un notebook; conviértelo en modelo real con un pegado.</li>
</ul>

<h3>Qué maneja</h3>
<ul>
  <li><code>type: object</code> con <code>properties</code> + <code>required</code> — clase BaseModel anidada.</li>
  <li><code>type: array</code> con <code>items</code> — <code>List[X]</code>.</li>
  <li>Primitivos: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> en string: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; variante null se pliega a <code>Optional[...]</code>.</li>
  <li><code>type: ["string", "null"]</code> → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → default del campo.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Identificadores no válidos en Python se sanean y se añade <code>alias=</code>; el modelo lleva <code>populate_by_name=True</code>.</li>
  <li>Objetos anidados → clases anidadas, con nombre del <code>title</code> si está, si no del campo.</li>
  <li><code>$defs/definitions</code> → clases separadas.</li>
</ul>

<h3>Lo que no maneja (todavía)</h3>
<ul>
  <li><code>$ref</code> externos (URLs). Los locales a <code>$defs</code> sí.</li>
  <li><code>patternProperties</code> — comentario TODO.</li>
  <li><code>additionalProperties</code> con schema — comentario TODO.</li>
  <li><code>allOf</code> multi-base. <code>allOf</code> de un elemento se desenvuelve.</li>
  <li>Validadores custom con <code>pattern</code> — el campo se emite, el regex se omite. Añade <code>@field_validator</code> tras pegar si lo necesitas.</li>
</ul>

<h3>Errores comunes</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Por defecto v2. Cambia con el dropdown si tu codebase es v1.</li>
  <li><strong>La salida es un punto de partida.</strong> Revísala, especialmente uniones complejas y tipos recursivos.</li>
  <li><strong>Los nombres importan.</strong> El campo "Root class name" controla el nombre de la clase top-level. Las sub-clases usan <code>title</code> o el nombre de campo.</li>
  <li><strong>Required vs Optional en v2.</strong> Required sin default usa <code>...</code>. Optional cae a <code>None</code> envuelto en <code>Optional[T]</code>.</li>
</ul>
""",
        "fr": """
<h2>À quoi ça sert ?</h2>
<p>JSON Schema et Pydantic sont les deux manières principales de décrire un objet structuré — JSON Schema est la lingua franca pour OpenAPI, le function calling LLM et les structured outputs ; Pydantic est la lib de validation de facto en Python. Souvent vous avez l'un et il vous faut l'autre. Cet outil fait la conversion dans un sens : collez un JSON Schema, obtenez une classe Pydantic prête à poser dans un .py.</p>

<h3>Quand l'utiliser</h3>
<ul>
  <li><strong>Structured outputs LLM.</strong> Le <code>response_format</code> d'OpenAI et le tool-use d'Anthropic prennent du JSON Schema. Après conception, vous voulez généralement un modèle Pydantic pour valider et accéder aux champs en Python.</li>
  <li><strong>Génération de client OpenAPI.</strong> Une spec OpenAPI fournit du JSON Schema par body ; voie rapide vers des modèles Pydantic sans générateur complet.</li>
  <li><strong>Migration Schema → Python.</strong> Système hérité avec validation JSON Schema à migrer vers Pydantic. Collez chaque schema, obtenez une classe de départ, raffinez.</li>
  <li><strong>Scaffolding rapide.</strong> Forme esquissée en JSON dans un notebook ; transformez-la en vrai modèle d'un coller.</li>
</ul>

<h3>Ce que l'outil gère</h3>
<ul>
  <li><code>type: object</code> avec <code>properties</code> + <code>required</code> — classe BaseModel imbriquée.</li>
  <li><code>type: array</code> avec <code>items</code> — <code>List[X]</code>.</li>
  <li>Primitifs : <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> sur string : <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code> ; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code> ; une variante null se replie en <code>Optional[...]</code>.</li>
  <li><code>type: ["string", "null"]</code> → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → default du champ.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Noms invalides en Python sont assainis et <code>alias=</code> ajouté ; modèle configuré avec <code>populate_by_name=True</code>.</li>
  <li>Objets imbriqués → classes imbriquées, nommées par <code>title</code> s'il existe, sinon par le champ.</li>
  <li><code>$defs/definitions</code> → classes séparées.</li>
</ul>

<h3>Ce qu'il ne gère pas (encore)</h3>
<ul>
  <li><code>$ref</code> externes (URLs). Les locaux vers <code>$defs</code> marchent.</li>
  <li><code>patternProperties</code> — commentaire TODO.</li>
  <li><code>additionalProperties</code> avec schema — commentaire TODO.</li>
  <li>Composition <code>allOf</code> multi-base. <code>allOf</code> à un élément est déballé.</li>
  <li>Validateurs custom basés sur <code>pattern</code> — le champ sort, le regex est omis. Ajoutez un <code>@field_validator</code> après si besoin.</li>
</ul>

<h3>Pièges courants</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Sortie par défaut v2. Bascule via le dropdown si v1.</li>
  <li><strong>La sortie est un point de départ.</strong> Relisez, surtout pour unions complexes et types récursifs.</li>
  <li><strong>Les noms comptent.</strong> Le champ "Root class name" contrôle le nom de la classe top-level. Sous-classes : <code>title</code> ou nom de champ.</li>
  <li><strong>Required vs Optional en v2.</strong> Required sans default utilise <code>...</code>. Optional retombe sur <code>None</code> enveloppé en <code>Optional[T]</code>.</li>
</ul>
""",
        "it": """
<h2>A cosa serve?</h2>
<p>JSON Schema e Pydantic sono i due modi principali per descrivere un oggetto strutturato — JSON Schema è la lingua franca per OpenAPI, function calling LLM e structured output; Pydantic è la libreria di validazione di fatto in Python. Spesso hai uno e ti serve l'altro. Questo strumento fa la conversione in una direzione: incolla un JSON Schema, ottieni una classe Pydantic pronta da incollare in un .py.</p>

<h3>Quando usarlo</h3>
<ul>
  <li><strong>Structured output LLM.</strong> Il <code>response_format</code> di OpenAI e il tool-use di Anthropic prendono entrambi JSON Schema. Dopo averlo progettato, di solito vuoi un modello Pydantic per validare e accedere ai campi in Python.</li>
  <li><strong>Generazione client OpenAPI.</strong> Una spec OpenAPI ti dà JSON Schema per body; via rapida a modelli Pydantic senza un generatore completo.</li>
  <li><strong>Migrazione Schema → Python.</strong> Hai ereditato un sistema con validazione JSON Schema da spostare a Pydantic. Incolla ogni schema, ottieni classe iniziale, rifinisci.</li>
  <li><strong>Scaffolding veloce.</strong> Forma abbozzata in JSON in un notebook; trasformala in modello vero con un incolla.</li>
</ul>

<h3>Cosa gestisce</h3>
<ul>
  <li><code>type: object</code> con <code>properties</code> + <code>required</code> — classe BaseModel annidata.</li>
  <li><code>type: array</code> con <code>items</code> — <code>List[X]</code>.</li>
  <li>Primitivi: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> su string: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; variante null si ripiega in <code>Optional[...]</code>.</li>
  <li><code>type: ["string", "null"]</code> → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → default del campo.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Nomi non validi in Python vengono sanificati e <code>alias=</code> aggiunto; modello con <code>populate_by_name=True</code>.</li>
  <li>Oggetti annidati → classi annidate, denominate per <code>title</code> se presente, altrimenti per il campo.</li>
  <li><code>$defs/definitions</code> → classi separate.</li>
</ul>

<h3>Cosa non gestisce (ancora)</h3>
<ul>
  <li><code>$ref</code> esterni (URL). Ref locali a <code>$defs</code> funzionano.</li>
  <li><code>patternProperties</code> — commento TODO.</li>
  <li><code>additionalProperties</code> con schema — commento TODO.</li>
  <li>Composizione <code>allOf</code> multi-base. <code>allOf</code> a singolo elemento è spacchettato.</li>
  <li>Validatori custom su <code>pattern</code> — il campo esce, la regex no. Aggiungi <code>@field_validator</code> dopo se serve.</li>
</ul>

<h3>Trappole comuni</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Default v2. Cambia col dropdown se sei su v1.</li>
  <li><strong>L'output è un punto di partenza.</strong> Revisionalo, specialmente union complesse e tipi ricorsivi.</li>
  <li><strong>I nomi contano.</strong> Il campo "Root class name" controlla il nome della classe top-level. Sotto-classi: <code>title</code> o nome di campo.</li>
  <li><strong>Required vs Optional in v2.</strong> Required senza default usa <code>...</code>. Optional cade a <code>None</code> avvolto in <code>Optional[T]</code>.</li>
</ul>
""",
        "pt": """
<h2>Para que serve?</h2>
<p>JSON Schema e Pydantic são as duas formas principais de descrever um objeto estruturado — JSON Schema é a lingua franca pra OpenAPI, function calling de LLMs e structured outputs; Pydantic é a lib de validação de fato em Python. Frequentemente você tem um e precisa do outro. Esta ferramenta faz a conversão em uma direção: cola um JSON Schema, recebe uma classe Pydantic pronta pra colar num .py.</p>

<h3>Quando usar</h3>
<ul>
  <li><strong>Structured outputs de LLM.</strong> O <code>response_format</code> da OpenAI e o tool-use da Anthropic pegam ambos JSON Schema. Depois de desenhar, você geralmente quer um modelo Pydantic pra validar e acessar campos.</li>
  <li><strong>Geração de client OpenAPI.</strong> Uma spec OpenAPI te dá JSON Schema por body; jeito rápido de chegar a modelos Pydantic sem um gerador completo.</li>
  <li><strong>Migração Schema → Python.</strong> Herdou um sistema com validação JSON Schema pra mover pro Pydantic. Cola cada schema, ganha classe inicial, refina.</li>
  <li><strong>Scaffolding rápido.</strong> Forma rascunhada em JSON num notebook; transforma em modelo real com um colar.</li>
</ul>

<h3>O que a ferramenta entende</h3>
<ul>
  <li><code>type: object</code> com <code>properties</code> + <code>required</code> — classe BaseModel aninhada.</li>
  <li><code>type: array</code> com <code>items</code> — <code>List[X]</code>.</li>
  <li>Primitivos: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> em string: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; variante null se dobra em <code>Optional[...]</code>.</li>
  <li><code>type: ["string", "null"]</code> → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → default do campo.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Nomes inválidos pra Python são higienizados e <code>alias=</code> adicionado; modelo configurado com <code>populate_by_name=True</code>.</li>
  <li>Objetos aninhados → classes aninhadas, nomeadas por <code>title</code> se houver, senão pelo campo.</li>
  <li><code>$defs/definitions</code> → classes separadas.</li>
</ul>

<h3>O que não entende (ainda)</h3>
<ul>
  <li><code>$ref</code> externo (URLs). Refs locais pra <code>$defs</code> funcionam.</li>
  <li><code>patternProperties</code> — comentário TODO.</li>
  <li><code>additionalProperties</code> com schema — comentário TODO.</li>
  <li>Composição <code>allOf</code> multi-base. <code>allOf</code> de um elemento é desempacotado.</li>
  <li>Validadores custom de <code>pattern</code> — campo sai, regex não. Adicione <code>@field_validator</code> depois se precisar.</li>
</ul>

<h3>Pegadinhas comuns</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Default v2. Troca no dropdown se for v1.</li>
  <li><strong>A saída é ponto de partida.</strong> Revisa, especialmente uniões complexas e tipos recursivos.</li>
  <li><strong>Nomes importam.</strong> O campo "Root class name" controla o nome da classe top-level. Sub-classes: <code>title</code> ou nome do campo.</li>
  <li><strong>Required vs Optional em v2.</strong> Required sem default usa <code>...</code>. Optional cai pra <code>None</code> envolto em <code>Optional[T]</code>.</li>
</ul>
""",
        "pl": """
<h2>Do czego to służy?</h2>
<p>JSON Schema i Pydantic to dwa główne sposoby opisywania ustrukturyzowanego obiektu — JSON Schema to lingua franca dla OpenAPI, function calling LLM-ów i structured output; Pydantic to faktyczny standard walidacji w Pythonie. Często masz jedno, a potrzebujesz drugiego. To narzędzie robi konwersję w jedną stronę: wklej JSON Schema, dostań klasę Pydantic gotową do wklejenia do pliku .py.</p>

<h3>Kiedy tego użyć</h3>
<ul>
  <li><strong>Structured output LLM-a.</strong> <code>response_format</code> z OpenAI i tool-use z Anthropica oba biorą JSON Schema. Po zaprojektowaniu zwykle chcesz model Pydantic do walidacji i dostępu do pól w Pythonie.</li>
  <li><strong>Generowanie klienta OpenAPI.</strong> Spec OpenAPI daje JSON Schema na request/response body; szybki sposób na modele Pydantic bez pełnego generatora.</li>
  <li><strong>Migracja Schema → Python.</strong> Odziedziczyłeś system z walidacją JSON Schema, chcesz przejść na Pydantic. Wklej każdy schema, dostań klasę startową, dopracuj.</li>
  <li><strong>Szybki scaffolding.</strong> Naszkicowałeś kształt w JSON-ie w notebooku; zamień to w prawdziwy model jednym wklejeniem.</li>
</ul>

<h3>Co narzędzie obsługuje</h3>
<ul>
  <li><code>type: object</code> z <code>properties</code> + <code>required</code> — zagnieżdżona klasa BaseModel.</li>
  <li><code>type: array</code> z <code>items</code> — <code>List[X]</code>.</li>
  <li>Prymitywy: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> na stringu: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; wariant null składa się do <code>Optional[...]</code>.</li>
  <li><code>type: ["string", "null"]</code> → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → default pola.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Nazwy nie-pythonowe są oczyszczane, dodawany <code>alias=</code>; model konfigurowany z <code>populate_by_name=True</code>.</li>
  <li>Obiekty zagnieżdżone → klasy zagnieżdżone, nazwane wg <code>title</code> jeśli jest, inaczej wg pola.</li>
  <li><code>$defs/definitions</code> → osobne klasy.</li>
</ul>

<h3>Czego (jeszcze) nie obsługuje</h3>
<ul>
  <li>Zewnętrzne <code>$ref</code> (URLe). Lokalne do <code>$defs</code> działają.</li>
  <li><code>patternProperties</code> — komentarz TODO.</li>
  <li><code>additionalProperties</code> z schemą — komentarz TODO.</li>
  <li>Wielobazowa kompozycja <code>allOf</code>. Jednoelementowe <code>allOf</code> jest rozpakowywane.</li>
  <li>Custom walidatory na <code>pattern</code> — pole jest, regex pomijany. Dodaj <code>@field_validator</code> po wklejeniu, jeśli potrzeba.</li>
</ul>

<h3>Częste pułapki</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Default v2. Przełącz w dropdownie, jeśli codebase to v1.</li>
  <li><strong>Wyjście to punkt startowy.</strong> Zrób review, zwłaszcza dla złożonych unii i typów rekurencyjnych.</li>
  <li><strong>Nazwy się liczą.</strong> Pole "Root class name" kontroluje nazwę top-levelowej klasy. Podklasy: <code>title</code> lub nazwa pola.</li>
  <li><strong>Required vs Optional w v2.</strong> Required bez defaultu używa <code>...</code>. Optional spada do <code>None</code> w <code>Optional[T]</code>.</li>
</ul>
""",
        "ja": """
<h2>用途</h2>
<p>JSON Schema と Pydantic は構造化オブジェクトを記述する 2 大手段です。JSON Schema は OpenAPI、LLM の function calling、構造化出力で共通言語となっており、Pydantic は Python のデファクトのバリデーションライブラリです。一方を持っていてもう一方が欲しい、という場面は頻繁にあります。本ツールは一方向の変換を担当します。JSON Schema を貼り付けると、<code>.py</code> にそのまま貼り付け可能な Pydantic クラスが得られます。</p>

<h3>使うべきタイミング</h3>
<ul>
  <li><strong>LLM の構造化出力。</strong> OpenAI の <code>response_format</code> や Anthropic の tool-use はどちらも JSON Schema を受け付けます。設計が固まった後、Python 側でバリデーションとフィールドアクセスのために Pydantic モデルが欲しくなります。</li>
  <li><strong>OpenAPI クライアント生成。</strong> OpenAPI 仕様には JSON Schema が含まれます。フル機能の codegen を入れずに、対応する Pydantic モデルを得る近道。</li>
  <li><strong>Schema から Python への移行。</strong> 既存の JSON Schema バリデーションを Pydantic に置き換えたい場合、貼り付け→雛形→手直し、という流れに使えます。</li>
  <li><strong>素早い足場作り。</strong> ノートブックで JSON で構造を試したものを、貼り付け 1 回で本物のモデルにできます。</li>
</ul>

<h3>対応している項目</h3>
<ul>
  <li><code>type: object</code> ＋ <code>properties</code> ＋ <code>required</code> → ネストした BaseModel クラス。</li>
  <li><code>type: array</code> ＋ <code>items</code> → <code>List[X]</code>。</li>
  <li>プリミティブ: <code>string</code> → <code>str</code>、<code>integer</code> → <code>int</code>、<code>number</code> → <code>float</code>、<code>boolean</code> → <code>bool</code>、<code>null</code> → <code>None</code>。</li>
  <li>string の <code>format</code>: <code>date-time</code> → <code>datetime</code>、<code>email</code> → <code>EmailStr</code>、<code>uri/url</code> → <code>HttpUrl</code>、<code>uuid</code> → <code>UUID</code>。</li>
  <li><code>enum</code> → <code>Literal[...]</code>、<code>const</code> → <code>Literal[X]</code>。</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>。null バリアントがあれば <code>Optional[...]</code> に畳まれます。</li>
  <li><code>type: ["string", "null"]</code> → <code>Optional[str]</code>。</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>。</li>
  <li><code>default</code> → フィールドのデフォルト値。</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>。</li>
  <li>Python 識別子として不正な名前はサニタイズされ、<code>alias=</code> で元のキーを保持。モデルは <code>populate_by_name=True</code> で設定されます。</li>
  <li>ネストしたオブジェクトはネストクラスになり、<code>title</code> があればそれを、なければフィールド名から命名されます。</li>
  <li><code>$defs</code>／<code>definitions</code> は別クラスとして出力されます。</li>
</ul>

<h3>未対応（今のところ）</h3>
<ul>
  <li>外部 <code>$ref</code>（URL 参照）。<code>$defs</code> へのローカル参照は対応。</li>
  <li><code>patternProperties</code> — TODO コメントを出力。</li>
  <li><code>additionalProperties</code> をスキーマで指定する形 — TODO コメント。</li>
  <li>多元 <code>allOf</code> 合成。要素 1 つの <code>allOf</code> は展開されます。</li>
  <li><code>pattern</code> ベースのカスタムバリデーション — フィールドは出すが正規表現は省略。必要なら貼り付け後に <code>@field_validator</code> を追加してください。</li>
</ul>

<h3>よくある注意点</h3>
<ul>
  <li><strong>Pydantic v2 と v1。</strong> 既定は v2（最新）。v1 のコードベースならドロップダウンで切り替えてください。<code>Field(...)</code>、<code>model_config</code> 対 <code>class Config:</code>、バリデーターデコレータの違いに注意。</li>
  <li><strong>出力はあくまでスタート地点。</strong> 複雑な Union や再帰型は必ず見直してください。</li>
  <li><strong>名前は重要。</strong> 「Root class name」がトップレベルのクラス名を決めます。サブクラスは <code>title</code> があればそれを使い、なければフィールド名から命名されます。</li>
  <li><strong>v2 の Required と Optional。</strong> デフォルトなしの required は <code>name: str = Field(...)</code>。Optional は <code>None</code> をデフォルトに <code>Optional[T]</code> で包まれます。</li>
</ul>
""",
        "nl": """
<h2>Waarvoor is dit?</h2>
<p>JSON Schema en Pydantic zijn de twee belangrijkste manieren om een gestructureerd object te beschrijven — JSON Schema is de lingua franca voor OpenAPI specs, LLM function-calling en structured outputs; Pydantic is Python's de-facto data-validation library. Vaak heb je de één en je hebt de ander nodig. Deze tool doet de conversie in één richting: plak een JSON Schema, krijg een Pydantic class die je in een Python file kunt droppen.</p>

<h3>Wanneer gebruiken</h3>
<ul>
  <li><strong>LLM structured outputs.</strong> OpenAI's <code>response_format</code> en Anthropic's tool-use nemen beide JSON Schema. Nadat je de schema hebt ontworpen en getest, wil je meestal een Pydantic model om velden te valideren en te benaderen in Python.</li>
  <li><strong>OpenAPI client-generatie.</strong> Een OpenAPI spec geeft je JSON Schema voor elke request/response body; dit is een snelle manier om matching Pydantic models te krijgen zonder een volledige code-generator.</li>
  <li><strong>Schema → Python migraties.</strong> Je erft een systeem dat JSON Schema gebruikt voor validatie en je wilt het naar Pydantic verplaatsen. Plak elke schema, krijg een startclass, verfijn.</li>
  <li><strong>Snelle scaffolding.</strong> Je hebt de vorm in JSON geschetst in een notebook; verander het in een echt model met één plak.</li>
</ul>

<h3>Wat deze tool afhandelt</h3>
<ul>
  <li><code>type: object</code> met <code>properties</code> + <code>required</code> — genereert een geneste BaseModel class.</li>
  <li><code>type: array</code> met <code>items</code> — genereert <code>List[X]</code>.</li>
  <li>Primitieven: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> op string: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; een null-variant vouwt naar <code>Optional[...]</code>.</li>
  <li><code>type: ["string", "null"]</code> arrays → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → field default.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Field-namen die geen geldige Python identifiers zijn worden gesanit, en een <code>alias=</code> wordt toegevoegd. Het model wordt geconfigureerd met <code>populate_by_name=True</code>.</li>
  <li>Geneste objecten worden geneste classes, genoemd naar het veld als geen <code>title</code> is opgegeven.</li>
  <li><code>$defs</code> / <code>definitions</code> worden als aparte classes uitgegeven.</li>
</ul>

<h3>Wat deze tool (nog) niet afhandelt</h3>
<ul>
  <li>Externe <code>$ref</code> (URL refs). Lokale refs naar <code>$defs</code> werken.</li>
  <li><code>patternProperties</code>. Geeft een TODO-commentaar.</li>
  <li><code>additionalProperties</code> met een schema. Geeft een TODO-commentaar.</li>
  <li>Multi-base <code>allOf</code> compositie. Single-element <code>allOf</code> wordt unwrapped.</li>
  <li>Custom validators op <code>pattern</code> — emit het veld maar skip de regex. Voeg een Pydantic <code>@field_validator</code> toe na plakken als je 'm nodig hebt.</li>
</ul>

<h3>Veelvoorkomende valkuilen</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Default output is v2 (huidig). Switch via de dropdown als je op een v1 codebase zit.</li>
  <li><strong>Output is een startpunt, geen eindpunt.</strong> Review het. Vooral voor complexe unions en recursieve types.</li>
  <li><strong>Namen matteren.</strong> Het "Root class name"-veld bepaalt de top-level class-naam. Sub-classes: <code>title</code> indien aanwezig, anders het veld dat ze bevat.</li>
  <li><strong>Required vs Optional in v2.</strong> Een required veld zonder default gebruikt <code>...</code>. Optional velden vallen terug op <code>None</code> en zijn gewikkeld in <code>Optional[T]</code>.</li>
</ul>
""",
        "tr": """
<h2>Bu ne işe yarar?</h2>
<p>JSON Schema ve Pydantic, yapılandırılmış bir nesneyi tanımlamanın iki ana yoludur — JSON Schema OpenAPI spec'leri, LLM function-calling ve yapılandırılmış çıktılar için ortak dildir; Pydantic ise Python'un fiili veri doğrulama kütüphanesidir. Sık sık birine sahip oluyorsun ve diğerine ihtiyacın oluyor. Bu araç dönüşümü tek yönde yapar: bir JSON Schema yapıştır, bir Python dosyasına bırakabileceğin bir Pydantic sınıfı al.</p>

<h3>Ne zaman kullanılır</h3>
<ul>
  <li><strong>LLM yapılandırılmış çıktılar.</strong> OpenAI'nin <code>response_format</code>'u ve Anthropic'in tool-use'u her ikisi de JSON Schema alır. Şemayı tasarlayıp test ettikten sonra genellikle Python'da alanları doğrulamak ve erişmek için bir Pydantic modeli istersin.</li>
  <li><strong>OpenAPI istemci üretimi.</strong> Bir OpenAPI spec her request/response body için JSON Schema verir; bu, tam bir kod üreteci çekmeden eşleşen Pydantic modelleri elde etmenin hızlı yoludur.</li>
  <li><strong>Şema → Python geçişleri.</strong> JSON Schema doğrulamasını kullanan bir sistemi miras aldın ve Pydantic'e taşımak istiyorsun. Her şemayı yapıştır, başlangıç sınıfı al, ince ayar yap.</li>
  <li><strong>Hızlı iskelet.</strong> Şekli bir notebook'ta JSON'da kabaca çizdin; tek yapıştırmayla onu gerçek bir modele dönüştür.</li>
</ul>

<h3>Bu aracın işlediği</h3>
<ul>
  <li><code>type: object</code> ile <code>properties</code> + <code>required</code> — iç içe BaseModel sınıfı üretir.</li>
  <li><code>type: array</code> ile <code>items</code> — <code>List[X]</code> üretir.</li>
  <li>İlkel tipler: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li>String üzerinde <code>format</code>: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; null varyant <code>Optional[...]</code>'a katlanır.</li>
  <li><code>type: ["string", "null"]</code> dizileri → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → alan varsayılanı.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Geçerli Python tanımlayıcısı olmayan alan adları temizlenir ve orijinal kablo adını korumak için bir <code>alias=</code> eklenir. Model <code>populate_by_name=True</code> ile yapılandırılır.</li>
  <li>İç içe nesneler iç içe sınıflar olur; <code>title</code> sağlanmadıysa alan adına göre adlandırılır.</li>
  <li><code>$defs</code> / <code>definitions</code> ayrı sınıflar olarak çıkarılır.</li>
</ul>

<h3>Bu aracın işlemediği (henüz)</h3>
<ul>
  <li>Harici <code>$ref</code> (URL ref'leri). <code>$defs</code>'e yerel ref'ler çalışır.</li>
  <li><code>patternProperties</code>. TODO yorumu çıkarır.</li>
  <li>Şema ile <code>additionalProperties</code>. TODO yorumu çıkarır.</li>
  <li>Çoklu tabanlı <code>allOf</code> kompozisyonu. Tek öğeli <code>allOf</code> açılır.</li>
  <li><code>pattern</code> tabanlı özel doğrulayıcılar — alanı yayınlar ama regex'i atlar. İhtiyacın olursa yapıştırdıktan sonra Pydantic <code>@field_validator</code> ekle.</li>
</ul>

<h3>Yaygın tuzaklar</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Varsayılan çıktı v2 (güncel). v1 kod tabanındaysan dropdown ile değiştir. Büyük farklar: required için <code>Field(...)</code>, <code>class Config:</code> yerine <code>model_config</code>, kullanımdan kaldırılmış doğrulayıcı dekoratörleri.</li>
  <li><strong>Çıktı bir başlangıç noktasıdır, son söz değil.</strong> İncele. Özellikle karmaşık birleşimler, özyinelemeli tipler ve özel doğrulama kuralları olan her şey için.</li>
  <li><strong>İsimler önemlidir.</strong> "Root class name" alanı üst seviye sınıf adını kontrol eder. Alt sınıflar mevcut olduğunda <code>title</code> özelliklerine göre, yoksa onları içeren alana göre adlandırılır.</li>
  <li><strong>v2'de Required vs Optional.</strong> Varsayılansız bir required alan <code>...</code> kullanır. Optional alanlar <code>None</code>'a varsayılan olur ve <code>Optional[T]</code> içinde sarılır.</li>
</ul>
""",
        "id": """
<h2>Untuk apa ini?</h2>
<p>JSON Schema dan Pydantic adalah dua cara utama mendeskripsikan objek terstruktur — JSON Schema adalah lingua franca untuk OpenAPI spec, LLM function-calling, dan structured outputs; Pydantic adalah library validasi data de-facto-nya Python. Sering kali kamu punya yang satu dan butuh yang lain. Tool ini melakukan konversi satu arah: tempel JSON Schema, dapatkan class Pydantic yang bisa langsung kamu drop ke file Python.</p>

<h3>Kapan digunakan</h3>
<ul>
  <li><strong>Structured output LLM.</strong> <code>response_format</code> dari OpenAI dan tool-use Anthropic keduanya menerima JSON Schema. Setelah kamu mendesain dan menguji schema, biasanya kamu ingin model Pydantic untuk memvalidasi dan mengakses field di Python.</li>
  <li><strong>Generasi klien OpenAPI.</strong> Spec OpenAPI memberi JSON Schema untuk tiap request/response body; ini cara cepat mendapatkan model Pydantic yang cocok tanpa menarik generator kode penuh.</li>
  <li><strong>Migrasi Schema → Python.</strong> Kamu mewarisi sistem yang pakai JSON Schema untuk validasi dan ingin memindahnya ke Pydantic. Tempel tiap schema, dapatkan class awal, refinasi.</li>
  <li><strong>Scaffolding cepat.</strong> Kamu sketsa bentuk di JSON di notebook; ubah menjadi model nyata dengan satu paste.</li>
</ul>

<h3>Apa yang tool ini tangani</h3>
<ul>
  <li><code>type: object</code> dengan <code>properties</code> + <code>required</code> — menghasilkan class BaseModel bersarang.</li>
  <li><code>type: array</code> dengan <code>items</code> — menghasilkan <code>List[X]</code>.</li>
  <li>Primitif: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> pada string: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; varian null dilipat ke <code>Optional[...]</code>.</li>
  <li>Array <code>type: ["string", "null"]</code> → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → default field.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Nama field yang bukan identifier Python valid disanitasi, dan <code>alias=</code> ditambahkan untuk mempertahankan nama wire asli. Model dikonfigurasi dengan <code>populate_by_name=True</code>.</li>
  <li>Objek bersarang menjadi class bersarang, dinamai berdasarkan field jika tidak ada <code>title</code>.</li>
  <li><code>$defs</code> / <code>definitions</code> dipancarkan sebagai class terpisah.</li>
</ul>

<h3>Yang belum ditangani</h3>
<ul>
  <li><code>$ref</code> eksternal (URL ref). Ref lokal ke <code>$defs</code> berfungsi.</li>
  <li><code>patternProperties</code>. Memancarkan komentar TODO.</li>
  <li><code>additionalProperties</code> dengan schema. Memancarkan komentar TODO.</li>
  <li>Komposisi <code>allOf</code> multi-base. <code>allOf</code> single-element diunwrap.</li>
  <li>Validator kustom berdasarkan <code>pattern</code> — pancarkan field tapi lewati regex. Tambahkan Pydantic <code>@field_validator</code> setelah tempel jika kamu butuh.</li>
</ul>

<h3>Kesalahan umum</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Output default v2 (saat ini). Ganti via dropdown kalau kamu di codebase v1.</li>
  <li><strong>Output adalah titik awal, bukan kata akhir.</strong> Review. Terutama untuk union kompleks, tipe rekursif, dan apa pun dengan aturan validasi kustom.</li>
  <li><strong>Nama penting.</strong> Field "Root class name" mengontrol nama class top-level. Sub-class dinamai dari property <code>title</code> jika ada, kalau tidak dari field yang berisi mereka.</li>
  <li><strong>Required vs Optional di v2.</strong> Field required tanpa default pakai <code>...</code>. Field optional default ke <code>None</code> dan dibungkus dalam <code>Optional[T]</code>.</li>
</ul>
""",
        "vi": """
<h2>Công cụ này để làm gì?</h2>
<p>JSON Schema và Pydantic là hai cách chính để mô tả một object có cấu trúc — JSON Schema là tiếng nói chung cho OpenAPI specs, LLM function-calling, và structured outputs; Pydantic là library validation dữ liệu de-facto của Python. Khá thường, bạn có cái này và cần cái kia. Công cụ này thực hiện chuyển đổi theo một chiều: dán JSON Schema, nhận class Pydantic có thể thả vào file Python.</p>

<h3>Khi nào nên dùng</h3>
<ul>
  <li><strong>LLM structured outputs.</strong> <code>response_format</code> của OpenAI và tool-use của Anthropic đều nhận JSON Schema. Sau khi thiết kế và test schema, bạn thường muốn một Pydantic model để validate và truy cập field trong Python.</li>
  <li><strong>Sinh client OpenAPI.</strong> Một spec OpenAPI cho bạn JSON Schema cho mỗi request/response body; đây là cách nhanh để có Pydantic model tương ứng mà không cần generator full.</li>
  <li><strong>Migrate Schema → Python.</strong> Bạn kế thừa một hệ thống dùng JSON Schema để validate và muốn chuyển sang Pydantic. Dán từng schema, nhận class khởi điểm, tinh chỉnh.</li>
  <li><strong>Scaffolding nhanh.</strong> Bạn phác thảo cấu trúc trong JSON trong notebook; biến nó thành model thật với một lần dán.</li>
</ul>

<h3>Công cụ này xử lý gì</h3>
<ul>
  <li><code>type: object</code> với <code>properties</code> + <code>required</code> — sinh class BaseModel lồng nhau.</li>
  <li><code>type: array</code> với <code>items</code> — sinh <code>List[X]</code>.</li>
  <li>Primitives: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> trên string: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; biến null gập vào <code>Optional[...]</code>.</li>
  <li>Mảng <code>type: ["string", "null"]</code> → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → default của field.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Tên field không phải identifier Python hợp lệ được sanitize, và <code>alias=</code> được thêm để giữ tên gốc trên wire. Model được cấu hình với <code>populate_by_name=True</code>.</li>
  <li>Object lồng nhau trở thành class lồng nhau, đặt tên theo field nếu không có <code>title</code>.</li>
  <li><code>$defs</code> / <code>definitions</code> được phát ra như class riêng.</li>
</ul>

<h3>Công cụ này chưa xử lý</h3>
<ul>
  <li><code>$ref</code> ngoài (URL refs). Refs cục bộ đến <code>$defs</code> hoạt động.</li>
  <li><code>patternProperties</code>. Phát comment TODO.</li>
  <li><code>additionalProperties</code> với schema. Phát comment TODO.</li>
  <li>Tổ hợp <code>allOf</code> multi-base. <code>allOf</code> single-element được unwrap.</li>
  <li>Validator tùy chỉnh dựa trên <code>pattern</code> — phát field nhưng bỏ qua regex. Thêm Pydantic <code>@field_validator</code> sau khi dán nếu bạn cần.</li>
</ul>

<h3>Lưu ý thường gặp</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Output mặc định là v2 (hiện tại). Chuyển qua dropdown nếu bạn ở codebase v1.</li>
  <li><strong>Output là điểm khởi đầu, không phải lời cuối.</strong> Review nó. Đặc biệt với union phức tạp, kiểu đệ quy, và bất cứ thứ gì với rule validation tùy chỉnh.</li>
  <li><strong>Tên quan trọng.</strong> Field "Root class name" kiểm soát tên class top-level. Sub-classes được đặt tên theo property <code>title</code> nếu có, ngược lại theo field chứa chúng.</li>
  <li><strong>Required vs Optional ở v2.</strong> Field required không có default dùng <code>...</code>. Field optional mặc định <code>None</code> và được bọc trong <code>Optional[T]</code>.</li>
</ul>
""",
        "hi": """
<h2>यह किसके लिए है?</h2>
<p>JSON Schema और Pydantic structured object describe करने के दो मुख्य तरीके हैं — JSON Schema OpenAPI specs, LLM function-calling, और structured outputs के लिए lingua-franca है; Pydantic Python की de-facto data-validation library है। काफ़ी अक्सर आपके पास एक होता है और आपको दूसरा चाहिए। यह tool एक direction में conversion करता है: एक JSON Schema paste करें, एक Pydantic class पाएं जिसे आप Python file में drop कर सकते हैं।</p>

<h3>कब इस्तेमाल करें</h3>
<ul>
  <li><strong>LLM structured outputs।</strong> OpenAI का <code>response_format</code> और Anthropic का tool-use दोनों JSON Schema लेते हैं। Schema design और test करने के बाद, आप आमतौर पर Python में fields को validate और access करने के लिए Pydantic model चाहते हैं।</li>
  <li><strong>OpenAPI client generation।</strong> एक OpenAPI spec आपको हर request/response body के लिए JSON Schema देती है; full code-generator pull किए बिना matching Pydantic models पाने का यह एक तेज़ तरीका है।</li>
  <li><strong>Schema → Python migrations।</strong> आपको एक system inherit मिला है जो validation के लिए JSON Schema उपयोग करता है और आप इसे Pydantic में move करना चाहते हैं। हर schema paste करें, starting class पाएं, refine करें।</li>
  <li><strong>Quick scaffolding।</strong> आपने notebook में JSON में shape sketch की; एक paste से इसे real model में बदलें।</li>
</ul>

<h3>यह tool क्या handle करता है</h3>
<ul>
  <li><code>type: object</code> with <code>properties</code> + <code>required</code> — एक nested BaseModel class generate करता है।</li>
  <li><code>type: array</code> with <code>items</code> — <code>List[X]</code> generate करता है।</li>
  <li>Primitives: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>।</li>
  <li>String पर <code>format</code>: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>।</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>।</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; एक null variant <code>Optional[...]</code> में folds हो जाता है।</li>
  <li><code>type: ["string", "null"]</code> arrays → <code>Optional[str]</code>।</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>।</li>
  <li><code>default</code> → field default।</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>।</li>
  <li>ऐसे field names जो valid Python identifiers नहीं हैं (hyphens, leading digits, keywords) sanitised होते हैं, और original wire name preserve करने के लिए एक <code>alias=</code> added जाता है। Model <code>populate_by_name=True</code> के साथ configured है।</li>
  <li>Nested objects nested classes बनते हैं, यदि कोई <code>title</code> provided नहीं तो field के नाम पर।</li>
  <li><code>$defs</code> / <code>definitions</code> separate classes के रूप में emitted होते हैं।</li>
</ul>

<h3>यह tool क्या handle नहीं करता (अभी)</h3>
<ul>
  <li>External <code>$ref</code> (URL refs)। <code>$defs</code> के लिए local refs काम करते हैं।</li>
  <li><code>patternProperties</code>। एक TODO comment emit करता है।</li>
  <li>Schema के साथ <code>additionalProperties</code>। एक TODO comment emit करता है।</li>
  <li>Multi-base <code>allOf</code> composition। Single-element <code>allOf</code> unwrapped होता है।</li>
  <li><code>pattern</code> पर आधारित custom validators — field emit करता है पर regex skip करता है। यदि आवश्यक हो तो paste करने के बाद एक Pydantic <code>@field_validator</code> add करें।</li>
</ul>

<h3>आम गलतियाँ</h3>
<ul>
  <li><strong>Pydantic v2 vs v1।</strong> Default output v2 (current) है। यदि v1 codebase पर हैं तो dropdown से switch करें।</li>
  <li><strong>Output एक starting point है, final word नहीं।</strong> इसे review करें। विशेषकर complex unions, recursive types, और custom validation rules के साथ कुछ भी के लिए।</li>
  <li><strong>Names matter करते हैं।</strong> "Root class name" field top-level class name को control करता है। Sub-classes का नाम उनकी <code>title</code> property के बाद होता है यदि present, अन्यथा उस field के बाद जो उन्हें contain करता है।</li>
  <li><strong>v2 में Required vs Optional।</strong> बिना default वाला एक required field <code>...</code> उपयोग करता है। Optional fields default <code>None</code> होते हैं और <code>Optional[T]</code> में wrapped होते हैं।</li>
</ul>
""",
        "sk": """
<h2>Načo to slúži?</h2>
<p>JSON Schema a Pydantic sú dva hlavné spôsoby, ako popísať štruktúrovaný objekt — JSON Schema je lingua franca pre OpenAPI špecifikácie, LLM function-calling a štruktúrované výstupy; Pydantic je de-facto knižnica na validáciu dát v Pythone. Pomerne často máš jedno a potrebuješ druhé. Tento nástroj robí konverziu jedným smerom: vlož JSON Schema, dostaneš triedu Pydantic, ktorú môžeš vložiť do Python súboru.</p>

<h3>Kedy to použiť</h3>
<ul>
  <li><strong>Štruktúrované výstupy LLM.</strong> OpenAI <code>response_format</code> aj Anthropic tool-use prijímajú JSON Schema. Po návrhu a otestovaní schémy zvyčajne chceš Pydantic model na validáciu a prístup k poliam v Pythone.</li>
  <li><strong>Generovanie OpenAPI klienta.</strong> OpenAPI špecifikácia ti dáva JSON Schema pre každé telo požiadavky/odpovede; toto je rýchly spôsob, ako získať zodpovedajúce Pydantic modely bez sťahovania plného code generátora.</li>
  <li><strong>Migrácia Schema → Python.</strong> Zdedil si systém, ktorý používa JSON Schema na validáciu a chceš ho presunúť na Pydantic. Vlož každú schému, dostaneš počiatočnú triedu, dolaď.</li>
  <li><strong>Rýchle scaffolding.</strong> Tvar si načrtol v JSONe v notebooku; premeň ho na skutočný model jedným vložením.</li>
</ul>

<h3>Čo nástroj zvláda</h3>
<ul>
  <li><code>type: object</code> s <code>properties</code> + <code>required</code> — generuje vnorenú triedu BaseModel.</li>
  <li><code>type: array</code> s <code>items</code> — generuje <code>List[X]</code>.</li>
  <li>Primitívy: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> na stringu: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; null varianta sa zloží do <code>Optional[...]</code>.</li>
  <li><code>type: ["string", "null"]</code> polia → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → predvolená hodnota poľa.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Názvy polí, ktoré nie sú platné Python identifikátory, sa sanitizujú a pridáva sa <code>alias=</code> na zachovanie pôvodného mena. Model je konfigurovaný s <code>populate_by_name=True</code>.</li>
  <li>Vnorené objekty sa stávajú vnorenými triedami, pomenovanými podľa poľa, ak nie je poskytnutý <code>title</code>.</li>
  <li><code>$defs</code> / <code>definitions</code> sa emitujú ako samostatné triedy.</li>
</ul>

<h3>Čo zatiaľ nezvláda</h3>
<ul>
  <li>Externé <code>$ref</code> (URL refs). Lokálne refs na <code>$defs</code> fungujú.</li>
  <li><code>patternProperties</code>. Emituje TODO komentár.</li>
  <li><code>additionalProperties</code> so schémou. Emituje TODO komentár.</li>
  <li>Viacbázová <code>allOf</code> kompozícia. Jednoprvkové <code>allOf</code> je rozbalené.</li>
  <li>Vlastné validátory na <code>pattern</code> — pole vypustí, regex preskočí. Po vložení pridaj <code>@field_validator</code>, ak potrebuješ.</li>
</ul>

<h3>Časté pasce</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Predvolený výstup je v2 (aktuálny). Prepni cez dropdown, ak si na v1 kódbáze.</li>
  <li><strong>Výstup je východiskový bod, nie posledné slovo.</strong> Skontroluj. Najmä pre zložité únie, rekurzívne typy a čokoľvek s vlastnými validačnými pravidlami.</li>
  <li><strong>Mená sú dôležité.</strong> Pole „Root class name" kontroluje názov triedy najvyššej úrovne. Podtriedy sú pomenované podľa ich <code>title</code> vlastnosti, ak je prítomná, inak podľa poľa, ktoré ich obsahuje.</li>
  <li><strong>Required vs Optional vo v2.</strong> Povinné pole bez defaultu používa <code>...</code>. Optional polia majú default <code>None</code> a sú zabalené v <code>Optional[T]</code>.</li>
</ul>
""",
        "cs": """
<h2>K čemu to slouží?</h2>
<p>JSON Schema a Pydantic jsou dva hlavní způsoby, jak popsat strukturovaný objekt — JSON Schema je lingua franca pro OpenAPI specifikace, LLM function-calling a strukturované výstupy; Pydantic je de-facto knihovna na validaci dat v Pythonu. Docela často máš jedno a potřebuješ druhé. Tento nástroj dělá konverzi jedním směrem: vlož JSON Schema, dostaneš třídu Pydantic, kterou můžeš vložit do Python souboru.</p>

<h3>Kdy to použít</h3>
<ul>
  <li><strong>Strukturované výstupy LLM.</strong> OpenAI <code>response_format</code> i Anthropic tool-use přijímají JSON Schema. Po návrhu a otestování schématu obvykle chceš Pydantic model na validaci a přístup k polím v Pythonu.</li>
  <li><strong>Generování OpenAPI klienta.</strong> OpenAPI specifikace ti dává JSON Schema pro každé tělo požadavku/odpovědi; tohle je rychlý způsob, jak získat odpovídající Pydantic modely bez stahování plného code generátoru.</li>
  <li><strong>Migrace Schema → Python.</strong> Zdědil jsi systém, který používá JSON Schema na validaci a chceš ho přesunout na Pydantic. Vlož každé schéma, dostaneš počáteční třídu, dolaď.</li>
  <li><strong>Rychlé scaffolding.</strong> Tvar jsi načrtl v JSONu v notebooku; přeměň ho na skutečný model jedním vložením.</li>
</ul>

<h3>Co nástroj zvládá</h3>
<ul>
  <li><code>type: object</code> s <code>properties</code> + <code>required</code> — generuje vnořenou třídu BaseModel.</li>
  <li><code>type: array</code> s <code>items</code> — generuje <code>List[X]</code>.</li>
  <li>Primitivy: <code>string</code> → <code>str</code>, <code>integer</code> → <code>int</code>, <code>number</code> → <code>float</code>, <code>boolean</code> → <code>bool</code>, <code>null</code> → <code>None</code>.</li>
  <li><code>format</code> na stringu: <code>date-time</code> → <code>datetime</code>, <code>email</code> → <code>EmailStr</code>, <code>uri/url</code> → <code>HttpUrl</code>, <code>uuid</code> → <code>UUID</code>.</li>
  <li><code>enum</code> → <code>Literal[...]</code>; <code>const</code> → <code>Literal[X]</code>.</li>
  <li><code>oneOf</code>/<code>anyOf</code> → <code>Union[...]</code>; null varianta se složí do <code>Optional[...]</code>.</li>
  <li><code>type: ["string", "null"]</code> pole → <code>Optional[str]</code>.</li>
  <li><code>description</code> → <code>Field(..., description=...)</code>.</li>
  <li><code>default</code> → výchozí hodnota pole.</li>
  <li><code>minimum/maximum/minLength/maxLength</code> → <code>Field(ge=..., le=..., min_length=..., max_length=...)</code>.</li>
  <li>Názvy polí, které nejsou platné Python identifikátory, se sanitizují a přidává se <code>alias=</code> na zachování původního jména. Model je konfigurován s <code>populate_by_name=True</code>.</li>
  <li>Vnořené objekty se stávají vnořenými třídami, pojmenované podle pole, pokud není poskytnutý <code>title</code>.</li>
  <li><code>$defs</code> / <code>definitions</code> se emitují jako samostatné třídy.</li>
</ul>

<h3>Co zatím nezvládá</h3>
<ul>
  <li>Externí <code>$ref</code> (URL refs). Lokální refs na <code>$defs</code> fungují.</li>
  <li><code>patternProperties</code>. Emituje TODO komentář.</li>
  <li><code>additionalProperties</code> se schématem. Emituje TODO komentář.</li>
  <li>Vícebázová <code>allOf</code> kompozice. Jednoprvkové <code>allOf</code> je rozbalené.</li>
  <li>Vlastní validátory na <code>pattern</code> — pole vypustí, regex přeskočí. Po vložení přidej <code>@field_validator</code>, pokud potřebuješ.</li>
</ul>

<h3>Časté pasti</h3>
<ul>
  <li><strong>Pydantic v2 vs v1.</strong> Výchozí výstup je v2 (aktuální). Přepni přes dropdown, pokud jsi na v1 kódové bázi.</li>
  <li><strong>Výstup je výchozí bod, ne poslední slovo.</strong> Zkontroluj. Zvláště pro složité unie, rekurzivní typy a cokoli s vlastními validačními pravidly.</li>
  <li><strong>Jména jsou důležitá.</strong> Pole „Root class name" kontroluje název třídy nejvyšší úrovně. Podtřídy jsou pojmenované podle jejich <code>title</code> vlastnosti, pokud je přítomna, jinak podle pole, které je obsahuje.</li>
  <li><strong>Required vs Optional ve v2.</strong> Povinné pole bez defaultu používá <code>...</code>. Optional pole mají default <code>None</code> a jsou zabalené v <code>Optional[T]</code>.</li>
</ul>
""",
    },
    "related": ["json-formatter", "json-diff", "yaml-json"],
    "howto": {"flow": "transform", "action": "convert", "noun": "JSON"},
}
