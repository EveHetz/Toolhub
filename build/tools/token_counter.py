TOOL = {
    "slug": "token-counter",
    "category": "ai",
    "icon": "🔢",
    "tags": ["token", "tokenizer", "count", "llm", "gpt", "claude", "openai", "anthropic", "context", "ai", "prompt"],
    "i18n": {
        "en": {
            "name": "Token Counter",
            "tagline": "Estimate token counts across GPT-4, Claude, Llama, Gemini, and other LLMs. Paste text, see counts per model side-by-side. Files never leave your browser.",
            "description": "Free in-browser token count estimator for LLMs. Paste any text and see token counts for GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini, and Mistral. Uses model-specific heuristics — accurate to ±5% for English, ±10% for code and CJK. Useful for prompt-budget planning, context-window sizing, API-cost estimation.",
        },
        "de": {
            "name": "Token-Zähler",
            "tagline": "Schätze Token-Anzahl für GPT-4, Claude, Llama, Gemini und andere LLMs. Text einfügen, Zählung pro Modell nebeneinander sehen. Dateien verlassen den Browser nicht.",
            "description": "Kostenloser Token-Zähler im Browser für LLMs. Füge beliebigen Text ein und sieh Token-Zahlen für GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini und Mistral. Modellspezifische Heuristiken — ±5 % genau für Englisch, ±10 % für Code und CJK. Nützlich für Prompt-Budget, Kontextfenster und API-Kostenschätzung.",
        },
        "es": {
            "name": "Contador de Tokens",
            "tagline": "Estima tokens para GPT-4, Claude, Llama, Gemini y otros LLMs. Pega texto y mira el conteo por modelo lado a lado. Los archivos no salen del navegador.",
            "description": "Contador de tokens gratuito en el navegador para LLMs. Pega cualquier texto y mira conteos para GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini y Mistral. Heurísticas por modelo — precisión ±5% en inglés, ±10% en código y CJK. Útil para planificar presupuesto de prompts, dimensionar contexto y estimar coste de API.",
        },
        "fr": {
            "name": "Compteur de Tokens",
            "tagline": "Estimez les tokens pour GPT-4, Claude, Llama, Gemini et d'autres LLM. Collez du texte, voyez le compte par modèle côte à côte. Les fichiers ne quittent jamais votre navigateur.",
            "description": "Compteur de tokens gratuit dans le navigateur pour LLMs. Collez n'importe quel texte et voyez le nombre de tokens pour GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini et Mistral. Heuristiques par modèle — précision ±5% en anglais, ±10% pour le code et le CJK. Utile pour planifier un budget prompt, dimensionner une fenêtre de contexte, estimer le coût d'une API.",
        },
        "it": {
            "name": "Contatore di Token",
            "tagline": "Stima i token per GPT-4, Claude, Llama, Gemini e altri LLM. Incolla il testo, vedi i conteggi per modello affiancati. I file non lasciano mai il tuo browser.",
            "description": "Contatore di token gratuito nel browser per LLM. Incolla qualsiasi testo e vedi il conteggio per GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini e Mistral. Euristiche per modello — precisione ±5% in inglese, ±10% per codice e CJK. Utile per pianificare un budget prompt, dimensionare la finestra di contesto, stimare il costo API.",
        },
        "pt": {
            "name": "Contador de Tokens",
            "tagline": "Estima a contagem de tokens em GPT-4, Claude, Llama, Gemini e outros LLMs. Cole o texto e veja a contagem por modelo lado a lado. Os arquivos nunca saem do seu navegador.",
            "description": "Estimador grátis de tokens no navegador para LLMs. Cole qualquer texto e veja a contagem para GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini e Mistral. Heurísticas por modelo — precisão de ±5% em inglês, ±10% em código e CJK. Útil para planejar o orçamento do prompt, dimensionar a janela de contexto e estimar o custo da API.",
        },
        "pl": {
            "name": "Licznik Tokenów",
            "tagline": "Oszacuj liczbę tokenów dla GPT-4, Claude, Llama, Gemini i innych LLM-ów. Wklej tekst, zobacz wynik per model obok siebie. Pliki nigdy nie opuszczają przeglądarki.",
            "description": "Darmowy licznik tokenów w przeglądarce dla LLM-ów. Wklej dowolny tekst i zobacz liczbę tokenów dla GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini i Mistrala. Heurystyki per model — dokładność ±5% dla angielskiego, ±10% dla kodu i CJK. Przydatne do planowania budżetu promptu, dobierania okna kontekstu i szacowania kosztów API.",
        },
        "ja": {
            "name": "トークンカウンター",
            "tagline": "GPT-4・Claude・Llama・Gemini など各種 LLM のトークン数を推定。テキストを貼ると、モデルごとの数値を並べて表示。ファイルはブラウザから出ません。",
            "description": "ブラウザ完結の LLM 用トークン数推定ツール（無料）。任意のテキストを貼り付けると、GPT-4、GPT-3.5、Claude（Sonnet / Opus / Haiku）、Llama、Gemini、Mistral のトークン数を表示します。モデル別ヒューリスティックを使用。英語で ±5%、コードや CJK で ±10% 程度の精度。プロンプト予算計画、コンテキスト窓のサイジング、API コスト見積りに便利です。",
        },
        "nl": {
            "name": "Token Counter",
            "tagline": "Schat tokens voor GPT-4, Claude, Llama, Gemini en andere LLM's. Plak tekst, zie tellingen per model naast elkaar. Files verlaten je browser nooit.",
            "description": "Gratis in-browser token-teller voor LLM's. Plak willekeurige tekst en zie token-tellingen voor GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini en Mistral. Per-model heuristieken — ±5% nauwkeurig voor Engels, ±10% voor code en CJK. Handig voor prompt-budget plannen, context-window sizing en API-kostraming.",
        },
        "tr": {
            "name": "Token Sayacı",
            "tagline": "GPT-4, Claude, Llama, Gemini ve diğer LLM'ler için token sayısını tahmin et. Metni yapıştır, model başına sayıları yan yana gör. Dosyalar tarayıcını asla terk etmez.",
            "description": "LLM'ler için ücretsiz tarayıcı içi token sayacı. Herhangi bir metni yapıştır ve GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini ve Mistral için token sayısını gör. Modele özgü sezgisel yöntemler — İngilizce için ±%5, kod ve CJK için ±%10 doğruluk. Prompt bütçesi planlama, bağlam penceresi boyutlandırma ve API maliyet tahmini için yararlıdır.",
        },
        "id": {
            "name": "Penghitung Token",
            "tagline": "Estimasi jumlah token untuk GPT-4, Claude, Llama, Gemini dan LLM lainnya. Tempel teks, lihat jumlah per model berdampingan. File tidak pernah meninggalkan browser-mu.",
            "description": "Penghitung token gratis di browser untuk LLM. Tempel teks apa pun dan lihat jumlah token untuk GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini, dan Mistral. Heuristik per model — akurat ±5% untuk bahasa Inggris, ±10% untuk kode dan CJK. Berguna untuk merencanakan budget prompt, mengukur context window, dan memperkirakan biaya API.",
        },
        "vi": {
            "name": "Đếm Token",
            "tagline": "Ước tính số token cho GPT-4, Claude, Llama, Gemini và các LLM khác. Dán văn bản, xem số đếm theo từng model cạnh nhau. File không bao giờ rời khỏi trình duyệt của bạn.",
            "description": "Công cụ ước tính token miễn phí trong trình duyệt cho LLM. Dán bất kỳ văn bản nào và xem số token cho GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini và Mistral. Heuristic riêng cho từng model — chính xác ±5% với tiếng Anh, ±10% với mã nguồn và CJK. Hữu ích để lên kế hoạch ngân sách prompt, ước lượng cửa sổ ngữ cảnh, và ước tính chi phí API.",
        },
        "hi": {
            "name": "Token Counter",
            "tagline": "GPT-4, Claude, Llama, Gemini और अन्य LLMs के लिए token गणना अनुमान। Text paste करें, model-wise counts साथ-साथ देखें। Files आपके browser से बाहर नहीं जातीं।",
            "description": "LLMs के लिए मुफ़्त in-browser token count estimator। कोई भी text paste करें और GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini, और Mistral के लिए token counts देखें। Model-specific heuristics — English के लिए ±5% सटीक, code और CJK के लिए ±10%। Prompt-budget planning, context-window sizing, API-cost अनुमान के लिए उपयोगी।",
        },
        "sk": {
            "name": "Počítadlo tokenov",
            "tagline": "Odhadni počet tokenov pre GPT-4, Claude, Llama, Gemini a ďalšie LLM. Vlož text, uvidíš počty per model vedľa seba. Súbory nikdy neopustia tvoj prehliadač.",
            "description": "Bezplatný in-browser odhadovač počtu tokenov pre LLM. Vlož ľubovoľný text a uvidíš počty tokenov pre GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini a Mistral. Heuristiky per model — presnosť ±5% pre angličtinu, ±10% pre kód a CJK. Užitočné pri plánovaní rozpočtu promptu, dimenzovaní kontextového okna a odhade nákladov na API.",
        },
        "cs": {
            "name": "Počítadlo tokenů",
            "tagline": "Odhadni počet tokenů pro GPT-4, Claude, Llama, Gemini a další LLM. Vlož text, uvidíš počty per model vedle sebe. Soubory nikdy neopustí tvůj prohlížeč.",
            "description": "Bezplatný in-browser odhad počtu tokenů pro LLM. Vlož libovolný text a uvidíš počty tokenů pro GPT-4, GPT-3.5, Claude (Sonnet / Opus / Haiku), Llama, Gemini a Mistral. Heuristiky per model — přesnost ±5 % pro angličtinu, ±10 % pro kód a CJK. Užitečné při plánování rozpočtu promptu, dimenzování kontextového okna a odhadu nákladů na API.",
        },
    },
    "body": """
<div class="tool-card">
  <label for="tc-input">{LBL_INPUT}</label>
  <textarea id="tc-input" oninput="tcRun()" spellcheck="false" placeholder="Paste any text here — a prompt, a chunk of code, a JSON payload…"></textarea>
  <div class="meta" id="tc-stats" style="margin-top:0.5rem"></div>
</div>
<div class="tool-card">
  <label>Token counts per model</label>
  <div class="tc-table-wrap">
    <table class="tc-table" id="tc-table">
      <thead>
        <tr><th>Model</th><th>Tokens</th><th>Input $/1M</th><th>Cost (this text)</th></tr>
      </thead>
      <tbody id="tc-tbody"></tbody>
    </table>
  </div>
  <div class="button-row" style="margin-top:0.6rem">
    <button class="secondary" onclick="tcCopyJson(this)">Copy as JSON</button>
  </div>
  <div class="meta" style="margin-top:0.6rem">Estimates only. Real tokenizers (tiktoken, SentencePiece, etc.) may differ by ±10%. For exact counts, run the model's tokenizer locally.</div>
</div>
""",
    "script": """
<style>
.tc-table-wrap{overflow:auto;border:1px solid var(--border);border-radius:8px}
.tc-table{width:100%;border-collapse:collapse;font-size:0.9rem}
.tc-table th,.tc-table td{padding:0.5rem 0.75rem;text-align:left;border-bottom:1px solid var(--border);font-family:ui-monospace,monospace}
.tc-table th{background:var(--bg);font-size:0.78rem;text-transform:uppercase;letter-spacing:0.04em;color:var(--text-muted);font-family:inherit}
.tc-table tbody tr:last-child td{border-bottom:none}
.tc-table td.num{text-align:right;font-variant-numeric:tabular-nums}
.tc-name{font-weight:600}
.tc-cost{color:var(--accent)}
.tc-stat{display:inline-block;margin-right:1rem}
.tc-stat strong{color:var(--text)}
</style>
<script>
const TC_MODELS = [
  {id:'gpt-4o',            label:'GPT-4o',            divisor:3.8, codeFactor:0.3, costPerM:2.50},
  {id:'gpt-3.5-turbo',     label:'GPT-3.5 Turbo',     divisor:3.8, codeFactor:0.3, costPerM:0.50},
  {id:'claude-3.5-sonnet', label:'Claude 3.5 Sonnet', divisor:3.6, codeFactor:0.3, costPerM:3.00},
  {id:'claude-3.5-haiku',  label:'Claude 3.5 Haiku',  divisor:3.6, codeFactor:0.3, costPerM:0.80},
  {id:'claude-3-opus',     label:'Claude 3 Opus',     divisor:3.6, codeFactor:0.3, costPerM:15.00},
  {id:'llama-3',           label:'Llama 3',           divisor:3.7, codeFactor:0.3, costPerM:0},
  {id:'gemini-1.5-pro',    label:'Gemini 1.5 Pro',    divisor:4.0, codeFactor:0.25, costPerM:1.25},
  {id:'mistral-large',     label:'Mistral Large',     divisor:3.5, codeFactor:0.3, costPerM:2.00},
];
const TC_CJK_RE = /[぀-ヿ㐀-䶿一-鿿가-힯]/g;
const TC_CODE_RE = /[{}()\\[\\];:.<>=]/g;
function tcEstimate(text, m){
  if(!text) return 0;
  const len = text.length;
  const cjk = (text.match(TC_CJK_RE) || []).length;
  const code = (text.match(TC_CODE_RE) || []).length;
  const en = Math.max(0, len - cjk);
  return Math.ceil(en / m.divisor + cjk + code * m.codeFactor);
}
function tcFmtNum(n){
  if(n >= 1000) return n.toLocaleString();
  return String(n);
}
function tcFmtCost(usd){
  if(usd === 0) return 'free / self-host';
  if(usd < 0.000001) return '$0.000000';
  if(usd < 0.01) return '$' + usd.toFixed(6);
  if(usd < 1) return '$' + usd.toFixed(4);
  return '$' + usd.toFixed(2);
}
function tcRun(){
  const text = document.getElementById('tc-input').value;
  const tbody = document.getElementById('tc-tbody');
  const rows = TC_MODELS.map(m => {
    const tokens = tcEstimate(text, m);
    const cost = (tokens / 1_000_000) * m.costPerM;
    return `<tr><td class="tc-name">${m.label}</td><td class="num">${tcFmtNum(tokens)}</td><td class="num">${m.costPerM === 0 ? '—' : '$' + m.costPerM.toFixed(2)}</td><td class="num tc-cost">${tcFmtCost(cost)}</td></tr>`;
  });
  tbody.innerHTML = rows.join('');
  // Stats
  const chars = text.length;
  const words = text.trim() ? text.trim().split(/\\s+/).length : 0;
  const lines = text === '' ? 0 : text.split('\\n').length;
  const avg = TC_MODELS.reduce((s, m) => s + tcEstimate(text, m), 0) / TC_MODELS.length;
  document.getElementById('tc-stats').innerHTML =
    `<span class="tc-stat"><strong>${tcFmtNum(chars)}</strong> chars</span>` +
    `<span class="tc-stat"><strong>${tcFmtNum(words)}</strong> words</span>` +
    `<span class="tc-stat"><strong>${tcFmtNum(lines)}</strong> lines</span>` +
    `<span class="tc-stat">~<strong>${tcFmtNum(Math.round(avg))}</strong> tokens (avg)</span>`;
}
function tcCopyJson(btn){
  const text = document.getElementById('tc-input').value;
  const obj = {};
  TC_MODELS.forEach(m => {
    const tokens = tcEstimate(text, m);
    obj[m.id] = { tokens: tokens, cost_usd: +((tokens / 1_000_000) * m.costPerM).toFixed(6) };
  });
  const json = JSON.stringify(obj, null, 2);
  navigator.clipboard.writeText(json).then(() => {
    const orig = btn.textContent;
    btn.textContent = '✓ Copied';
    setTimeout(() => btn.textContent = orig, 1400);
  });
}
document.addEventListener('DOMContentLoaded', tcRun);
</script>
""",
    "help": {
        "en": """
<h2>What is this for?</h2>
<p>Every interaction with an LLM is metered in <em>tokens</em> — sub-word units that the model's tokenizer carves from your text. Tokens drive both context-window limits ("does this fit?") and pricing ("how much will this cost?"). The exact count depends on the model's tokenizer, which you usually don't have at hand. This tool gives you a fast estimate for every major model side-by-side, plus the dollar cost for the input text against each model's published per-token price.</p>

<h3>When to use it</h3>
<ul>
  <li>Sizing a system prompt to make sure it (plus expected user input and headroom for output) fits the context window.</li>
  <li>Estimating the API cost of a batch job before you run it — paste 100 representative inputs and multiply.</li>
  <li>Comparing prompt-engineering iterations: did the new prompt actually get shorter, or did you just <em>feel</em> like it did?</li>
  <li>Sanity-checking that a "this is too long" error wasn't caused by hidden whitespace, BOM markers, or copy-paste noise.</li>
</ul>

<h3>Honesty about accuracy</h3>
<ul>
  <li><strong>These are heuristics, not the real tokenizers.</strong> tiktoken (OpenAI), Anthropic's tokenizer, and SentencePiece (Llama, Gemini, Mistral) each carve text differently. For English prose, our estimates land within ±5%. Code, dense JSON, and CJK text drift to ±10% or worse.</li>
  <li><strong>Why we don't ship the real tokenizers.</strong> tiktoken alone is ~1 MB of WASM + data files; loading it just to count tokens would bloat the page tenfold. If you need exact counts (e.g. you're hitting a hard 8k context limit), run <code>tiktoken</code> in Python locally or call the model's <code>/v1/tokenize</code> endpoint.</li>
  <li><strong>What we get right.</strong> Relative ordering (which model uses more tokens for the same text) is reliable. Cost rankings are usually accurate. Order-of-magnitude estimates ("is this 500 or 5000 tokens?") are dead-on.</li>
</ul>

<h3>Pricing notes</h3>
<ul>
  <li>Costs are <em>input</em> prices per 1M tokens as of 2025. Output tokens are typically more expensive — multiply by 3–5× for a worst-case output estimate.</li>
  <li>Llama 3 shows zero cost because the typical deployment is self-hosted. Hosted offerings (Together, Groq, Fireworks) charge $0.20–$1 per 1M depending on size.</li>
  <li>Prices change. Check the provider's pricing page before relying on these numbers for a real budget.</li>
</ul>

<h3>Common gotchas</h3>
<ul>
  <li><strong>Tokens ≠ words.</strong> An English word averages 1.3 tokens; "antidisestablishmentarianism" is roughly 7. Code and structured text tokenise much higher per character.</li>
  <li><strong>CJK text is dense.</strong> Each Chinese / Japanese / Korean character can be its own token, so 1000 chars ≈ 1000 tokens — much more expensive per "character" than English.</li>
  <li><strong>Hidden characters add up.</strong> Pasted text with zero-width joiners, NBSPs, or BOMs gets counted too. Use the Unicode Inspector tool if your token count looks suspiciously high.</li>
  <li><strong>System + user + assistant tokens compound.</strong> The "context window" budget includes every message in the conversation. Don't size your input against the raw limit; leave 30–50% headroom for replies.</li>
</ul>
""",
        "de": """
<h2>Wozu ist das gut?</h2>
<p>Jede Interaktion mit einem LLM wird in <em>Tokens</em> abgerechnet — Unter-Wort-Einheiten, in die der Tokenizer deinen Text zerlegt. Tokens bestimmen sowohl Kontextfenster-Limits ("passt das rein?") als auch Preise ("was kostet das?"). Die exakte Zahl hängt vom Tokenizer des Modells ab, den du selten zur Hand hast. Dieses Tool gibt dir eine schnelle Schätzung für jedes wichtige Modell nebeneinander, plus die Kosten in Dollar nach offiziellen Preisen.</p>

<h3>Wann nützlich</h3>
<ul>
  <li>Größenabschätzung eines System-Prompts — passt er (plus User-Eingabe plus Output-Puffer) ins Kontextfenster?</li>
  <li>API-Kosten eines Batch-Jobs schätzen, bevor du ihn startest — 100 repräsentative Inputs einfügen und multiplizieren.</li>
  <li>Prompt-Iterationen vergleichen: ist der neue Prompt wirklich kürzer, oder fühlt es sich nur so an?</li>
  <li>Sanity-Check, wenn ein "zu lang"-Fehler kommt — unsichtbares Whitespace, BOM, Copy-Paste-Müll?</li>
</ul>

<h3>Ehrlich zur Genauigkeit</h3>
<ul>
  <li><strong>Das sind Heuristiken, nicht die echten Tokenizer.</strong> tiktoken (OpenAI), Anthropics Tokenizer, SentencePiece (Llama, Gemini, Mistral) zerlegen unterschiedlich. Für englischen Fließtext liegen wir bei ±5%. Code, dichtes JSON und CJK driften auf ±10% oder schlechter.</li>
  <li><strong>Warum nicht die echten Tokenizer mitliefern?</strong> tiktoken allein ist ~1 MB WASM + Daten — würde die Seite um Faktor 10 aufblähen. Für exakte Zahlen führe <code>tiktoken</code> in Python lokal aus oder nutze den <code>/v1/tokenize</code>-Endpoint.</li>
  <li><strong>Was wir korrekt liefern.</strong> Relative Reihenfolge (welches Modell braucht mehr Tokens für denselben Text) ist verlässlich. Kostenrangfolge meist auch. Größenordnungsschätzungen sind treffsicher.</li>
</ul>

<h3>Preishinweise</h3>
<ul>
  <li>Preise sind <em>Input</em>-Preise pro 1M Tokens (Stand 2025). Output ist meist teurer — für eine Worst-Case-Schätzung mal 3–5.</li>
  <li>Llama 3 zeigt Kosten von 0, weil es typischerweise selbst gehostet wird. Hosted-Anbieter (Together, Groq, Fireworks) berechnen $0.20–$1 pro 1M je nach Größe.</li>
  <li>Preise ändern sich. Vor einem echten Budget die Preisseite des Anbieters checken.</li>
</ul>

<h3>Typische Stolperfallen</h3>
<ul>
  <li><strong>Tokens ≠ Wörter.</strong> Ein englisches Wort sind im Schnitt 1.3 Tokens; "Donaudampfschifffahrtsgesellschaft" sind eher 8–10. Code und strukturierter Text tokenisieren viel dichter.</li>
  <li><strong>CJK ist dicht.</strong> Jedes chinesische / japanische / koreanische Zeichen kann ein Token sein, also 1000 Zeichen ≈ 1000 Tokens — pro "Zeichen" deutlich teurer als Englisch.</li>
  <li><strong>Versteckte Zeichen summieren sich.</strong> Eingefügter Text mit Zero-Width-Joinern, NBSPs oder BOM wird mitgezählt. Bei verdächtig hohen Werten den Unicode Inspector nutzen.</li>
  <li><strong>System + User + Assistant addieren sich.</strong> Das Kontextfenster umfasst jede Nachricht. Nicht gegen das Roh-Limit dimensionieren — 30–50% Puffer für Antworten lassen.</li>
</ul>
""",
        "es": """
<h2>¿Para qué sirve?</h2>
<p>Cada interacción con un LLM se cobra en <em>tokens</em> — sub-unidades de palabra que el tokenizador del modelo extrae de tu texto. Los tokens determinan tanto los límites de la ventana de contexto ("¿cabe esto?") como el precio ("¿cuánto costará?"). El recuento exacto depende del tokenizador del modelo, que normalmente no tienes a mano. Esta herramienta te da una estimación rápida para todos los modelos principales lado a lado, más el coste en dólares según los precios publicados.</p>

<h3>Cuándo usarlo</h3>
<ul>
  <li>Dimensionar un system prompt para asegurar que (más la entrada del usuario y margen para la respuesta) cabe en la ventana de contexto.</li>
  <li>Estimar el coste de API de un trabajo en lote antes de lanzarlo — pega 100 entradas representativas y multiplica.</li>
  <li>Comparar iteraciones de prompt: ¿el nuevo prompt es realmente más corto o solo lo parecía?</li>
  <li>Comprobación rápida cuando un error de "demasiado largo" pueda deberse a espacios ocultos, BOM o ruido al pegar.</li>
</ul>

<h3>Honestidad sobre la precisión</h3>
<ul>
  <li><strong>Son heurísticas, no los tokenizadores reales.</strong> tiktoken (OpenAI), el tokenizador de Anthropic y SentencePiece (Llama, Gemini, Mistral) cortan el texto distinto. Para prosa inglesa, estamos dentro de ±5%. Código, JSON denso y CJK derivan a ±10% o peor.</li>
  <li><strong>Por qué no enviamos los tokenizadores reales.</strong> tiktoken solo son ~1 MB de WASM + datos; cargarlo solo para contar tokens inflaría la página diez veces. Para conteos exactos, ejecuta <code>tiktoken</code> en Python localmente o llama al endpoint <code>/v1/tokenize</code>.</li>
  <li><strong>Lo que sí acertamos.</strong> El orden relativo (qué modelo usa más tokens para el mismo texto) es fiable. El ranking de coste suele ser correcto. Las magnitudes ("¿son 500 o 5000 tokens?") son exactas.</li>
</ul>

<h3>Notas sobre los precios</h3>
<ul>
  <li>Los costes son precios de <em>entrada</em> por 1M de tokens en 2025. Los tokens de salida suelen ser más caros — multiplica por 3–5 para una estimación pesimista.</li>
  <li>Llama 3 muestra coste cero porque el despliegue habitual es auto-alojado. Las ofertas alojadas (Together, Groq, Fireworks) cobran $0.20–$1 por 1M según el tamaño.</li>
  <li>Los precios cambian. Consulta la página de precios del proveedor antes de fiarte de estos números para un presupuesto real.</li>
</ul>

<h3>Errores comunes</h3>
<ul>
  <li><strong>Tokens ≠ palabras.</strong> Una palabra en inglés son ~1.3 tokens; "antidisestablishmentarianism" son ~7. El código y el texto estructurado tokenizan mucho más alto por carácter.</li>
  <li><strong>El texto CJK es denso.</strong> Cada carácter chino / japonés / coreano puede ser un token, así que 1000 caracteres ≈ 1000 tokens — bastante más caro por "carácter" que en inglés.</li>
  <li><strong>Los caracteres ocultos suman.</strong> Texto pegado con zero-width joiners, NBSPs o BOM se cuenta también. Usa el Unicode Inspector si tu recuento parece sospechosamente alto.</li>
  <li><strong>Sistema + usuario + asistente se acumulan.</strong> El presupuesto de la ventana de contexto incluye cada mensaje de la conversación. No dimensiones tu entrada contra el límite bruto; deja un 30–50% de margen para respuestas.</li>
</ul>
""",
        "fr": """
<h2>À quoi ça sert ?</h2>
<p>Chaque interaction avec un LLM est facturée en <em>tokens</em> — des sous-unités de mots que le tokenizer du modèle découpe dans votre texte. Les tokens déterminent à la fois les limites de la fenêtre de contexte ("est-ce que ça rentre ?") et le prix ("combien ça va coûter ?"). Le décompte exact dépend du tokenizer du modèle, que vous n'avez généralement pas sous la main. Cet outil donne une estimation rapide pour chaque modèle majeur côte à côte, plus le coût en dollars selon les prix publiés.</p>

<h3>Quand l'utiliser</h3>
<ul>
  <li>Dimensionner un system prompt pour s'assurer qu'il (plus l'entrée utilisateur et la marge pour la sortie) tient dans la fenêtre de contexte.</li>
  <li>Estimer le coût API d'un job batch avant de le lancer — collez 100 entrées représentatives et multipliez.</li>
  <li>Comparer des itérations de prompt : le nouveau prompt est-il vraiment plus court, ou en aviez-vous juste l'impression ?</li>
  <li>Vérification de bon sens quand une erreur "trop long" pourrait venir d'espaces invisibles, de BOM ou de bruit au copier-coller.</li>
</ul>

<h3>Honnêteté sur la précision</h3>
<ul>
  <li><strong>Ce sont des heuristiques, pas les vrais tokenizers.</strong> tiktoken (OpenAI), le tokenizer d'Anthropic et SentencePiece (Llama, Gemini, Mistral) découpent différemment. Pour la prose anglaise, on est à ±5%. Code, JSON dense et CJK dérivent à ±10% ou plus.</li>
  <li><strong>Pourquoi on n'embarque pas les vrais tokenizers.</strong> tiktoken seul, c'est ~1 Mo de WASM + données ; le charger juste pour compter ferait gonfler la page d'un facteur 10. Pour des comptes exacts, lancez <code>tiktoken</code> en Python localement ou appelez l'endpoint <code>/v1/tokenize</code>.</li>
  <li><strong>Ce qu'on a juste.</strong> L'ordre relatif (quel modèle utilise plus de tokens pour le même texte) est fiable. Le classement de coût l'est généralement aussi. Les ordres de grandeur ("c'est 500 ou 5000 tokens ?") sont précis.</li>
</ul>

<h3>Notes sur les prix</h3>
<ul>
  <li>Les coûts sont des prix <em>d'entrée</em> par 1M de tokens en 2025. Les tokens de sortie sont généralement plus chers — multipliez par 3–5 pour une estimation pessimiste.</li>
  <li>Llama 3 affiche un coût nul parce que le déploiement typique est auto-hébergé. Les offres hébergées (Together, Groq, Fireworks) facturent $0.20–$1 par 1M selon la taille.</li>
  <li>Les prix changent. Vérifiez la page de prix du fournisseur avant de vous fier à ces chiffres pour un vrai budget.</li>
</ul>

<h3>Pièges courants</h3>
<ul>
  <li><strong>Tokens ≠ mots.</strong> Un mot anglais fait en moyenne 1.3 token ; "anticonstitutionnellement" fait environ 7–8. Le code et le texte structuré tokenisent beaucoup plus dense par caractère.</li>
  <li><strong>Le CJK est dense.</strong> Chaque caractère chinois / japonais / coréen peut être son propre token, donc 1000 caractères ≈ 1000 tokens — bien plus cher par "caractère" qu'en anglais.</li>
  <li><strong>Les caractères cachés s'accumulent.</strong> Du texte collé avec des zero-width joiners, NBSP ou BOM est compté aussi. Utilisez l'Unicode Inspector si votre compte semble suspicieusement élevé.</li>
  <li><strong>Système + utilisateur + assistant s'additionnent.</strong> Le budget de la fenêtre de contexte inclut chaque message. Ne dimensionnez pas votre entrée contre la limite brute ; laissez 30–50% de marge pour les réponses.</li>
</ul>
""",
        "it": """
<h2>A cosa serve?</h2>
<p>Ogni interazione con un LLM viene tariffata in <em>token</em> — sotto-unità di parola che il tokenizer del modello ricava dal tuo testo. I token determinano sia i limiti della finestra di contesto ("ci sta?") sia il prezzo ("quanto costerà?"). Il conteggio esatto dipende dal tokenizer del modello, che di solito non hai a portata. Questo strumento ti dà una stima rapida per ogni modello principale affiancato, più il costo in dollari secondo i prezzi pubblicati.</p>

<h3>Quando usarlo</h3>
<ul>
  <li>Dimensionare un system prompt per essere certo che (più l'input utente e un margine per l'output) ci stia nella finestra di contesto.</li>
  <li>Stimare il costo API di un batch job prima di lanciarlo — incolla 100 input rappresentativi e moltiplica.</li>
  <li>Confrontare iterazioni di prompt: il nuovo è davvero più corto o solo sembrava?</li>
  <li>Sanity check quando un errore "troppo lungo" potrebbe venire da whitespace invisibili, BOM o rumore di copia-incolla.</li>
</ul>

<h3>Onestà sull'accuratezza</h3>
<ul>
  <li><strong>Sono euristiche, non i veri tokenizer.</strong> tiktoken (OpenAI), il tokenizer di Anthropic e SentencePiece (Llama, Gemini, Mistral) tagliano il testo in modo diverso. Per prosa inglese stiamo entro ±5%. Codice, JSON denso e CJK arrivano a ±10% o peggio.</li>
  <li><strong>Perché non spediamo i veri tokenizer.</strong> tiktoken da solo è ~1 MB di WASM + dati; caricarlo solo per contare i token gonfierebbe la pagina di 10×. Per conteggi esatti lancia <code>tiktoken</code> in Python localmente o chiama l'endpoint <code>/v1/tokenize</code>.</li>
  <li><strong>Cosa azzecchiamo.</strong> L'ordine relativo (quale modello usa più token per lo stesso testo) è affidabile. Il ranking di costo di solito anche. Gli ordini di grandezza ("è 500 o 5000 token?") sono precisi.</li>
</ul>

<h3>Note sui prezzi</h3>
<ul>
  <li>I costi sono prezzi <em>input</em> per 1M token al 2025. I token di output sono di solito più cari — moltiplica per 3–5 per una stima pessimistica.</li>
  <li>Llama 3 mostra costo zero perché il deployment tipico è self-hosted. Le offerte hosted (Together, Groq, Fireworks) chiedono $0.20–$1 per 1M a seconda della taglia.</li>
  <li>I prezzi cambiano. Controlla la pagina prezzi del provider prima di fidarti di questi numeri per un budget vero.</li>
</ul>

<h3>Trappole comuni</h3>
<ul>
  <li><strong>Token ≠ parole.</strong> Una parola inglese è in media 1.3 token; "antidisestablishmentarianism" è circa 7. Codice e testo strutturato tokenizzano molto più denso per carattere.</li>
  <li><strong>Il CJK è denso.</strong> Ogni carattere cinese / giapponese / coreano può essere un token, quindi 1000 caratteri ≈ 1000 token — molto più caro per "carattere" rispetto all'inglese.</li>
  <li><strong>I caratteri nascosti si sommano.</strong> Testo incollato con zero-width joiner, NBSP o BOM viene contato. Usa l'Unicode Inspector se il conteggio sembra sospettosamente alto.</li>
  <li><strong>Sistema + utente + assistente si sommano.</strong> Il budget della finestra di contesto include ogni messaggio della conversazione. Non dimensionare l'input contro il limite grezzo; lascia 30–50% di margine per le risposte.</li>
</ul>
""",
        "pt": """
<h2>Para que serve?</h2>
<p>Toda interação com um LLM é cobrada em <em>tokens</em> — sub-unidades de palavra que o tokenizador do modelo extrai do seu texto. Tokens determinam tanto os limites da janela de contexto ("isso cabe?") quanto o preço ("quanto vai custar?"). A contagem exata depende do tokenizador do modelo, que você normalmente não tem à mão. Esta ferramenta dá uma estimativa rápida para todos os modelos principais lado a lado, mais o custo em dólares pelos preços publicados.</p>

<h3>Quando usar</h3>
<ul>
  <li>Dimensionar um system prompt pra ter certeza que (mais a entrada do usuário e folga pra saída) cabe na janela de contexto.</li>
  <li>Estimar o custo de API de um job em lote antes de rodar — cole 100 entradas representativas e multiplique.</li>
  <li>Comparar iterações de prompt: o novo prompt ficou mesmo mais curto ou só pareceu?</li>
  <li>Sanity check quando um erro "longo demais" pode vir de whitespace invisível, BOM ou ruído de copy-paste.</li>
</ul>

<h3>Honestidade sobre precisão</h3>
<ul>
  <li><strong>São heurísticas, não os tokenizadores reais.</strong> tiktoken (OpenAI), o tokenizador da Anthropic e SentencePiece (Llama, Gemini, Mistral) cortam o texto de jeito diferente. Pra prosa inglesa, ficamos dentro de ±5%. Código, JSON denso e CJK derivam pra ±10% ou pior.</li>
  <li><strong>Por que não embarcamos os tokenizadores reais.</strong> tiktoken sozinho é ~1 MB de WASM + dados; carregar só pra contar token inflaria a página em 10×. Pra contagens exatas, rode <code>tiktoken</code> em Python localmente ou chame o endpoint <code>/v1/tokenize</code>.</li>
  <li><strong>O que acertamos.</strong> A ordem relativa (qual modelo usa mais tokens pro mesmo texto) é confiável. O ranking de custo geralmente também. Ordens de grandeza ("é 500 ou 5000 tokens?") são precisas.</li>
</ul>

<h3>Notas sobre preços</h3>
<ul>
  <li>Os custos são preços de <em>entrada</em> por 1M tokens em 2025. Tokens de saída costumam ser mais caros — multiplique por 3–5 pra estimativa de pior caso.</li>
  <li>Llama 3 mostra custo zero porque o deploy típico é self-hosted. Ofertas hospedadas (Together, Groq, Fireworks) cobram $0.20–$1 por 1M dependendo do tamanho.</li>
  <li>Preços mudam. Confira a página de preços do provedor antes de confiar nesses números pra um orçamento real.</li>
</ul>

<h3>Pegadinhas comuns</h3>
<ul>
  <li><strong>Tokens ≠ palavras.</strong> Uma palavra em inglês são em média 1.3 tokens; "antidisestablishmentarianism" são uns 7. Código e texto estruturado tokenizam muito mais denso por caractere.</li>
  <li><strong>Texto CJK é denso.</strong> Cada caractere chinês / japonês / coreano pode ser um token, então 1000 chars ≈ 1000 tokens — bem mais caro por "caractere" que inglês.</li>
  <li><strong>Caracteres invisíveis somam.</strong> Texto colado com zero-width joiners, NBSPs ou BOM é contado também. Use o Unicode Inspector se sua contagem parece suspeita.</li>
  <li><strong>Sistema + usuário + assistente acumulam.</strong> O orçamento da janela de contexto inclui cada mensagem da conversa. Não dimensione sua entrada contra o limite cru; deixe 30–50% de folga pra respostas.</li>
</ul>
""",
        "pl": """
<h2>Do czego to służy?</h2>
<p>Każda interakcja z LLM-em jest taryfowana w <em>tokenach</em> — sub-słownych jednostkach, na które tokenizer modelu kraje twój tekst. Tokeny determinują zarówno limity okna kontekstu ("zmieści się?") jak i cenę ("ile to będzie kosztować?"). Dokładna liczba zależy od tokenizera modelu, którego zwykle nie masz pod ręką. To narzędzie daje ci szybką estymację dla każdego głównego modelu obok siebie plus koszt w dolarach wg opublikowanych cen.</p>

<h3>Kiedy tego użyć</h3>
<ul>
  <li>Wymiarowanie system promptu, żeby (plus input usera i zapas na output) zmieścił się w oknie kontekstu.</li>
  <li>Estymacja kosztu API joba batchowego przed odpaleniem — wklej 100 reprezentatywnych inputów i pomnóż.</li>
  <li>Porównywanie iteracji promptów: czy nowy faktycznie się skrócił czy tylko ci się tak wydało?</li>
  <li>Sanity check, kiedy błąd "za długi" mógł wyjść z niewidocznego whitespace'u, BOM-a albo śmieci z kopiuj-wklej.</li>
</ul>

<h3>Uczciwie o dokładności</h3>
<ul>
  <li><strong>To heurystyki, nie prawdziwe tokenizery.</strong> tiktoken (OpenAI), tokenizer Anthropica i SentencePiece (Llama, Gemini, Mistral) kroją tekst inaczej. Dla angielskiej prozy mieścimy się w ±5%. Kod, gęsty JSON i CJK driftują na ±10% lub gorzej.</li>
  <li><strong>Czemu nie wożymy ze sobą prawdziwych tokenizerów.</strong> Sam tiktoken to ~1 MB WASM + dane; ładowanie tylko po to, żeby policzyć tokeny, napompowałoby stronę 10×. Dla dokładnych liczb odpal <code>tiktoken</code> w Pythonie lokalnie albo zawołaj endpoint <code>/v1/tokenize</code>.</li>
  <li><strong>Co trafiamy.</strong> Względna kolejność (który model używa więcej tokenów dla tego samego tekstu) jest wiarygodna. Ranking kosztów zwykle też. Rzędy wielkości ("to 500 czy 5000 tokenów?") są strzałem w dziesiątkę.</li>
</ul>

<h3>O cenach</h3>
<ul>
  <li>Koszty to ceny <em>input</em> za 1M tokenów na 2025. Tokeny output zazwyczaj droższe — pomnóż razy 3–5 dla pesymistycznej estymacji.</li>
  <li>Llama 3 ma koszt zero, bo typowy deployment to self-host. Hosted (Together, Groq, Fireworks) kasują $0.20–$1 za 1M w zależności od rozmiaru.</li>
  <li>Ceny się zmieniają. Sprawdź stronę cenową dostawcy, zanim zaufasz tym liczbom dla realnego budżetu.</li>
</ul>

<h3>Częste pułapki</h3>
<ul>
  <li><strong>Tokeny ≠ słowa.</strong> Angielskie słowo to średnio 1.3 tokenu; "antidisestablishmentarianism" to ~7. Kod i tekst strukturalny tokenizują się dużo gęściej per znak.</li>
  <li><strong>CJK jest gęste.</strong> Każdy chiński / japoński / koreański znak może być osobnym tokenem, więc 1000 znaków ≈ 1000 tokenów — sporo drożej per "znak" niż angielski.</li>
  <li><strong>Ukryte znaki się sumują.</strong> Wklejony tekst z zero-width joinerami, NBSP-ami albo BOM-em też jest liczony. Użyj Unicode Inspectora, jeśli twoja liczba podejrzanie wysoka.</li>
  <li><strong>System + user + assistant się sumują.</strong> Budżet okna kontekstu obejmuje każdą wiadomość rozmowy. Nie wymiaruj inputu pod surowy limit; zostaw 30–50% zapasu na odpowiedzi.</li>
</ul>
""",
        "ja": """
<h2>用途</h2>
<p>LLM とのやり取りはすべて <em>トークン</em> 単位で課金されます。トークンは、モデルのトークナイザがテキストを切り分けるサブワード単位です。コンテキスト窓の上限（「収まるか」）も、料金（「いくら掛かるか」）もトークンで決まります。正確な数はモデルのトークナイザに依存しますが、手元にないことが普通です。本ツールは主要モデルごとの推定値を並べて表示し、公表されている単価で本テキストの推定コスト（USD）も出します。</p>

<h3>使うべきタイミング</h3>
<ul>
  <li>システムプロンプトのサイジング。ユーザー入力と出力余裕を加えてもコンテキスト窓に収まるかを確かめる。</li>
  <li>バッチジョブを実行する前の API コスト見積り。代表的な入力を 100 件貼り付けて掛け算するだけ。</li>
  <li>プロンプト改良の比較。本当に短くなったのか、それともそう感じただけなのか。</li>
  <li>「長すぎる」エラーが、見えない空白・BOM・コピペノイズで膨らんでいないかのサニティチェック。</li>
</ul>

<h3>精度についての本音</h3>
<ul>
  <li><strong>これらはヒューリスティックであり、本物のトークナイザではありません。</strong> tiktoken（OpenAI）、Anthropic のトークナイザ、SentencePiece（Llama・Gemini・Mistral）はそれぞれ切り方が違います。英語散文では ±5% 程度、コード／密な JSON／CJK は ±10% 以上ぶれることがあります。</li>
  <li><strong>本物のトークナイザを同梱しない理由。</strong> tiktoken だけで WASM ＋データで約 1 MB あり、トークンを数えるためだけにページサイズを 10 倍にする価値はありません。厳密な数が必要なら、Python でローカルに <code>tiktoken</code> を回すか、各モデルの <code>/v1/tokenize</code> エンドポイントを叩いてください。</li>
  <li><strong>このツールが正しく示すもの。</strong> 同じテキストでどのモデルがトークンを多く使うかという相対順位は信頼できます。コスト順位もほぼ正確。「500 トークンなのか 5000 なのか」といったオーダー判定はほぼ常に当たります。</li>
</ul>

<h3>料金に関する補足</h3>
<ul>
  <li>料金は 2025 年時点の <em>入力</em> 1M トークン単価です。出力は通常もっと高価で、最悪ケース見積りでは 3〜5 倍してください。</li>
  <li>Llama 3 はゼロ表示にしています。典型的にはセルフホストで使うためです。ホスティング業者（Together、Groq、Fireworks）はサイズに応じて 1M あたり $0.20〜$1 程度を課金します。</li>
  <li>料金は変動します。本気の予算計算では各プロバイダの料金ページを必ず確認してください。</li>
</ul>

<h3>よくある落とし穴</h3>
<ul>
  <li><strong>トークン ≠ 単語。</strong> 英単語は平均 1.3 トークン。長い学術語は 5〜10 トークン。コードや構造化テキストは文字数あたりずっと密度が高いです。</li>
  <li><strong>CJK は密度が高い。</strong> 中日韓の各文字がそのまま 1 トークンになることがあるため、1000 文字 ≒ 1000 トークン。英語に比べて 1 文字あたりの単価がかなり高くなります。</li>
  <li><strong>不可視文字が積み上がる。</strong> ゼロ幅結合子、NBSP、BOM などはすべて数えられます。値が異常に高いときは Unicode Inspector を使ってください。</li>
  <li><strong>System／User／Assistant は積算。</strong> コンテキスト窓は会話全メッセージを含みます。素の上限ぎりぎりで設計せず、応答用に 30〜50% の余裕を残しましょう。</li>
</ul>
""",
        "nl": """
<h2>Waarvoor is dit?</h2>
<p>Elke interactie met een LLM wordt afgerekend in <em>tokens</em> — sub-woord eenheden die de tokenizer van het model uit je tekst hakt. Tokens bepalen zowel context-window limieten ("past dit?") als de prijs ("wat gaat dit kosten?"). Het exacte aantal hangt af van de tokenizer van het model, die je meestal niet bij de hand hebt. Deze tool geeft je een snelle schatting voor elk groot model naast elkaar, plus de dollar-kosten voor de invoer op basis van gepubliceerde prijzen.</p>

<h3>Wanneer gebruiken</h3>
<ul>
  <li>Een system prompt sizen om zeker te weten dat hij (plus user input en marge voor output) in het context window past.</li>
  <li>De API-kosten van een batch job schatten voor je hem draait — plak 100 representatieve inputs en vermenigvuldig.</li>
  <li>Prompt-iteraties vergelijken: is de nieuwe prompt echt korter, of <em>voelde</em> het alleen zo?</li>
  <li>Sanity check als een "te lang"-error kan komen van verborgen whitespace, BOM-markers of copy-paste rommel.</li>
</ul>

<h3>Eerlijk over nauwkeurigheid</h3>
<ul>
  <li><strong>Dit zijn heuristieken, niet de echte tokenizers.</strong> tiktoken (OpenAI), Anthropic's tokenizer en SentencePiece (Llama, Gemini, Mistral) hakken tekst verschillend. Voor Engelse proza komen we binnen ±5%. Code, dichte JSON en CJK driften naar ±10% of slechter.</li>
  <li><strong>Waarom we de echte tokenizers niet meeleveren.</strong> tiktoken alleen al is ~1 MB WASM + data; alleen voor tellen zou de pagina 10× opblazen. Voor exacte tellingen draai <code>tiktoken</code> in Python lokaal of roep het <code>/v1/tokenize</code>-endpoint aan.</li>
  <li><strong>Wat we wel goed doen.</strong> Relatieve volgorde (welk model meer tokens gebruikt voor dezelfde tekst) is betrouwbaar. Kosten-ranking meestal ook. Orde-van-grootte ("is dit 500 of 5000 tokens?") is dead-on.</li>
</ul>

<h3>Prijs-notities</h3>
<ul>
  <li>Kosten zijn <em>input</em>-prijzen per 1M tokens vanaf 2025. Output tokens zijn meestal duurder — vermenigvuldig met 3–5× voor een worst-case schatting.</li>
  <li>Llama 3 toont nul kosten omdat het typisch self-hosted draait. Hosted aanbiedingen (Together, Groq, Fireworks) rekenen $0.20–$1 per 1M afhankelijk van grootte.</li>
  <li>Prijzen veranderen. Check de prijspagina van de aanbieder voor je op deze cijfers vertrouwt voor een echt budget.</li>
</ul>

<h3>Veelvoorkomende valkuilen</h3>
<ul>
  <li><strong>Tokens ≠ woorden.</strong> Een Engels woord is gemiddeld 1.3 tokens; "antidisestablishmentarianism" is ongeveer 7. Code en gestructureerde tekst tokeniseren veel hoger per karakter.</li>
  <li><strong>CJK-tekst is dicht.</strong> Elk Chinees / Japans / Koreaans karakter kan zijn eigen token zijn, dus 1000 karakters ≈ 1000 tokens — veel duurder per "karakter" dan Engels.</li>
  <li><strong>Verborgen karakters tellen op.</strong> Geplakte tekst met zero-width joiners, NBSP's of BOM's wordt ook meegeteld. Gebruik de Unicode Inspector als je telling verdacht hoog lijkt.</li>
  <li><strong>System + user + assistant stapelen.</strong> Het context-window budget omvat elke message in het gesprek. Maat je input niet tegen de ruwe limiet; laat 30–50% marge voor antwoorden.</li>
</ul>
""",
        "tr": """
<h2>Bu ne işe yarar?</h2>
<p>Bir LLM ile her etkileşim <em>token</em> bazında ücretlendirilir — modelin tokenizer'ının metninden kestiği alt-kelime birimleri. Token'lar hem bağlam penceresi sınırlarını ("bu sığar mı?") hem de fiyatı ("ne kadar tutacak?") belirler. Tam sayı modelin tokenizer'ına bağlıdır ki bu genelde elinin altında olmaz. Bu araç her büyük model için yan yana hızlı bir tahmin, artı yayınlanan fiyatlarla bu metin için dolar maliyetini verir.</p>

<h3>Ne zaman kullanılır</h3>
<ul>
  <li>Bir system prompt'u boyutlandırma — (artı kullanıcı girişi ve çıktı için marj) bağlam penceresine sığıyor mu emin olma.</li>
  <li>Bir batch işin API maliyetini çalıştırmadan önce tahmin etme — 100 temsili girdi yapıştır ve çarp.</li>
  <li>Prompt iterasyonlarını karşılaştırma: yeni prompt gerçekten kısaldı mı yoksa öyle mi hissettin?</li>
  <li>Bir "çok uzun" hatasının gizli whitespace, BOM işaretçisi veya copy-paste gürültüsünden kaynaklandığını sanity-check etme.</li>
</ul>

<h3>Doğruluk hakkında dürüstlük</h3>
<ul>
  <li><strong>Bunlar sezgisel yöntemler, gerçek tokenizer'lar değil.</strong> tiktoken (OpenAI), Anthropic'in tokenizer'ı ve SentencePiece (Llama, Gemini, Mistral) metni farklı keser. İngilizce düz yazı için ±%5 içinde kalırız. Kod, yoğun JSON ve CJK ±%10 veya daha kötüye sürüklenir.</li>
  <li><strong>Neden gerçek tokenizer'ları göndermiyoruz.</strong> tiktoken tek başına ~1 MB WASM + veri dosyaları; sırf token saymak için yüklemek sayfayı 10 katına şişirir. Tam sayım için <code>tiktoken</code>'i Python'da yerel çalıştır veya modelin <code>/v1/tokenize</code> endpoint'ini çağır.</li>
  <li><strong>Doğru yaptıklarımız.</strong> Göreli sıralama (hangi model aynı metin için daha fazla token kullanıyor) güvenilirdir. Maliyet sıralaması genellikle doğrudur. Büyüklük sırası tahminleri ("bu 500 mü 5000 mi?") tam isabet.</li>
</ul>

<h3>Fiyat notları</h3>
<ul>
  <li>Maliyetler 2025 itibariyle 1M token başına <em>girdi</em> fiyatlarıdır. Çıktı token'ları genellikle daha pahalıdır — kötü senaryo için 3–5 ile çarp.</li>
  <li>Llama 3 sıfır maliyet gösterir çünkü tipik deployment self-hosted'dır. Hosted teklifler (Together, Groq, Fireworks) boyuta göre 1M başına $0.20–$1 alır.</li>
  <li>Fiyatlar değişir. Gerçek bir bütçe için bu rakamlara güvenmeden önce sağlayıcının fiyat sayfasını kontrol et.</li>
</ul>

<h3>Yaygın tuzaklar</h3>
<ul>
  <li><strong>Token ≠ kelime.</strong> Bir İngilizce kelime ortalama 1.3 token'dır; "antidisestablishmentarianism" yaklaşık 7'dir. Kod ve yapısal metin karakter başına çok daha yoğun tokenize olur.</li>
  <li><strong>CJK metni yoğundur.</strong> Her Çince / Japonca / Korece karakter kendi token'ı olabilir, yani 1000 karakter ≈ 1000 token — İngilizce'den "karakter" başına çok daha pahalı.</li>
  <li><strong>Gizli karakterler birikir.</strong> Sıfır genişlikli birleştiriciler, NBSP veya BOM içeren yapıştırılmış metin de sayılır. Token sayın şüpheli yüksek görünüyorsa Unicode Inspector aracını kullan.</li>
  <li><strong>Sistem + kullanıcı + asistan birikir.</strong> Bağlam penceresi bütçesi sohbetteki her mesajı içerir. Girdini ham limite göre boyutlandırma; cevaplar için %30–50 marj bırak.</li>
</ul>
""",
        "id": """
<h2>Untuk apa ini?</h2>
<p>Setiap interaksi dengan LLM ditagih dalam <em>token</em> — unit sub-kata yang dipotong tokenizer model dari teksmu. Token menentukan baik batas context window ("muat tidak?") maupun harga ("berapa biayanya?"). Hitungan tepat bergantung pada tokenizer model, yang biasanya tidak kamu pegang. Tool ini memberi estimasi cepat untuk tiap model utama berdampingan, plus biaya dolar untuk teks ini berdasarkan harga yang dipublikasikan.</p>

<h3>Kapan digunakan</h3>
<ul>
  <li>Mengukur system prompt agar (plus user input dan margin output) muat di context window.</li>
  <li>Memperkirakan biaya API job batch sebelum dijalankan — tempel 100 input representatif dan kalikan.</li>
  <li>Membandingkan iterasi prompt: apakah prompt baru benar-benar lebih pendek atau cuma terasa begitu?</li>
  <li>Sanity check saat error "terlalu panjang" mungkin disebabkan whitespace tersembunyi, BOM, atau noise copy-paste.</li>
</ul>

<h3>Jujur soal akurasi</h3>
<ul>
  <li><strong>Ini heuristik, bukan tokenizer asli.</strong> tiktoken (OpenAI), tokenizer Anthropic, dan SentencePiece (Llama, Gemini, Mistral) memotong teks beda-beda. Untuk prosa Inggris, kami berada dalam ±5%. Kode, JSON padat, dan CJK bisa drift ke ±10% atau lebih buruk.</li>
  <li><strong>Kenapa kami tidak mengirim tokenizer asli.</strong> tiktoken saja sekitar ~1 MB WASM + file data; memuatnya hanya untuk menghitung token akan menggemukkan halaman 10× lipat. Untuk hitungan tepat, jalankan <code>tiktoken</code> di Python lokal atau panggil endpoint <code>/v1/tokenize</code>.</li>
  <li><strong>Yang kami benar.</strong> Urutan relatif (model mana yang pakai lebih banyak token untuk teks sama) bisa diandalkan. Ranking biaya biasanya akurat. Estimasi orde-of-magnitude ("ini 500 atau 5000 token?") sangat tepat.</li>
</ul>

<h3>Catatan harga</h3>
<ul>
  <li>Biaya adalah harga <em>input</em> per 1M token per 2025. Token output biasanya lebih mahal — kalikan 3–5× untuk estimasi worst-case.</li>
  <li>Llama 3 menampilkan biaya nol karena deployment-nya biasanya self-hosted. Penawaran hosted (Together, Groq, Fireworks) mengenakan $0.20–$1 per 1M tergantung ukuran.</li>
  <li>Harga berubah. Cek halaman harga provider sebelum percaya angka ini untuk budget sungguhan.</li>
</ul>

<h3>Kesalahan umum</h3>
<ul>
  <li><strong>Token ≠ kata.</strong> Satu kata Inggris rata-rata 1.3 token; "antidisestablishmentarianism" sekitar 7. Kode dan teks terstruktur tokenize jauh lebih padat per karakter.</li>
  <li><strong>Teks CJK itu padat.</strong> Tiap karakter Tionghoa / Jepang / Korea bisa jadi satu token, jadi 1000 karakter ≈ 1000 token — jauh lebih mahal per "karakter" dibanding Inggris.</li>
  <li><strong>Karakter tersembunyi menambah.</strong> Teks yang ditempel dengan zero-width joiner, NBSP, atau BOM juga dihitung. Pakai Unicode Inspector kalau jumlah token-mu mencurigakan tinggi.</li>
  <li><strong>System + user + assistant terakumulasi.</strong> Budget context window mencakup tiap pesan di percakapan. Jangan ukur input dengan batas mentah; sisakan 30–50% margin untuk balasan.</li>
</ul>
""",
        "vi": """
<h2>Công cụ này để làm gì?</h2>
<p>Mỗi tương tác với LLM được tính phí theo <em>token</em> — đơn vị sub-từ mà tokenizer của model cắt ra từ văn bản của bạn. Token quyết định cả giới hạn context window ("có vừa không?") lẫn giá ("tốn bao nhiêu?"). Số đếm chính xác phụ thuộc vào tokenizer của model — thường bạn không có sẵn. Công cụ này cho ước tính nhanh cho từng model lớn cạnh nhau, cộng với chi phí USD cho văn bản này theo giá công bố.</p>

<h3>Khi nào nên dùng</h3>
<ul>
  <li>Đo lường system prompt để chắc rằng (cộng với input người dùng và phần dư cho output) vừa với context window.</li>
  <li>Ước tính chi phí API của một batch job trước khi chạy — dán 100 input đại diện và nhân.</li>
  <li>So sánh các phiên bản prompt: prompt mới thực sự ngắn hơn hay chỉ cảm giác vậy?</li>
  <li>Sanity check khi lỗi "quá dài" có thể đến từ whitespace ẩn, BOM, hoặc nhiễu copy-paste.</li>
</ul>

<h3>Trung thực về độ chính xác</h3>
<ul>
  <li><strong>Đây là heuristic, không phải tokenizer thật.</strong> tiktoken (OpenAI), tokenizer của Anthropic, và SentencePiece (Llama, Gemini, Mistral) cắt văn bản khác nhau. Với văn xuôi tiếng Anh, chúng tôi nằm trong ±5%. Mã nguồn, JSON dày đặc, và CJK lệch lên ±10% hoặc tệ hơn.</li>
  <li><strong>Tại sao không nhúng tokenizer thật.</strong> Riêng tiktoken đã ~1 MB WASM + data files; tải chỉ để đếm token sẽ làm trang phình to gấp 10. Cần số đếm chính xác thì chạy <code>tiktoken</code> bằng Python cục bộ hoặc gọi endpoint <code>/v1/tokenize</code> của model.</li>
  <li><strong>Cái mà chúng tôi đúng.</strong> Thứ tự tương đối (model nào dùng nhiều token hơn cho cùng văn bản) đáng tin cậy. Xếp hạng chi phí thường đúng. Ước tính bậc độ lớn ("500 hay 5000 token?") rất chính xác.</li>
</ul>

<h3>Ghi chú về giá</h3>
<ul>
  <li>Chi phí là giá <em>input</em> trên 1M token tính đến 2025. Output token thường đắt hơn — nhân 3–5× cho ước tính tệ nhất.</li>
  <li>Llama 3 hiển thị chi phí 0 vì deployment thường là self-hosted. Các nhà cung cấp hosted (Together, Groq, Fireworks) thu $0.20–$1 mỗi 1M tùy kích thước.</li>
  <li>Giá thay đổi. Kiểm tra trang giá của nhà cung cấp trước khi dựa vào con số này cho ngân sách thật.</li>
</ul>

<h3>Lưu ý thường gặp</h3>
<ul>
  <li><strong>Token ≠ từ.</strong> Một từ tiếng Anh trung bình 1.3 token; "antidisestablishmentarianism" khoảng 7. Mã nguồn và văn bản có cấu trúc tokenize dày đặc hơn nhiều trên mỗi ký tự.</li>
  <li><strong>Văn bản CJK dày.</strong> Mỗi ký tự Trung / Nhật / Hàn có thể là một token, nên 1000 ký tự ≈ 1000 token — đắt hơn nhiều trên mỗi "ký tự" so với tiếng Anh.</li>
  <li><strong>Ký tự ẩn cộng dồn.</strong> Văn bản dán với zero-width joiner, NBSP, hoặc BOM cũng được tính. Dùng Unicode Inspector nếu số token có vẻ cao đáng ngờ.</li>
  <li><strong>System + user + assistant cộng dồn.</strong> Ngân sách context window bao gồm mọi tin nhắn trong cuộc trò chuyện. Đừng tính input theo giới hạn thô; chừa 30–50% margin cho câu trả lời.</li>
</ul>
""",
        "hi": """
<h2>यह किसके लिए है?</h2>
<p>LLM के साथ हर interaction <em>tokens</em> में meter होता है — sub-word units जो model का tokenizer आपके text से काटता है। Tokens दोनों context-window limits ("क्या यह fit होगा?") और pricing ("इसका कितना खर्चा होगा?") को drive करते हैं। सटीक count model के tokenizer पर निर्भर करता है, जो आमतौर पर आपके पास नहीं होता। यह tool हर major model के लिए side-by-side एक तेज़ अनुमान देता है, साथ ही प्रत्येक model की published per-token price के against input text का dollar cost भी।</p>

<h3>कब इस्तेमाल करें</h3>
<ul>
  <li>System prompt को size करना यह सुनिश्चित करने के लिए कि वह (plus expected user input और output के लिए headroom) context window में fit होता है।</li>
  <li>Batch job चलाने से पहले API cost का अनुमान — 100 representative inputs paste करें और multiply करें।</li>
  <li>Prompt-engineering iterations की तुलना: क्या नया prompt वास्तव में छोटा हो गया, या आपको ऐसा <em>लगा</em> कि हुआ?</li>
  <li>Sanity-check कि "यह बहुत लंबा है" error छुपे whitespace, BOM markers, या copy-paste noise के कारण नहीं हुआ।</li>
</ul>

<h3>सटीकता के बारे में ईमानदारी</h3>
<ul>
  <li><strong>ये heuristics हैं, real tokenizers नहीं।</strong> tiktoken (OpenAI), Anthropic का tokenizer, और SentencePiece (Llama, Gemini, Mistral) हर एक text को अलग-अलग काटते हैं। English prose के लिए हमारे अनुमान ±5% के अंदर रहते हैं। Code, dense JSON, और CJK text ±10% या उससे ज़्यादा drift करते हैं।</li>
  <li><strong>हम real tokenizers क्यों ship नहीं करते।</strong> अकेला tiktoken ~1 MB का WASM + data files है; tokens count करने के लिए ही इसे load करना page को दस गुना भारी कर देगा। सटीक counts के लिए Python में local में <code>tiktoken</code> चलाएं या model का <code>/v1/tokenize</code> endpoint call करें।</li>
  <li><strong>हम क्या सही करते हैं।</strong> Relative ordering (कौन सा model उसी text के लिए ज़्यादा tokens उपयोग करता है) reliable है। Cost rankings आमतौर पर सटीक होती हैं। Order-of-magnitude अनुमान ("क्या यह 500 या 5000 tokens है?") dead-on होते हैं।</li>
</ul>

<h3>Pricing notes</h3>
<ul>
  <li>Costs 2025 के अनुसार 1M tokens प्रति <em>input</em> prices हैं। Output tokens आमतौर पर ज़्यादा महंगे होते हैं — worst-case output अनुमान के लिए 3–5× गुणा करें।</li>
  <li>Llama 3 zero cost दिखाता है क्योंकि typical deployment self-hosted होता है। Hosted offerings (Together, Groq, Fireworks) size के आधार पर $0.20–$1 प्रति 1M charge करती हैं।</li>
  <li>Prices बदलती रहती हैं। एक real budget के लिए इन numbers पर भरोसा करने से पहले provider का pricing page चेक करें।</li>
</ul>

<h3>आम गलतियाँ</h3>
<ul>
  <li><strong>Tokens ≠ words.</strong> एक English word औसतन 1.3 tokens है; "antidisestablishmentarianism" लगभग 7 है। Code और structured text प्रति character ज़्यादा densely tokenise होते हैं।</li>
  <li><strong>CJK text dense है।</strong> प्रत्येक Chinese / Japanese / Korean character अपना खुद का token हो सकता है, तो 1000 chars ≈ 1000 tokens — English की तुलना में प्रति "character" बहुत ज़्यादा महंगा।</li>
  <li><strong>Hidden characters जुड़ते हैं।</strong> Zero-width joiners, NBSPs, या BOMs वाला pasted text भी count होता है। यदि आपका token count संदिग्ध रूप से ज़्यादा दिख रहा है तो Unicode Inspector tool उपयोग करें।</li>
  <li><strong>System + user + assistant tokens compound होते हैं।</strong> "Context window" budget में conversation का हर message शामिल होता है। Raw limit के against अपना input size न करें; replies के लिए 30–50% headroom छोड़ें।</li>
</ul>
""",
        "sk": """
<h2>Načo to slúži?</h2>
<p>Každá interakcia s LLM sa účtuje v <em>tokenoch</em> — pod-slovných jednotkách, ktoré tokenizer modelu vyseká z tvojho textu. Tokeny určujú limity kontextového okna („zmestí sa to?") aj cenu („koľko to bude stáť?"). Presný počet závisí od tokenizera modelu, ktorý zvyčajne nemáš poruke. Tento nástroj dá rýchly odhad pre každý hlavný model vedľa seba plus dolárovú cenu za tento text podľa zverejnených cien.</p>

<h3>Kedy to použiť</h3>
<ul>
  <li>Dimenzovanie system promptu — či (plus očakávaný vstup používateľa a rezerva na výstup) zmestí do kontextového okna.</li>
  <li>Odhad API nákladov dávkovej úlohy pred spustením — vlož 100 reprezentatívnych vstupov a vynásob.</li>
  <li>Porovnanie iterácií promptov: skutočne sa nový skrátil alebo sa ti len zdalo?</li>
  <li>Sanity check, či chyba „príliš dlhé" nepochádza zo skrytého whitespace, BOM značiek alebo zvyškov z kopírovania.</li>
</ul>

<h3>Úprimne o presnosti</h3>
<ul>
  <li><strong>Toto sú heuristiky, nie skutočné tokenizery.</strong> tiktoken (OpenAI), Anthropic tokenizer, SentencePiece (Llama, Gemini, Mistral) sekajú text rôzne. Pre anglickú prózu sme do ±5%. Kód, hustý JSON a CJK driftujú na ±10% alebo horšie.</li>
  <li><strong>Prečo neposielame skutočné tokenizery.</strong> Samotný tiktoken je ~1 MB WASM + dáta; načítavať ho len na počítanie tokenov by nafúklo stránku desaťnásobne. Pre presné počty spusti <code>tiktoken</code> v Pythone lokálne alebo zavolaj endpoint <code>/v1/tokenize</code>.</li>
  <li><strong>Čo robíme správne.</strong> Relatívne poradie (ktorý model použije viac tokenov pre rovnaký text) je spoľahlivé. Poradie nákladov tiež. Odhady rádu („je to 500 alebo 5000 tokenov?") sú presné.</li>
</ul>

<h3>Poznámky k cenám</h3>
<ul>
  <li>Ceny sú <em>vstupné</em> za 1M tokenov k roku 2025. Výstupné tokeny sú zvyčajne drahšie — vynásob 3–5× pre najhorší prípad.</li>
  <li>Llama 3 ukazuje nulovú cenu, lebo typický deployment je self-hosted. Hosted ponuky (Together, Groq, Fireworks) účtujú $0.20–$1 za 1M podľa veľkosti.</li>
  <li>Ceny sa menia. Skontroluj cenovú stránku poskytovateľa, kým sa spoľahneš na tieto čísla pre reálny rozpočet.</li>
</ul>

<h3>Časté pasce</h3>
<ul>
  <li><strong>Tokeny ≠ slová.</strong> Anglické slovo má priemerne 1.3 tokenu; „antidisestablishmentarianism" má asi 7. Kód a štruktúrovaný text tokenizujú hustejšie na znak.</li>
  <li><strong>CJK text je hustý.</strong> Každý čínsky / japonský / kórejský znak môže byť vlastný token, takže 1000 znakov ≈ 1000 tokenov — oveľa drahšie na „znak" než angličtina.</li>
  <li><strong>Skryté znaky sa sčítajú.</strong> Vložený text so zero-width joinermi, NBSP alebo BOM sa tiež počíta. Použi Unicode Inspector, ak ti počet vychádza podozrivo vysoký.</li>
  <li><strong>System + user + assistant sa sčítavajú.</strong> Rozpočet kontextového okna zahŕňa každú správu konverzácie. Nedimenzuj vstup proti surovému limitu; nechaj 30–50% rezervu na odpovede.</li>
</ul>
""",
        "cs": """
<h2>K čemu to slouží?</h2>
<p>Každá interakce s LLM se účtuje v <em>tokenech</em> — pod-slovních jednotkách, které tokenizer modelu vyseká z tvého textu. Tokeny určují limity kontextového okna („vejde se to?") i cenu („kolik to bude stát?"). Přesný počet závisí na tokenizeru modelu, který obvykle nemáš po ruce. Tento nástroj dá rychlý odhad pro každý hlavní model vedle sebe plus dolarovou cenu za tento text podle zveřejněných cen.</p>

<h3>Kdy to použít</h3>
<ul>
  <li>Dimenzování system promptu — zda (plus očekávaný vstup uživatele a rezerva na výstup) vejde do kontextového okna.</li>
  <li>Odhad API nákladů dávkové úlohy před spuštěním — vlož 100 reprezentativních vstupů a vynásob.</li>
  <li>Porovnání iterací promptů: opravdu se nový zkrátil nebo se ti to jen zdálo?</li>
  <li>Sanity check, jestli chyba „příliš dlouhé" nepochází ze skrytého whitespace, BOM značek nebo zbytků z kopírování.</li>
</ul>

<h3>Upřímně o přesnosti</h3>
<ul>
  <li><strong>Tohle jsou heuristiky, ne skutečné tokenizery.</strong> tiktoken (OpenAI), Anthropic tokenizer, SentencePiece (Llama, Gemini, Mistral) sekají text různě. Pro anglickou prózu jsme do ±5%. Kód, hustý JSON a CJK driftují na ±10% nebo hůř.</li>
  <li><strong>Proč neposíláme skutečné tokenizery.</strong> Samotný tiktoken je ~1 MB WASM + data; načítat ho jen kvůli počítání tokenů by nafouklo stránku desetinásobně. Pro přesné počty spusť <code>tiktoken</code> v Pythonu lokálně nebo zavolej endpoint <code>/v1/tokenize</code>.</li>
  <li><strong>Co děláme správně.</strong> Relativní pořadí (který model používá více tokenů pro stejný text) je spolehlivé. Pořadí nákladů taky. Odhady řádu („je to 500 nebo 5000 tokenů?") jsou přesné.</li>
</ul>

<h3>Poznámky k cenám</h3>
<ul>
  <li>Ceny jsou <em>vstupní</em> za 1M tokenů k roku 2025. Výstupní tokeny jsou obvykle dražší — vynásob 3–5× pro nejhorší případ.</li>
  <li>Llama 3 ukazuje nulovou cenu, protože typický deployment je self-hosted. Hosted nabídky (Together, Groq, Fireworks) účtují $0.20–$1 za 1M podle velikosti.</li>
  <li>Ceny se mění. Zkontroluj cenovou stránku poskytovatele, než se spolehneš na tato čísla pro reálný rozpočet.</li>
</ul>

<h3>Časté pasti</h3>
<ul>
  <li><strong>Tokeny ≠ slova.</strong> Anglické slovo má průměrně 1.3 tokenu; „antidisestablishmentarianism" má asi 7. Kód a strukturovaný text tokenizují hustěji na znak.</li>
  <li><strong>CJK text je hustý.</strong> Každý čínský / japonský / korejský znak může být vlastní token, takže 1000 znaků ≈ 1000 tokenů — mnohem dražší na „znak" než angličtina.</li>
  <li><strong>Skryté znaky se sčítají.</strong> Vložený text se zero-width joinery, NBSP nebo BOM se taky počítá. Použij Unicode Inspector, pokud ti počet vychází podezřele vysoký.</li>
  <li><strong>System + user + assistant se sčítají.</strong> Rozpočet kontextového okna zahrnuje každou zprávu konverzace. Nedimenzuj vstup proti surovému limitu; nech 30–50% rezervu na odpovědi.</li>
</ul>
""",
    },
    "related": ["unicode-inspector", "word-counter", "json-formatter"],
    "howto": {"flow": "calculate", "action": "compare", "noun": "text"},
}
