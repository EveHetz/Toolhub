TOOL = {
    "slug": "xp-curve-calculator",
    "category": "gaming",
    "icon": "📈",
    "tags": ["xp", "experience", "level", "curve", "rpg", "progression", "game-dev"],
    "i18n": {
        "en": {
            "name": "XP Curve Calculator",
            "tagline": "Generate XP-per-level tables for RPG progression. Linear, polynomial, exponential, or custom curves. See the full table + visual graph + copy as JSON/CSV.",
            "description": "Free RPG XP curve generator. Pick a curve type (linear, polynomial e.g. n², exponential, logarithmic, fibonacci-like), set base + multiplier + max level. See the resulting XP-per-level table + cumulative XP + level-up gaps. Visualize as a simple SVG line chart. Copy as JSON for engine config or CSV for spreadsheets. Browser-only — no signup.",
        },
        "de": {
            "name": "XP-Kurven-Rechner",
            "tagline": "Generiere XP-pro-Level-Tabellen für RPG-Progression. Linear, polynomial, exponentiell oder eigene Kurven. Volle Tabelle + Grafik + Export als JSON/CSV.",
            "description": "Kostenloser RPG-XP-Kurven-Generator. Wähle einen Kurventyp (linear, polynomial z. B. n², exponentiell, logarithmisch, fibonacci-artig), setze Basis-XP, Multiplikator und Max-Level. Erhalte die XP-pro-Level-Tabelle, kumulative XP und Lücken pro Level-Up. Visualisierung als SVG-Liniendiagramm. Kopierbar als JSON für Engine-Configs oder CSV für Tabellen. Nur im Browser — keine Anmeldung.",
        },
        "es": {
            "name": "Calculadora de curva de XP",
            "tagline": "Genera tablas de XP por nivel para progresión RPG. Curvas lineal, polinómica, exponencial o personalizada. Tabla completa + gráfica + copiar como JSON/CSV.",
            "description": "Generador gratuito de curvas de XP para RPG. Elige el tipo de curva (lineal, polinómica p. ej. n², exponencial, logarítmica, tipo fibonacci), define XP base, multiplicador y nivel máximo. Obtén la tabla de XP por nivel, XP acumulada y huecos entre niveles. Visualización como gráfica SVG. Copia como JSON para configuración del motor o CSV para hojas de cálculo. Solo en el navegador — sin registro.",
        },
        "fr": {
            "name": "Calculateur de courbe d'XP",
            "tagline": "Génère des tables d'XP par niveau pour la progression RPG. Courbes linéaire, polynomiale, exponentielle ou personnalisée. Table + graphique + export JSON/CSV.",
            "description": "Générateur gratuit de courbe d'XP pour RPG. Choisis un type de courbe (linéaire, polynomiale p. ex. n², exponentielle, logarithmique, type fibonacci), règle l'XP de base, le multiplicateur et le niveau max. Obtiens la table d'XP par niveau, l'XP cumulée et les écarts entre niveaux. Visualisation en graphique SVG. Copie en JSON pour la config du moteur ou CSV pour tableur. Tout dans le navigateur — pas d'inscription.",
        },
        "it": {
            "name": "Calcolatore di curva XP",
            "tagline": "Genera tabelle di XP per livello per la progressione RPG. Curve lineare, polinomiale, esponenziale o personalizzata. Tabella + grafico + export JSON/CSV.",
            "description": "Generatore gratuito di curve XP per RPG. Scegli il tipo di curva (lineare, polinomiale es. n², esponenziale, logaritmica, tipo fibonacci), imposta XP base, moltiplicatore e livello massimo. Ottieni la tabella XP per livello, XP cumulativa e gap tra livelli. Visualizzazione come grafico SVG. Copia come JSON per la config del motore o CSV per fogli di calcolo. Solo nel browser — nessuna registrazione.",
        },
        "pt": {
            "name": "Calculadora de curva de XP",
            "tagline": "Gere tabelas de XP por nível para progressão de RPG. Curvas linear, polinomial, exponencial ou personalizada. Tabela + gráfico + export JSON/CSV.",
            "description": "Gerador gratuito de curva de XP para RPG. Escolha o tipo de curva (linear, polinomial p. ex. n², exponencial, logarítmica, tipo fibonacci), defina XP base, multiplicador e nível máximo. Veja a tabela de XP por nível, XP cumulativo e gaps entre níveis. Visualização como gráfico SVG. Copie como JSON para config de engine ou CSV para planilhas. Só no browser — sem cadastro.",
        },
        "pl": {
            "name": "Kalkulator krzywej XP",
            "tagline": "Generuj tabele XP na poziom dla progresji RPG. Krzywe liniowa, wielomianowa, wykładnicza albo własna. Pełna tabela + wykres + eksport jako JSON/CSV.",
            "description": "Darmowy generator krzywej XP do RPG. Wybierz typ krzywej (liniowa, wielomianowa np. n², wykładnicza, logarytmiczna, fibonacci-like), ustaw bazowe XP, mnożnik i maks. poziom. Dostań tabelę XP na poziom, skumulowane XP i odstępy między poziomami. Wizualizacja jako wykres liniowy SVG. Kopiuj jako JSON do configa silnika albo CSV do arkuszy. Tylko w przeglądarce — bez rejestracji.",
        },
        "ja": {
            "name": "XP カーブ計算機",
            "tagline": "RPG の成長カーブ（レベルごとの必要 XP）を生成。線形・多項式・指数・対数・カスタム。テーブル＋グラフ＋JSON／CSV にコピー。",
            "description": "RPG 向けの無料 XP カーブジェネレーター。カーブ種別（線形、多項式（例：n²）、指数、対数、フィボナッチ風）を選び、ベース XP、係数、最大レベルを設定すると、レベルごとの必要 XP、累計 XP、レベルアップ差分を表示します。SVG 折れ線グラフで可視化。エンジン設定用の JSON や、表計算用の CSV としてコピーできます。ブラウザ内のみ — サインアップ不要。",
        },
        "nl": {
            "name": "XP-curve-calculator",
            "tagline": "Genereer XP-per-level-tabellen voor RPG-progressie. Lineair, polynomiaal, exponentieel of eigen curve. Volledige tabel + grafiek + export als JSON/CSV.",
            "description": "Gratis RPG XP-curve-generator. Kies een curve-type (lineair, polynomiaal bijv. n², exponentieel, logaritmisch, fibonacci-achtig), stel basis-XP, multiplier en max level in. Krijg de XP-per-level-tabel, cumulatieve XP en gaps tussen levels. Visualisatie als simpele SVG-lijngrafiek. Kopieer als JSON voor engine-config of CSV voor spreadsheets. Alleen in de browser — geen signup.",
        },
        "tr": {
            "name": "XP Eğrisi Hesaplayıcı",
            "tagline": "RPG ilerlemesi için seviye başına XP tabloları üret. Lineer, polinom, üstel veya özel eğriler. Tablo + grafik + JSON/CSV olarak kopyala.",
            "description": "Ücretsiz RPG XP eğrisi üreteci. Bir eğri tipi seç (lineer, polinom örn. n², üstel, logaritmik, fibonacci benzeri), baz XP, çarpan ve maks. seviyeyi ayarla. Seviye başına XP tablosu, kümülatif XP ve seviye atlama farklarını gör. SVG çizgi grafik ile görselleştir. Motor config'i için JSON veya tablo için CSV olarak kopyala. Sadece tarayıcıda — kayıt yok.",
        },
        "id": {
            "name": "Kalkulator kurva XP",
            "tagline": "Hasilkan tabel XP per level untuk progresi RPG. Kurva linear, polinomial, eksponensial, atau custom. Tabel lengkap + grafik + salin sebagai JSON/CSV.",
            "description": "Generator kurva XP RPG gratis. Pilih jenis kurva (linear, polinomial mis. n², eksponensial, logaritmik, mirip fibonacci), atur XP dasar, pengali, dan level maksimum. Lihat tabel XP per level, XP kumulatif, dan gap antar level. Visualisasi sebagai grafik garis SVG sederhana. Salin sebagai JSON untuk config engine atau CSV untuk spreadsheet. Hanya di browser — tanpa daftar.",
        },
        "vi": {
            "name": "Máy tính đường cong XP",
            "tagline": "Tạo bảng XP theo cấp cho tiến trình RPG. Đường cong tuyến tính, đa thức, hàm mũ hoặc tùy chỉnh. Bảng đầy đủ + biểu đồ + sao chép dưới dạng JSON/CSV.",
            "description": "Trình tạo đường cong XP RPG miễn phí. Chọn loại đường cong (tuyến tính, đa thức ví dụ n², hàm mũ, logarit, kiểu fibonacci), đặt XP cơ sở, hệ số và cấp tối đa. Xem bảng XP theo cấp, XP tích lũy và khoảng cách giữa các cấp. Hiển thị dưới dạng biểu đồ đường SVG đơn giản. Sao chép thành JSON cho cấu hình engine hoặc CSV cho bảng tính. Chỉ trong trình duyệt — không cần đăng ký.",
        },
        "hi": {
            "name": "XP Curve Calculator",
            "tagline": "RPG progression के लिए XP-per-level tables generate करें। Linear, polynomial, exponential, या custom curves। पूरी table + visual graph + JSON/CSV के रूप में copy।",
            "description": "मुफ़्त RPG XP curve generator। एक curve type चुनें (linear, polynomial जैसे n², exponential, logarithmic, fibonacci-like), base + multiplier + max level set करें। Resulting XP-per-level table + cumulative XP + level-up gaps देखें। एक सरल SVG line chart के रूप में visualize करें। Engine config के लिए JSON या spreadsheets के लिए CSV के रूप में copy करें। केवल browser में — कोई signup नहीं।",
        },
        "sk": {
            "name": "Kalkulačka XP krivky",
            "tagline": "Vygeneruj tabuľky XP na úroveň pre RPG progresiu. Lineárna, polynomická, exponenciálna alebo vlastná krivka. Tabuľka + graf + export JSON/CSV.",
            "description": "Bezplatný generátor XP krivky pre RPG. Vyber typ krivky (lineárna, polynomická napr. n², exponenciálna, logaritmická, fibonacci-style), nastav základné XP, multiplikátor a max. úroveň. Dostaneš tabuľku XP na úroveň, kumulatívne XP a medzery medzi úrovňami. Vizualizácia ako jednoduchý SVG čiarový graf. Skopíruj ako JSON pre engine config alebo CSV pre tabuľky. Iba v prehliadači — žiadna registrácia.",
        },
        "cs": {
            "name": "Kalkulačka XP křivky",
            "tagline": "Vygeneruj tabulky XP na úroveň pro RPG progresi. Lineární, polynomická, exponenciální nebo vlastní křivka. Tabulka + graf + export JSON/CSV.",
            "description": "Bezplatný generátor XP křivky pro RPG. Vyber typ křivky (lineární, polynomická např. n², exponenciální, logaritmická, fibonacci-style), nastav základní XP, multiplikátor a max. úroveň. Dostaneš tabulku XP na úroveň, kumulativní XP a mezery mezi úrovněmi. Vizualizace jako jednoduchý SVG čárový graf. Zkopíruj jako JSON pro engine config nebo CSV pro tabulky. Jen v prohlížeči — žádná registrace.",
        },
    },
    "body": """
<div class="tool-card">
  <div class="row-2col">
    <div>
      <label>Curve type</label>
      <select id="xpc-curve" onchange="xpcRun()">
        <option value="linear">Linear: base × level</option>
        <option value="poly" selected>Polynomial: base × level^exp</option>
        <option value="exp">Exponential: base × mult^(level-1)</option>
        <option value="log">Logarithmic: base × ln(level+1) × mult</option>
        <option value="fib">Fibonacci-like: f(n)=f(n-1)+f(n-2)</option>
      </select>
    </div>
    <div>
      <label>Max level</label>
      <input type="number" id="xpc-max" value="50" min="2" max="500" oninput="xpcRun()">
    </div>
  </div>
  <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:1rem;margin-top:0.7rem">
    <div>
      <label>Base XP <span class="meta" style="font-weight:normal">(level 1 cost)</span></label>
      <input type="number" id="xpc-base" value="100" min="1" step="any" oninput="xpcRun()">
    </div>
    <div>
      <label id="xpc-l-mult">Exponent</label>
      <input type="number" id="xpc-mult" value="2" min="0.1" max="10" step="0.05" oninput="xpcRun()">
    </div>
    <div>
      <label>Starting level</label>
      <input type="number" id="xpc-start" value="1" min="1" oninput="xpcRun()">
    </div>
  </div>
</div>
<div class="tool-card">
  <label>Curve preview</label>
  <div id="xpc-chart" style="background:var(--code-bg);border:1px solid var(--border);border-radius:6px;padding:0.6rem"></div>
  <div class="meta" id="xpc-chart-meta" style="margin-top:0.5rem"></div>
</div>
<div class="tool-card">
  <label>Level table</label>
  <div style="overflow:auto;max-height:360px;border:1px solid var(--border);border-radius:6px">
    <table id="xpc-table" style="width:100%;border-collapse:collapse;font-family:ui-monospace,monospace;font-size:0.88rem">
      <thead style="position:sticky;top:0;background:var(--bg-elev)">
        <tr><th style="text-align:left;padding:0.4rem 0.7rem;border-bottom:1px solid var(--border)">Level</th><th style="text-align:right;padding:0.4rem 0.7rem;border-bottom:1px solid var(--border)">XP to next</th><th style="text-align:right;padding:0.4rem 0.7rem;border-bottom:1px solid var(--border)">Cumulative</th><th style="text-align:right;padding:0.4rem 0.7rem;border-bottom:1px solid var(--border)">Gap</th></tr>
      </thead>
      <tbody id="xpc-tbody"></tbody>
    </table>
  </div>
</div>
<div class="tool-card">
  <label>Export</label>
  <div class="button-row" style="flex-wrap:wrap">
    <button type="button" onclick="xpcCopy('json')">Copy JSON</button>
    <button type="button" onclick="xpcCopy('csv')">Copy CSV</button>
    <button type="button" onclick="xpcCopy('lua')">Copy Lua table</button>
    <button type="button" onclick="xpcCopy('py')">Copy Python dict</button>
  </div>
  <div id="xpc-copy-status" class="meta" style="margin-top:0.5rem;min-height:1.2rem"></div>
</div>
""",
    "script": """
<style>
#xpc-tbody tr:nth-child(even){background:var(--bg-elev)}
#xpc-tbody td{padding:0.3rem 0.7rem;border-bottom:1px dashed var(--border)}
#xpc-tbody td:first-child{color:var(--accent);font-weight:600}
#xpc-tbody td:not(:first-child){text-align:right}
.xpc-overflow{color:#f85149;font-weight:600}
</style>
<script>
let xpcRows = [];
function xpcFmt(n){
  if(!isFinite(n)) return '∞';
  if(n >= 1e12) return n.toExponential(3);
  if(n >= 1e6) return Math.round(n).toLocaleString('en-US');
  return Math.round(n).toLocaleString('en-US');
}
function xpcCompute(){
  const curve = document.getElementById('xpc-curve').value;
  const base = parseFloat(document.getElementById('xpc-base').value);
  const mult = parseFloat(document.getElementById('xpc-mult').value);
  const maxL = Math.max(2, Math.min(500, parseInt(document.getElementById('xpc-max').value, 10) || 50));
  const startL = Math.max(1, parseInt(document.getElementById('xpc-start').value, 10) || 1);
  document.getElementById('xpc-max').value = maxL;
  document.getElementById('xpc-start').value = startL;
  // Label for the second slider depending on curve type
  const lblMult = document.getElementById('xpc-l-mult');
  lblMult.textContent = {linear:'Multiplier (per level)',poly:'Exponent',exp:'Multiplier (per level)',log:'Multiplier',fib:'(unused for fibonacci)'}[curve];
  const xp = (n) => {
    switch(curve){
      case 'linear': return base * n * mult;
      case 'poly':   return base * Math.pow(n, mult);
      case 'exp':    return base * Math.pow(mult, n - 1);
      case 'log':    return base * Math.log(n + 1) * mult;
      case 'fib':    {
        // f(1)=base, f(2)=base, f(n)=f(n-1)+f(n-2)
        if(n <= 2) return base;
        let a = base, b = base;
        for(let i = 3; i <= n; i++){ const c = a + b; a = b; b = c; }
        return b;
      }
    }
    return 0;
  };
  const rows = [];
  let cum = 0;
  let prev = null;
  for(let lv = startL; lv <= maxL; lv++){
    const v = Math.floor(xp(lv));
    cum += v;
    rows.push({ level: lv, xp: v, cum, gap: prev === null ? null : v - prev });
    prev = v;
  }
  return rows;
}
function xpcRenderTable(rows){
  const tb = document.getElementById('xpc-tbody');
  const parts = [];
  for(const r of rows){
    const overflow = !isFinite(r.xp) || r.xp > 1e15;
    const cls = overflow ? ' class="xpc-overflow"' : '';
    parts.push(`<tr><td>${r.level}</td><td${cls}>${xpcFmt(r.xp)}</td><td${cls}>${xpcFmt(r.cum)}</td><td${cls}>${r.gap === null ? '—' : xpcFmt(r.gap)}</td></tr>`);
  }
  tb.innerHTML = parts.join('');
}
function xpcRenderChart(rows){
  if(rows.length < 2){ document.getElementById('xpc-chart').innerHTML = '<div class="meta">Need at least 2 levels.</div>'; return; }
  const W = 600, H = 240, PAD_L = 56, PAD_R = 12, PAD_T = 12, PAD_B = 28;
  const xs = rows.map(r => r.level);
  const ys = rows.map(r => isFinite(r.xp) ? r.xp : 0);
  const xMin = Math.min(...xs), xMax = Math.max(...xs);
  const yMax = Math.max(...ys, 1);
  const sx = v => PAD_L + (v - xMin) / Math.max(1, xMax - xMin) * (W - PAD_L - PAD_R);
  const sy = v => H - PAD_B - v / yMax * (H - PAD_T - PAD_B);
  const path = rows.map((r,i) => `${i===0?'M':'L'}${sx(r.level).toFixed(1)},${sy(isFinite(r.xp)?r.xp:0).toFixed(1)}`).join(' ');
  // 4 horizontal gridlines + y-axis labels
  const ticks = [0, 0.25, 0.5, 0.75, 1];
  const yLabels = ticks.map(t => {
    const v = yMax * t;
    const y = H - PAD_B - t * (H - PAD_T - PAD_B);
    return `<line x1="${PAD_L}" y1="${y}" x2="${W - PAD_R}" y2="${y}" stroke="currentColor" stroke-opacity="0.15"/><text x="${PAD_L - 6}" y="${y + 3}" text-anchor="end" font-size="10" fill="currentColor" opacity="0.7">${xpcFmt(v)}</text>`;
  }).join('');
  // x-axis ticks: ~5 marks
  const nXTicks = Math.min(6, rows.length);
  const xLabels = [];
  for(let i = 0; i < nXTicks; i++){
    const t = nXTicks === 1 ? 0 : i / (nXTicks - 1);
    const lv = Math.round(xMin + t * (xMax - xMin));
    const x = sx(lv);
    xLabels.push(`<text x="${x}" y="${H - 8}" text-anchor="middle" font-size="10" fill="currentColor" opacity="0.7">${lv}</text>`);
  }
  const svg = `
    <svg viewBox="0 0 ${W} ${H}" style="width:100%;height:auto;color:var(--text);font-family:ui-monospace,monospace" xmlns="http://www.w3.org/2000/svg">
      ${yLabels}
      <path d="${path}" fill="none" stroke="var(--accent)" stroke-width="2"/>
      ${xLabels.join('')}
      <line x1="${PAD_L}" y1="${H - PAD_B}" x2="${W - PAD_R}" y2="${H - PAD_B}" stroke="currentColor" stroke-opacity="0.3"/>
    </svg>`;
  document.getElementById('xpc-chart').innerHTML = svg;
  document.getElementById('xpc-chart-meta').textContent = `Levels ${xMin}–${xMax} · peak XP to next = ${xpcFmt(yMax)} · total = ${xpcFmt(rows.reduce((s,r)=>s+(isFinite(r.xp)?r.xp:0),0))}`;
}
function xpcRun(){
  xpcRows = xpcCompute();
  xpcRenderTable(xpcRows);
  xpcRenderChart(xpcRows);
}
function xpcSerialize(fmt){
  if(fmt === 'json'){
    const obj = {};
    for(const r of xpcRows) obj[r.level] = r.xp;
    return JSON.stringify(obj, null, 2);
  }
  if(fmt === 'csv'){
    const lines = ['level,xp_to_next,cumulative,gap'];
    for(const r of xpcRows) lines.push(`${r.level},${r.xp},${r.cum},${r.gap===null?'':r.gap}`);
    return lines.join('\\n');
  }
  if(fmt === 'lua'){
    const lines = ['local XP_TABLE = {'];
    for(const r of xpcRows) lines.push(`  [${r.level}] = ${r.xp},`);
    lines.push('}');
    return lines.join('\\n');
  }
  if(fmt === 'py'){
    const lines = ['XP_TABLE = {'];
    for(const r of xpcRows) lines.push(`    ${r.level}: ${r.xp},`);
    lines.push('}');
    return lines.join('\\n');
  }
  return '';
}
function xpcCopy(fmt){
  const text = xpcSerialize(fmt);
  navigator.clipboard.writeText(text).then(() => {
    const s = document.getElementById('xpc-copy-status');
    s.textContent = `✓ Copied ${fmt.toUpperCase()} (${text.length} chars).`;
    setTimeout(() => { s.textContent = ''; }, 2200);
  });
}
document.addEventListener('DOMContentLoaded', xpcRun);
</script>
""",
    "help": {
        "en": """
<h2>What is this for?</h2>
<p>Tuning the experience curve is one of the more delicate jobs in RPG design. Too flat and the game has no late-game weight; too steep and the player hits a wall and quits. This tool generates the XP-per-level table for five canonical curve types so you can compare them side-by-side, eyeball the shape, and copy the result into your engine config without doing the maths by hand each time you tweak a constant.</p>

<h3>The curve types</h3>
<ul>
  <li><strong>Linear</strong> (<code>base × level × mult</code>) — the simplest. Constant delta between levels. Feels grindy at low levels and trivial at high levels because the proportional jump shrinks. Used in casual / mobile games for predictability.</li>
  <li><strong>Polynomial</strong> (<code>base × level^exp</code>) — the default. With <code>exp = 2</code> it's the classic quadratic curve used in D&D-likes and most JRPGs. Tune the exponent to control late-game grind: 1.5 is gentle, 2.5+ is brutal.</li>
  <li><strong>Exponential</strong> (<code>base × mult^(level-1)</code>) — uncontrollable growth. With <code>mult = 2</code>, level 30 costs ~500 million XP. Use only for very short max-level games or with <code>mult</code> close to 1.</li>
  <li><strong>Logarithmic</strong> (<code>base × ln(level+1) × mult</code>) — diminishing returns. The next level always costs <em>more</em>, but only slightly. Useful for cosmetic / mastery systems where players plateau.</li>
  <li><strong>Fibonacci-like</strong> — a recursive curve (<em>f(n) = f(n-1) + f(n-2)</em>) that grows roughly exponentially (golden-ratio rate) but with the characteristic "bumpy" feel from the sum-of-previous structure. Popular in roguelikes.</li>
</ul>

<h3>What the table shows</h3>
<ul>
  <li><strong>XP to next</strong> — the XP required to go from this level to the next.</li>
  <li><strong>Cumulative</strong> — total XP needed to reach this level from zero. Useful for save-file design.</li>
  <li><strong>Gap</strong> — change in XP-to-next from the previous level. Lets you spot discontinuities.</li>
</ul>

<h3>Common gotchas</h3>
<ul>
  <li><strong>Exponentials blow up fast.</strong> <code>2^50</code> is roughly 10¹⁵ — way past 32-bit integer territory and into JavaScript-loses-precision territory. The table will show overflow values in red.</li>
  <li><strong>Cumulative XP is what players see.</strong> Storage on the player object is usually total XP, not "XP into the current level". The cumulative column is your save-file value.</li>
  <li><strong>Tune by feel, not maths.</strong> The "right" curve depends on encounter pacing, XP rewards, and content density. The shape on the chart should match the difficulty ramp you've designed for content.</li>
  <li><strong>Power-of-two growth is a trap.</strong> <code>exp = 2</code> on polynomial means level 100 costs 100× level 10. That's usually fine. <code>mult = 2</code> on exponential means level 100 costs 10²⁹× level 10. That isn't.</li>
  <li><strong>Fibonacci on long max-level games.</strong> Fibonacci's growth rate is the golden ratio (~1.618ⁿ), so it still blows up — slower than 2ⁿ but faster than n². For a max-level-100 game, expect the last few levels to be wildly more expensive than the early ones.</li>
</ul>
""",
        "de": """
<h2>Wofür ist das?</h2>
<p>Die XP-Kurve zu tunen ist eine der heikleren Aufgaben im RPG-Design. Zu flach und das Late-Game hat kein Gewicht; zu steil und der Spieler steht vor einer Wand und hört auf. Dieses Tool generiert die XP-pro-Level-Tabelle für fünf kanonische Kurventypen, damit du sie Seite an Seite vergleichen, die Form abschätzen und das Ergebnis in deine Engine-Config kopieren kannst, ohne die Mathematik jedes Mal neu zu rechnen.</p>

<h3>Kurventypen</h3>
<ul>
  <li><strong>Linear</strong> — konstantes Delta zwischen Levels. Wirkt früh wie Grind, spät trivial.</li>
  <li><strong>Polynomial</strong> (<code>base × level^exp</code>) — der Klassiker. Mit <code>exp = 2</code> die quadratische Kurve aus D&D / JRPGs.</li>
  <li><strong>Exponentiell</strong> — wächst unkontrollierbar. Mit <code>mult = 2</code> kostet Level 30 ~500 Mio XP.</li>
  <li><strong>Logarithmisch</strong> — abnehmender Ertrag, gut für Mastery-Systeme.</li>
  <li><strong>Fibonacci-artig</strong> — rekursiv, wächst ungefähr mit Goldenem Schnitt.</li>
</ul>

<h3>Stolperfallen</h3>
<ul>
  <li><strong>Exponentialkurven explodieren schnell.</strong> Überlauf-Werte sind rot markiert.</li>
  <li><strong>Cumulative ist der Save-Wert.</strong> Speichere Gesamt-XP, nicht "XP im aktuellen Level".</li>
  <li><strong>Tunen nach Gefühl.</strong> Die "richtige" Kurve hängt von Encounter-Pacing und Content-Dichte ab.</li>
</ul>
""",
        "es": """
<h2>¿Para qué sirve?</h2>
<p>Ajustar la curva de experiencia es uno de los trabajos más delicados en diseño de RPG. Demasiado plana y el end-game pierde peso; demasiado empinada y el jugador choca contra un muro. Esta herramienta genera la tabla XP-por-nivel para cinco tipos canónicos de curva para que los compares en paralelo y copies el resultado a la config del motor sin rehacer las cuentas cada vez.</p>

<h3>Tipos de curva</h3>
<ul>
  <li><strong>Lineal</strong> — delta constante. Aburrido pronto, trivial tarde.</li>
  <li><strong>Polinómica</strong> (<code>base × nivel^exp</code>) — el default. <code>exp = 2</code> es la cuadrática clásica.</li>
  <li><strong>Exponencial</strong> — crecimiento incontrolable. Solo para max-level bajos.</li>
  <li><strong>Logarítmica</strong> — rendimientos decrecientes, ideal para mastery.</li>
  <li><strong>Tipo Fibonacci</strong> — recursiva, crecimiento ~razón áurea.</li>
</ul>

<h3>Cuidados comunes</h3>
<ul>
  <li><strong>Las exponenciales explotan rápido.</strong> Los desbordes salen en rojo.</li>
  <li><strong>El XP acumulado es lo que se guarda.</strong> Almacena el total, no "XP del nivel actual".</li>
  <li><strong>Ajusta por sensación.</strong> La curva "correcta" depende del ritmo de encuentros y la densidad de contenido.</li>
</ul>
""",
        "fr": """
<h2>À quoi ça sert ?</h2>
<p>Régler la courbe d'XP est l'un des travaux les plus délicats en game design RPG. Trop plate et la fin de partie n'a aucun poids ; trop raide et le joueur tape un mur. Cet outil génère la table XP-par-niveau pour cinq types de courbe canoniques pour les comparer côte à côte et copier le résultat dans la config moteur sans refaire les calculs.</p>

<h3>Types de courbe</h3>
<ul>
  <li><strong>Linéaire</strong> — delta constant. Grindy tôt, trivial tard.</li>
  <li><strong>Polynomiale</strong> (<code>base × niveau^exp</code>) — par défaut. <code>exp = 2</code> = quadratique classique.</li>
  <li><strong>Exponentielle</strong> — explose. Pour max-level bas seulement.</li>
  <li><strong>Logarithmique</strong> — rendements décroissants, bon pour la maîtrise.</li>
  <li><strong>Fibonacci-like</strong> — récursive, croissance ~nombre d'or.</li>
</ul>

<h3>Pièges courants</h3>
<ul>
  <li><strong>Les exponentielles explosent vite.</strong> Les dépassements sont en rouge.</li>
  <li><strong>L'XP cumulé est la valeur de sauvegarde.</strong> Stocke le total, pas "XP du niveau courant".</li>
  <li><strong>Règle au feeling.</strong> La "bonne" courbe dépend du rythme et de la densité du contenu.</li>
</ul>
""",
        "it": """
<h2>A cosa serve?</h2>
<p>Calibrare la curva di esperienza è uno dei lavori più delicati nel design di un RPG. Troppo piatta e l'end-game non ha peso; troppo ripida e il giocatore sbatte contro un muro. Questo strumento genera la tabella XP-per-livello per cinque tipi canonici di curva per confrontarli affiancati e copiare il risultato nella config del motore senza rifare i conti.</p>

<h3>Tipi di curva</h3>
<ul>
  <li><strong>Lineare</strong> — delta costante. Grindy presto, banale tardi.</li>
  <li><strong>Polinomiale</strong> (<code>base × livello^exp</code>) — la default. <code>exp = 2</code> = quadratica classica.</li>
  <li><strong>Esponenziale</strong> — esplode. Solo per max-level bassi.</li>
  <li><strong>Logaritmica</strong> — rendimenti decrescenti, ideale per mastery.</li>
  <li><strong>Tipo Fibonacci</strong> — ricorsiva, crescita ~rapporto aureo.</li>
</ul>

<h3>Errori comuni</h3>
<ul>
  <li><strong>Le esponenziali esplodono in fretta.</strong> Gli overflow sono in rosso.</li>
  <li><strong>L'XP cumulativa è il valore di save.</strong> Salva il totale, non "XP nel livello corrente".</li>
  <li><strong>Calibra a sensazione.</strong> La curva "giusta" dipende dal ritmo degli incontri e dalla densità dei contenuti.</li>
</ul>
""",
        "pt": """
<h2>Para que serve?</h2>
<p>Ajustar a curva de experiência é um dos trabalhos mais delicados no design de RPG. Plana demais e o fim de jogo perde peso; íngreme demais e o player bate num muro. Esta ferramenta gera a tabela XP-por-nível para cinco tipos canônicos de curva pra você comparar lado a lado e copiar pro config do engine sem refazer a conta toda vez.</p>

<h3>Tipos de curva</h3>
<ul>
  <li><strong>Linear</strong> — delta constante. Grindy cedo, trivial tarde.</li>
  <li><strong>Polinomial</strong> (<code>base × nível^exp</code>) — o padrão. <code>exp = 2</code> = quadrática clássica.</li>
  <li><strong>Exponencial</strong> — explode. Só pra max-level baixo.</li>
  <li><strong>Logarítmica</strong> — retornos decrescentes, boa pra mastery.</li>
  <li><strong>Tipo Fibonacci</strong> — recursiva, crescimento ~razão áurea.</li>
</ul>

<h3>Cuidados comuns</h3>
<ul>
  <li><strong>Exponenciais explodem rápido.</strong> Overflows aparecem em vermelho.</li>
  <li><strong>O XP cumulativo é o valor de save.</strong> Armazene o total, não "XP no nível atual".</li>
  <li><strong>Ajuste no feeling.</strong> A curva "certa" depende do ritmo de encontros e da densidade do conteúdo.</li>
</ul>
""",
        "pl": """
<h2>Do czego to służy?</h2>
<p>Strojenie krzywej doświadczenia to jedna z bardziej delikatnych prac w designie RPG. Za płaska i late-game traci wagę; za stroma i gracz wali w ścianę. To narzędzie generuje tabelę XP-na-poziom dla pięciu kanonicznych typów krzywej, żebyś mógł porównać je obok siebie i wkleić wynik do configa silnika bez liczenia ręcznie za każdym razem.</p>

<h3>Typy krzywych</h3>
<ul>
  <li><strong>Liniowa</strong> — stały delta. Grindy wcześnie, trywialna późno.</li>
  <li><strong>Wielomianowa</strong> (<code>base × poziom^exp</code>) — domyślna. <code>exp = 2</code> = klasyczna kwadratowa.</li>
  <li><strong>Wykładnicza</strong> — eksploduje. Tylko dla niskiego max-levelu.</li>
  <li><strong>Logarytmiczna</strong> — malejące zwroty, dobre dla systemów mastery.</li>
  <li><strong>Fibonacci-style</strong> — rekurencyjna, wzrost ~złoty podział.</li>
</ul>

<h3>Częste pułapki</h3>
<ul>
  <li><strong>Wykładnicze wybuchają szybko.</strong> Overflow zaznaczony na czerwono.</li>
  <li><strong>Skumulowane XP to wartość save'u.</strong> Trzymaj total, nie "XP w obecnym poziomie".</li>
  <li><strong>Strojenie na czuja.</strong> "Właściwa" krzywa zależy od tempa encounterów i gęstości contentu.</li>
</ul>
""",
        "ja": """
<h2>用途</h2>
<p>経験値カーブの調整は RPG デザインで特にデリケートな作業です。フラットすぎると終盤が軽くなり、急すぎると壁に当たって離脱します。本ツールは 5 種類の代表的なカーブで「レベルごとの必要 XP」テーブルを生成し、並べて比較・形状確認・エンジン設定への貼り付けまで行えます。</p>

<h3>カーブの種類</h3>
<ul>
  <li><strong>線形</strong> — 一定の差。序盤グラインド、後半が軽い。</li>
  <li><strong>多項式</strong>（<code>base × level^exp</code>）— デフォルト。<code>exp = 2</code> は D&D／JRPG でおなじみの二次曲線。</li>
  <li><strong>指数</strong> — 爆発的に伸びる。最大レベルが低い設計向け。</li>
  <li><strong>対数</strong> — 逓減リターン。マスタリー系に向く。</li>
  <li><strong>フィボナッチ風</strong> — 再帰的、増加率は黄金比に近い。</li>
</ul>

<h3>よくある注意点</h3>
<ul>
  <li><strong>指数は急速にオーバーフローします。</strong> 表ではオーバーフロー値が赤で表示されます。</li>
  <li><strong>累計 XP がセーブ値です。</strong> 「現在レベル内の XP」ではなく、累計を保存してください。</li>
  <li><strong>調整は感覚で。</strong> 「正しい」カーブは戦闘ペースとコンテンツ密度次第です。</li>
</ul>
""",
        "nl": """
<h2>Waarvoor is dit?</h2>
<p>De XP-curve tunen is een van de delicatere klusjes in RPG-design. Te plat en de end-game heeft geen gewicht; te steil en de speler raakt een muur en stopt. Deze tool genereert de XP-per-level-tabel voor vijf canonieke curve-types zodat je ze naast elkaar kunt vergelijken en het resultaat in je engine-config kunt plakken zonder telkens te rekenen.</p>

<h3>Curve-types</h3>
<ul>
  <li><strong>Lineair</strong> — constante delta. Grindy vroeg, triviaal laat.</li>
  <li><strong>Polynomiaal</strong> (<code>base × level^exp</code>) — de default. <code>exp = 2</code> = klassieke kwadratisch.</li>
  <li><strong>Exponentieel</strong> — explodeert. Alleen voor lage max-level.</li>
  <li><strong>Logaritmisch</strong> — afnemende returns, goed voor mastery.</li>
  <li><strong>Fibonacci-achtig</strong> — recursief, groei ~gulden snede.</li>
</ul>

<h3>Veelvoorkomende valkuilen</h3>
<ul>
  <li><strong>Exponentiëlen ontploffen snel.</strong> Overflows worden rood weergegeven.</li>
  <li><strong>Cumulatieve XP is de save-waarde.</strong> Sla het totaal op, niet "XP in huidig level".</li>
  <li><strong>Tunen op gevoel.</strong> De "juiste" curve hangt af van encounter-pacing en content-dichtheid.</li>
</ul>
""",
        "tr": """
<h2>Bu ne işe yarar?</h2>
<p>XP eğrisini ayarlamak RPG tasarımının en hassas işlerinden biri. Çok düz olursa oyun sonu ağırlığını kaybeder; çok dik olursa oyuncu duvara çarpar. Bu araç beş kanonik eğri tipi için seviye başına XP tablosunu üretir; yan yana karşılaştır, şekli gözle ve sonucu motor config'ine her sabit ayarlamada matematik yapmadan kopyala.</p>

<h3>Eğri tipleri</h3>
<ul>
  <li><strong>Lineer</strong> — sabit delta. Erken grind, geç önemsiz.</li>
  <li><strong>Polinom</strong> (<code>base × seviye^exp</code>) — varsayılan. <code>exp = 2</code> = klasik karesel.</li>
  <li><strong>Üstel</strong> — patlar. Sadece düşük max-level için.</li>
  <li><strong>Logaritmik</strong> — azalan getiri, mastery için iyi.</li>
  <li><strong>Fibonacci benzeri</strong> — özyinelemeli, ~altın oran hızında büyür.</li>
</ul>

<h3>Sık yapılan hatalar</h3>
<ul>
  <li><strong>Üsteller çabuk patlar.</strong> Taşma değerleri kırmızı.</li>
  <li><strong>Kümülatif XP save değeridir.</strong> Toplamı sakla, "mevcut seviyedeki XP" değil.</li>
  <li><strong>Hisle ayarla.</strong> "Doğru" eğri karşılaşma ritmine ve içerik yoğunluğuna bağlı.</li>
</ul>
""",
        "id": """
<h2>Untuk apa ini?</h2>
<p>Tuning kurva XP adalah salah satu pekerjaan tersulit dalam desain RPG. Terlalu datar dan late-game tidak punya bobot; terlalu curam dan player hits a wall lalu berhenti. Tool ini menghasilkan tabel XP per level untuk lima jenis kurva kanonik supaya bisa kamu bandingkan berdampingan, lihat bentuknya, dan salin hasilnya ke config engine tanpa hitung manual setiap kali ubah konstanta.</p>

<h3>Jenis kurva</h3>
<ul>
  <li><strong>Linear</strong> — delta konstan. Grindy di awal, sepele di akhir.</li>
  <li><strong>Polinomial</strong> (<code>base × level^exp</code>) — default. <code>exp = 2</code> = kuadratik klasik.</li>
  <li><strong>Eksponensial</strong> — meledak. Hanya untuk max-level rendah.</li>
  <li><strong>Logaritmik</strong> — diminishing returns, cocok untuk mastery.</li>
  <li><strong>Mirip Fibonacci</strong> — rekursif, pertumbuhan ~golden ratio.</li>
</ul>

<h3>Kesalahan umum</h3>
<ul>
  <li><strong>Eksponensial meledak cepat.</strong> Nilai overflow ditampilkan merah.</li>
  <li><strong>XP kumulatif adalah nilai save.</strong> Simpan total, bukan "XP di level sekarang".</li>
  <li><strong>Tuning by feel.</strong> Kurva "yang benar" tergantung tempo encounter dan kepadatan konten.</li>
</ul>
""",
        "vi": """
<h2>Công cụ này để làm gì?</h2>
<p>Tinh chỉnh đường cong kinh nghiệm là một trong những công việc tinh tế nhất trong thiết kế RPG. Quá phẳng thì cuối game không có sức nặng; quá dốc thì người chơi đập vào tường và bỏ cuộc. Tool này sinh bảng XP theo cấp cho năm loại đường cong cổ điển để bạn so sánh song song, ước lượng hình dạng và sao chép kết quả vào config engine mà không phải tính tay mỗi lần đổi hằng số.</p>

<h3>Các loại đường cong</h3>
<ul>
  <li><strong>Tuyến tính</strong> — delta hằng số. Grindy sớm, tầm thường muộn.</li>
  <li><strong>Đa thức</strong> (<code>base × level^exp</code>) — mặc định. <code>exp = 2</code> = bậc hai cổ điển.</li>
  <li><strong>Hàm mũ</strong> — bùng nổ. Chỉ dùng cho max-level thấp.</li>
  <li><strong>Logarit</strong> — lợi nhuận giảm dần, hợp với hệ mastery.</li>
  <li><strong>Kiểu Fibonacci</strong> — đệ quy, tăng trưởng ~tỉ lệ vàng.</li>
</ul>

<h3>Lưu ý thường gặp</h3>
<ul>
  <li><strong>Hàm mũ vọt rất nhanh.</strong> Giá trị tràn được tô đỏ.</li>
  <li><strong>XP tích lũy là giá trị save.</strong> Lưu tổng, không phải "XP trong level hiện tại".</li>
  <li><strong>Chỉnh theo cảm giác.</strong> Đường cong "đúng" phụ thuộc nhịp đụng độ và mật độ nội dung.</li>
</ul>
""",
        "hi": """
<h2>यह किसके लिए है?</h2>
<p>Experience curve को tune करना RPG design के सबसे delicate कामों में से एक है। बहुत flat तो late-game में कोई weight नहीं; बहुत steep तो player wall से टकराकर quit कर देता है। यह tool पांच canonical curve types के लिए XP-per-level table generate करता है ताकि आप side-by-side compare कर सकें, shape देख सकें, और हर बार maths किए बिना result को engine config में copy कर सकें।</p>

<h3>Curve types</h3>
<ul>
  <li><strong>Linear</strong> — constant delta। Early grindy, late trivial।</li>
  <li><strong>Polynomial</strong> (<code>base × level^exp</code>) — default। <code>exp = 2</code> classic quadratic है।</li>
  <li><strong>Exponential</strong> — uncontrollable। केवल short max-level games के लिए।</li>
  <li><strong>Logarithmic</strong> — diminishing returns, mastery systems के लिए useful।</li>
  <li><strong>Fibonacci-like</strong> — recursive, golden-ratio rate के साथ बढ़ता है।</li>
</ul>

<h3>आम गलतियाँ</h3>
<ul>
  <li><strong>Exponential जल्दी blow up हो जाता है.</strong> Overflow values लाल में दिखती हैं।</li>
  <li><strong>Cumulative XP save value है.</strong> Total store करें, "XP into current level" नहीं।</li>
  <li><strong>Feel से tune करें।</strong> "Right" curve encounter pacing और content density पर निर्भर करती है।</li>
</ul>
""",
        "sk": """
<h2>Načo to slúži?</h2>
<p>Ladenie XP krivky je jedna z najjemnejších úloh v RPG dizajne. Príliš plochá a hra nemá záver; príliš strmá a hráč narazí do steny a skončí. Tento nástroj vygeneruje tabuľku XP na úroveň pre päť kanonických typov krivky, aby si ich vedel porovnať vedľa seba a skopírovať výsledok do enginu bez ručného počítania.</p>

<h3>Typy kriviek</h3>
<ul>
  <li><strong>Lineárna</strong> — konštantný delta. Skoro grindy, neskôr triviálna.</li>
  <li><strong>Polynomická</strong> (<code>base × level^exp</code>) — default. <code>exp = 2</code> = klasická kvadratická.</li>
  <li><strong>Exponenciálna</strong> — exploduje. Len pre nízky max-level.</li>
  <li><strong>Logaritmická</strong> — klesajúce výnosy, vhodné pre mastery systémy.</li>
  <li><strong>Fibonacci-style</strong> — rekurzívna, rast ~zlatý rez.</li>
</ul>

<h3>Časté chyby</h3>
<ul>
  <li><strong>Exponenciálne krivky rýchlo pretečú.</strong> Pretečené hodnoty sú červené.</li>
  <li><strong>Kumulatívne XP je hodnota do save.</strong> Ulož celkové, nie „XP v aktuálnej úrovni".</li>
  <li><strong>Ladiť podľa pocitu.</strong> „Správna" krivka závisí od tempa stretov a hustoty obsahu.</li>
</ul>
""",
        "cs": """
<h2>K čemu to slouží?</h2>
<p>Ladění XP křivky je jedna z nejjemnějších prací v RPG designu. Příliš plochá a hra nemá závěr; příliš strmá a hráč narazí do zdi a skončí. Tenhle nástroj vygeneruje tabulku XP na úroveň pro pět kanonických typů křivky, abys je mohl porovnat vedle sebe a zkopírovat výsledek do enginu bez ručního počítání.</p>

<h3>Typy křivek</h3>
<ul>
  <li><strong>Lineární</strong> — konstantní delta. Brzy grindy, pozdě triviální.</li>
  <li><strong>Polynomická</strong> (<code>base × level^exp</code>) — default. <code>exp = 2</code> = klasická kvadratická.</li>
  <li><strong>Exponenciální</strong> — exploduje. Jen pro nízký max-level.</li>
  <li><strong>Logaritmická</strong> — klesající výnosy, vhodné pro mastery systémy.</li>
  <li><strong>Fibonacci-style</strong> — rekurzivní, růst ~zlatý řez.</li>
</ul>

<h3>Časté chyby</h3>
<ul>
  <li><strong>Exponenciální křivky rychle přetečou.</strong> Přetečené hodnoty jsou červené.</li>
  <li><strong>Kumulativní XP je hodnota do save.</strong> Ulož celkové, ne „XP v aktuální úrovni".</li>
  <li><strong>Ladit podle pocitu.</strong> „Správná" křivka závisí na tempu střetů a hustotě obsahu.</li>
</ul>
""",
    },
    "related": ["dice-roller", "damage-calculator", "color-blind-sim"],
    "howto": {"flow": "generate", "action": "format", "noun": "XP curve"},
}
