TOOL = {
    "slug": "citation-formatter",
    "category": "text",
    "icon": "📚",
    "tags": ["citation", "apa", "mla", "chicago", "harvard", "bibliography", "reference", "academic"],
    "i18n": {
        "en": {
            "name": "Citation Formatter",
            "tagline": "Format citations in APA 7, MLA 9, Chicago, and Harvard styles. Enter source fields manually — no DOI lookups, no tracking. Browser-only.",
            "description": "Free citation formatter for academic writing. Pick the source type (book / journal article / website / chapter / conference paper / thesis / news article / report), enter the fields (authors, title, year, publisher, etc.), pick the style (APA 7, MLA 9, Chicago author-date, Harvard), and copy the formatted citation. Bibliography mode lets you build a sorted reference list. No external lookups, no DOI APIs, no tracking — works offline once loaded.",
        },
        "de": {"name": "Zitations-Formatierer", "tagline": "Zitate in APA 7, MLA 9, Chicago und Harvard formatieren. Felder selbst eintragen — keine DOI-Abfragen, kein Tracking. Nur im Browser.", "description": "Kostenloser Zitations-Formatierer für wissenschaftliche Arbeiten. Quellentyp wählen (Buch / Zeitschriftenartikel / Webseite / Buchkapitel / Konferenzbeitrag / Abschlussarbeit / Zeitungsartikel / Bericht), Felder ausfüllen (Autor:innen, Titel, Jahr, Verlag …), Stil auswählen (APA 7, MLA 9, Chicago Autor-Datum, Harvard) und die formatierte Quellenangabe kopieren. Im Literaturlisten-Modus baust du eine sortierte Liste. Keine externen Abfragen, keine DOI-API, kein Tracking — funktioniert offline."},
        "es": {"name": "Formateador de Citas", "tagline": "Formatea citas en APA 7, MLA 9, Chicago y Harvard. Introduce los campos a mano — sin búsquedas DOI, sin tracking. Solo en el navegador.", "description": "Formateador de citas gratuito para textos académicos. Elige el tipo de fuente (libro / artículo de revista / web / capítulo / paper de congreso / tesis / artículo de prensa / informe), rellena los campos (autores, título, año, editorial…), elige el estilo (APA 7, MLA 9, Chicago autor-fecha, Harvard) y copia la cita formateada. El modo bibliografía te arma una lista ordenada. Sin búsquedas externas, sin DOI APIs, sin tracking — funciona offline tras la primera carga."},
        "fr": {"name": "Formateur de Citations", "tagline": "Formatez vos citations en APA 7, MLA 9, Chicago et Harvard. Saisie manuelle des champs — pas d'appel DOI, pas de tracking. 100 % navigateur.", "description": "Formateur de citations gratuit pour la rédaction universitaire. Choisissez le type de source (livre / article de revue / site web / chapitre / acte de colloque / mémoire ou thèse / article de presse / rapport), remplissez les champs (auteurs, titre, année, éditeur…), choisissez le style (APA 7, MLA 9, Chicago auteur-date, Harvard) et copiez la référence formatée. Le mode bibliographie construit une liste triée. Aucun appel externe, aucune API DOI, aucun tracking — fonctionne hors-ligne après chargement."},
        "it": {"name": "Formattatore di Citazioni", "tagline": "Formatta citazioni in APA 7, MLA 9, Chicago e Harvard. Inserisci i campi a mano — niente lookup DOI, niente tracking. Solo nel browser.", "description": "Formattatore di citazioni gratuito per la scrittura accademica. Scegli il tipo di fonte (libro / articolo di rivista / sito web / capitolo / atto di convegno / tesi / articolo di giornale / rapporto), compila i campi (autori, titolo, anno, editore…), scegli lo stile (APA 7, MLA 9, Chicago autore-data, Harvard) e copia la citazione formattata. La modalità bibliografia compone un elenco ordinato. Nessuna chiamata esterna, nessuna API DOI, nessun tracking — offline dopo il primo caricamento."},
        "pt": {"name": "Formatador de Citações", "tagline": "Formate citações em APA 7, MLA 9, Chicago e Harvard. Preencha os campos manualmente — sem buscar DOI, sem tracking. Tudo no navegador.", "description": "Formatador de citações gratuito para escrita acadêmica. Escolha o tipo de fonte (livro / artigo de periódico / site / capítulo / paper de congresso / tese ou dissertação / matéria de jornal / relatório), preencha os campos (autores, título, ano, editora…), escolha o estilo (APA 7, MLA 9, Chicago autor-data, Harvard) e copie a citação pronta. Modo bibliografia monta uma lista ordenada. Sem buscas externas, sem APIs DOI, sem rastreamento — funciona offline depois de carregar."},
        "pl": {"name": "Formater Cytowań", "tagline": "Formatuj cytowania w stylach APA 7, MLA 9, Chicago i Harvard. Wpisuj pola ręcznie — bez wyszukiwania DOI, bez śledzenia. Tylko w przeglądarce.", "description": "Darmowy formater cytowań dla pisania akademickiego. Wybierz typ źródła (książka / artykuł w czasopiśmie / strona WWW / rozdział / referat konferencyjny / praca dyplomowa / artykuł prasowy / raport), wpisz pola (autorzy, tytuł, rok, wydawca…), wybierz styl (APA 7, MLA 9, Chicago autor-data, Harvard) i skopiuj gotowy zapis. Tryb bibliografii buduje posortowaną listę. Brak zewnętrznych zapytań, brak API DOI, brak śledzenia — działa offline po pierwszym załadowaniu."},
        "ja": {"name": "引用フォーマッター", "tagline": "APA 7、MLA 9、Chicago、Harvard 形式で引用を整形。フィールドは手入力 — DOI 検索なし、トラッキングなし。ブラウザのみで動作。", "description": "学術ライティング向けの無料引用フォーマッター。資料種別（書籍／学術論文／ウェブサイト／章／会議録／学位論文／新聞記事／報告書）を選び、フィールド（著者・タイトル・発行年・出版社など）を入力し、スタイル（APA 7／MLA 9／Chicago 著者-年／Harvard）を選んで整形済みの引用をコピーできます。文献リストモードでは並べ替え済みの一覧を生成。外部参照なし、DOI API なし、トラッキングなし — 一度読み込めばオフラインで動作します。"},
        "nl": {"name": "Citatie-formatter", "tagline": "Formatteer citaties in APA 7, MLA 9, Chicago en Harvard. Vul de velden zelf in — geen DOI-lookups, geen tracking. Alles in de browser.", "description": "Gratis citatie-formatter voor academisch schrijven. Kies het brontype (boek / tijdschriftartikel / website / hoofdstuk / conferentiebijdrage / scriptie of proefschrift / krantenartikel / rapport), vul de velden in (auteurs, titel, jaar, uitgever…), kies de stijl (APA 7, MLA 9, Chicago auteur-datum, Harvard) en kopieer de geformatteerde verwijzing. In bibliografiemodus bouw je een gesorteerde lijst. Geen externe lookups, geen DOI-API, geen tracking — werkt offline na de eerste keer laden."},
        "tr": {"name": "Kaynakça Biçimleyici", "tagline": "APA 7, MLA 9, Chicago ve Harvard stillerinde kaynak biçimle. Alanları kendin gir — DOI sorgusu yok, izleme yok. Tamamen tarayıcıda.", "description": "Akademik yazım için ücretsiz kaynakça biçimleyici. Kaynak türünü seç (kitap / makale / web sitesi / kitap bölümü / konferans bildirisi / tez / haber / rapor), alanları doldur (yazarlar, başlık, yıl, yayınevi…), stili seç (APA 7, MLA 9, Chicago yazar-tarih, Harvard) ve biçimlenmiş kaynağı kopyala. Kaynakça modu sıralı bir liste üretir. Dış sorgu yok, DOI API yok, izleme yok — ilk yüklemeden sonra çevrimdışı da çalışır."},
        "id": {"name": "Formatter Sitasi", "tagline": "Format sitasi dalam gaya APA 7, MLA 9, Chicago, dan Harvard. Isi field manual — tanpa lookup DOI, tanpa tracking. Sepenuhnya di browser.", "description": "Formatter sitasi gratis untuk penulisan akademik. Pilih tipe sumber (buku / artikel jurnal / website / bab buku / makalah konferensi / tesis / artikel berita / laporan), isi field-nya (penulis, judul, tahun, penerbit…), pilih gaya (APA 7, MLA 9, Chicago penulis-tahun, Harvard), lalu salin sitasi yang sudah diformat. Mode daftar pustaka membangun list terurut. Tanpa lookup eksternal, tanpa API DOI, tanpa tracking — jalan offline setelah pertama kali dimuat."},
        "vi": {"name": "Trình tạo Trích dẫn", "tagline": "Định dạng trích dẫn theo APA 7, MLA 9, Chicago và Harvard. Tự nhập trường — không tra DOI, không theo dõi. Hoạt động trong trình duyệt.", "description": "Công cụ định dạng trích dẫn miễn phí cho viết học thuật. Chọn loại nguồn (sách / bài báo tạp chí / website / chương sách / kỷ yếu hội thảo / luận văn / bài báo tin tức / báo cáo), nhập các trường (tác giả, tiêu đề, năm, nhà xuất bản…), chọn phong cách (APA 7, MLA 9, Chicago tác giả-năm, Harvard) và sao chép trích dẫn đã định dạng. Chế độ thư mục tạo danh sách đã sắp xếp. Không tra cứu bên ngoài, không API DOI, không theo dõi — chạy ngoại tuyến sau khi tải lần đầu."},
        "hi": {"name": "Citation Formatter", "tagline": "APA 7, MLA 9, Chicago और Harvard styles में citations format करें। Fields manually भरें — कोई DOI lookup नहीं, कोई tracking नहीं। Browser-only।", "description": "Academic writing के लिए मुफ़्त citation formatter। Source type चुनें (book / journal article / website / chapter / conference paper / thesis / news article / report), fields भरें (authors, title, year, publisher आदि), style चुनें (APA 7, MLA 9, Chicago author-date, Harvard) और formatted citation copy करें। Bibliography mode एक sorted list बनाता है। कोई external lookup नहीं, कोई DOI API नहीं, कोई tracking नहीं — पहले load के बाद offline भी चलता है।"},
        "sk": {"name": "Formátovač citácií", "tagline": "Formátuj citácie v štýloch APA 7, MLA 9, Chicago a Harvard. Polia zadávaj ručne — žiadne DOI vyhľadávanie, žiadne sledovanie. Iba v prehliadači.", "description": "Bezplatný formátovač citácií pre akademické písanie. Vyber typ zdroja (kniha / článok v časopise / web / kapitola / konferenčný príspevok / diplomová alebo dizertačná práca / spravodajský článok / správa), vypĺňaj polia (autori, názov, rok, vydavateľ…), vyber štýl (APA 7, MLA 9, Chicago autor-rok, Harvard) a skopíruj naformátovaný záznam. Režim bibliografie poskladá utriedený zoznam. Žiadne externé vyhľadávanie, žiadne DOI API, žiadne sledovanie — po prvom načítaní funguje offline."},
        "cs": {"name": "Formátovač citací", "tagline": "Formátuj citace ve stylech APA 7, MLA 9, Chicago a Harvard. Pole zadávej ručně — žádné DOI vyhledávání, žádné sledování. Jen v prohlížeči.", "description": "Bezplatný formátovač citací pro akademické psaní. Vyber typ zdroje (kniha / článek v časopise / web / kapitola / konferenční příspěvek / diplomová nebo disertační práce / novinový článek / zpráva), vyplň pole (autoři, název, rok, vydavatel…), vyber styl (APA 7, MLA 9, Chicago autor-rok, Harvard) a zkopíruj naformátovaný záznam. Režim bibliografie sestaví seřazený seznam. Žádné externí dotazy, žádné DOI API, žádné sledování — po prvním načtení funguje offline."},
    },
    "body": """
<div class="tool-card">
  <label>Style</label>
  <div class="button-row" id="cf-styles" role="tablist">
    <button type="button" class="cf-style-btn" data-style="apa">APA 7</button>
    <button type="button" class="cf-style-btn" data-style="mla">MLA 9</button>
    <button type="button" class="cf-style-btn" data-style="chicago">Chicago</button>
    <button type="button" class="cf-style-btn" data-style="harvard">Harvard</button>
  </div>
  <div class="meta" style="margin-top:0.5rem">Authors: enter one per line as <code>Last, First Middle</code>. Multiple authors → multiple lines.</div>
</div>

<div class="tool-card">
  <div id="cf-sources"></div>
  <div class="button-row" style="margin-top:0.6rem">
    <button class="secondary" type="button" onclick="cfAddSource()">+ Add source</button>
    <button class="secondary" type="button" onclick="cfSort()">Sort A→Z</button>
  </div>
</div>

<div class="tool-card">
  <label>Bibliography preview</label>
  <div id="cf-preview" class="cf-preview"></div>
</div>

<div class="tool-card">
  <label>Plain text bibliography</label>
  <div class="output-row">
    <pre class="output" id="cf-out" style="margin:0;flex:1">{LBL_NO_INPUT}</pre>
    <button class="copy-btn secondary" onclick="copyOutput('cf-out', this)">{LBL_COPY}</button>
  </div>
</div>
""",
    "script": """
<style>
.cf-style-btn{background:var(--bg-elev);border:1px solid var(--border);color:var(--text);padding:0.4rem 0.9rem;border-radius:6px;cursor:pointer;font-size:0.9rem}
.cf-style-btn.active{background:var(--accent);color:#fff;border-color:var(--accent)}
.cf-style-btn:hover:not(.active){border-color:var(--accent)}
#cf-sources{display:flex;flex-direction:column;gap:0.6rem}
.cf-card{border:1px solid var(--border);border-radius:8px;padding:0.8rem;background:var(--bg-elev)}
.cf-card-head{display:flex;gap:0.5rem;align-items:center;margin-bottom:0.6rem}
.cf-card-head select{flex:1}
.cf-card-head .cf-del{background:transparent;border:1px solid var(--border);border-radius:4px;padding:0.3rem 0.6rem;color:var(--text-muted);cursor:pointer;font-size:0.85rem}
.cf-card-head .cf-del:hover{color:#dc2626;border-color:#dc2626}
.cf-fields{display:grid;grid-template-columns:repeat(auto-fit, minmax(200px, 1fr));gap:0.5rem 0.7rem}
.cf-fields .cf-wide{grid-column:1/-1}
.cf-fields label{font-size:0.78rem;color:var(--text-muted);margin-bottom:0.15rem;display:block}
.cf-fields input,.cf-fields textarea{font-size:0.88rem}
.cf-fields textarea{min-height:56px;font-family:inherit}
.cf-rendered{padding:0.5rem 0.7rem;background:var(--surface);border:1px solid var(--border);border-radius:6px;margin-top:0.6rem;font-size:0.92rem;line-height:1.5}
.cf-rendered i{font-style:italic}
.cf-preview{padding:0.8rem;background:var(--surface);border:1px solid var(--border);border-radius:8px;line-height:1.55;font-size:0.92rem}
.cf-preview .cf-entry{padding-left:1.5em;text-indent:-1.5em;margin:0.4em 0}
.cf-preview .cf-empty{color:var(--text-muted)}
</style>
<script>
const CF_STYLE_LABELS = { apa: 'APA 7', mla: 'MLA 9', chicago: 'Chicago', harvard: 'Harvard' };
let CF_STYLE = 'apa';
const CF_TYPE_LABELS = {
  book: 'Book',
  'journal-article': 'Journal article',
  website: 'Website',
  chapter: 'Book chapter',
  conference: 'Conference paper',
  thesis: 'Thesis / dissertation',
  news: 'News article',
  report: 'Report'
};
// Fields per source type. Each entry: [key, label, wide?, placeholder?]
const CF_FIELDS = {
  book: [
    ['authors','Authors (one per line: Last, First)',true,'Smith, John\\nDoe, Jane M.'],
    ['year','Year',false,'2023'],
    ['title','Title',true,'The Origins of Civilization'],
    ['edition','Edition (optional)',false,'2nd ed.'],
    ['place','Publisher location (optional)',false,'New York'],
    ['publisher','Publisher',false,'Routledge'],
    ['doi','DOI or URL (optional)',true,'https://doi.org/10.xxxx/xxxx']
  ],
  'journal-article': [
    ['authors','Authors',true,''],
    ['year','Year',false,'2022'],
    ['title','Article title',true,'Mapping the impact of remote learning'],
    ['journal','Journal name',true,'Journal of Educational Research'],
    ['volume','Volume',false,'45'],
    ['issue','Issue (optional)',false,'3'],
    ['pages','Pages',false,'215-238'],
    ['doi','DOI or URL (optional)',true,'']
  ],
  website: [
    ['authors','Author or organization',true,''],
    ['year','Year',false,'2024'],
    ['title','Page title',true,''],
    ['site','Site name',false,''],
    ['url','URL',true,'https://example.org/page'],
    ['accessed','Accessed date (optional)',false,'12 March 2025']
  ],
  chapter: [
    ['authors','Chapter authors',true,''],
    ['year','Year',false,''],
    ['title','Chapter title',true,''],
    ['editors','Book editors (one per line)',true,''],
    ['booktitle','Book title',true,''],
    ['edition','Edition (optional)',false,''],
    ['pages','Pages',false,''],
    ['place','Publisher location (optional)',false,''],
    ['publisher','Publisher',false,''],
    ['doi','DOI or URL (optional)',true,'']
  ],
  conference: [
    ['authors','Authors',true,''],
    ['year','Year',false,''],
    ['title','Paper title',true,''],
    ['conference','Conference / proceedings name',true,''],
    ['pages','Pages (optional)',false,''],
    ['place','Location (optional)',false,''],
    ['publisher','Publisher (optional)',false,''],
    ['doi','DOI or URL (optional)',true,'']
  ],
  thesis: [
    ['authors','Author',true,''],
    ['year','Year',false,''],
    ['title','Title',true,''],
    ['kind','Type',false,'PhD dissertation'],
    ['institution','Institution',true,''],
    ['url','URL (optional)',true,'']
  ],
  news: [
    ['authors','Author(s)',true,''],
    ['date','Date (e.g. 12 March 2024)',false,''],
    ['year','Year',false,''],
    ['title','Headline',true,''],
    ['publication','Publication',false,''],
    ['url','URL (optional)',true,'']
  ],
  report: [
    ['authors','Authors / organization',true,''],
    ['year','Year',false,''],
    ['title','Report title',true,''],
    ['number','Report number (optional)',false,''],
    ['institution','Issuing institution',true,''],
    ['url','URL (optional)',true,'']
  ]
};

let CF_SRC = [];   // array of {type, fields:{key:value}}
let CF_NEXT_ID = 1;

function cfEsc(s){return String(s ?? '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}
function cfStripTags(s){return String(s ?? '').replace(/<[^>]*>/g,'');}

function cfParseAuthors(raw){
  if(!raw) return [];
  return raw.split(/\\n|;/).map(l => l.trim()).filter(Boolean).map(line => {
    // "Last, First Middle" or "First Middle Last"
    if(line.includes(',')){
      const [last, rest] = line.split(/,(.+)/);
      const names = (rest||'').trim().split(/\\s+/).filter(Boolean);
      return { last: last.trim(), first: names[0]||'', middle: names.slice(1).join(' ') };
    }
    const parts = line.trim().split(/\\s+/);
    if(parts.length === 1) return { last: parts[0], first: '', middle: '', org: true };
    const last = parts.pop();
    return { last, first: parts[0], middle: parts.slice(1).join(' ') };
  });
}

function cfInitials(first, middle, dot=true){
  const seg = (first||'') + ' ' + (middle||'');
  return seg.trim().split(/[\\s-]+/).filter(Boolean).map(w => w[0].toUpperCase() + (dot?'.':'')).join(' ');
}

function cfAuthorsAPA(authors){
  if(!authors.length) return '';
  const fmt = a => a.first ? `${a.last}, ${cfInitials(a.first, a.middle)}` : a.last;
  if(authors.length === 1) return fmt(authors[0]);
  if(authors.length === 2) return `${fmt(authors[0])}, & ${fmt(authors[1])}`;
  if(authors.length <= 20){
    return authors.slice(0,-1).map(fmt).join(', ') + ', & ' + fmt(authors[authors.length-1]);
  }
  return authors.slice(0,19).map(fmt).join(', ') + ', ... ' + fmt(authors[authors.length-1]);
}

function cfAuthorsMLA(authors){
  if(!authors.length) return '';
  const full = a => a.first ? `${a.last}, ${a.first}${a.middle? ' '+a.middle : ''}` : a.last;
  const inv  = a => a.first ? `${a.first}${a.middle? ' '+a.middle : ''} ${a.last}` : a.last;
  if(authors.length === 1) return full(authors[0]);
  if(authors.length === 2) return `${full(authors[0])}, and ${inv(authors[1])}`;
  return `${full(authors[0])}, et al.`;
}

function cfAuthorsChicago(authors){
  if(!authors.length) return '';
  const full = a => a.first ? `${a.last}, ${a.first}${a.middle? ' '+a.middle : ''}` : a.last;
  const inv  = a => a.first ? `${a.first}${a.middle? ' '+a.middle : ''} ${a.last}` : a.last;
  if(authors.length === 1) return full(authors[0]);
  if(authors.length <= 3){
    const tail = authors.slice(1).map(inv);
    if(tail.length === 1) return `${full(authors[0])}, and ${tail[0]}`;
    return `${full(authors[0])}, ${tail.slice(0,-1).join(', ')}, and ${tail[tail.length-1]}`;
  }
  // 4+ authors → first author + et al. (author-date style)
  return `${full(authors[0])} et al.`;
}

function cfAuthorsHarvard(authors){
  if(!authors.length) return '';
  const fmt = a => a.first ? `${a.last}, ${cfInitials(a.first, a.middle)}` : a.last;
  if(authors.length === 1) return fmt(authors[0]);
  if(authors.length <= 3){
    return authors.slice(0,-1).map(fmt).join(', ') + ' and ' + fmt(authors[authors.length-1]);
  }
  return `${fmt(authors[0])} et al.`;
}

function cfTitleCase(s){
  if(!s) return '';
  const small = new Set(['a','an','and','as','at','but','by','for','if','in','nor','of','on','or','so','the','to','up','yet','vs','via']);
  const words = s.split(/(\\s+)/);
  return words.map((w, i) => {
    if(/^\\s+$/.test(w)) return w;
    const lw = w.toLowerCase();
    const isFirst = words.slice(0,i).every(x => /^\\s+$/.test(x));
    const isLast  = words.slice(i+1).every(x => /^\\s+$/.test(x));
    if(!isFirst && !isLast && small.has(lw)) return lw;
    return lw.charAt(0).toUpperCase() + lw.slice(1);
  }).join('');
}

function cfSentenceCase(s){
  if(!s) return '';
  const trimmed = s.trim();
  // Lowercase everything except first letter and after colons/question marks
  let out = trimmed.toLowerCase();
  out = out.charAt(0).toUpperCase() + out.slice(1);
  out = out.replace(/([.:?!]\\s+)([a-z])/g, (_, sep, ch) => sep + ch.toUpperCase());
  return out;
}

// Italic helper: returns wrapped <i>...</i> for HTML render, the same for plain (we'll strip later)
function it(s){ return s ? `<i>${cfEsc(s)}</i>` : ''; }
function tx(s){ return cfEsc(s); }

// ---------- APA 7 ----------
function cfFormatAPA(src){
  const f = src.fields;
  const authors = cfParseAuthors(f.authors||'');
  const A = cfAuthorsAPA(authors);
  const yr = f.year ? ` (${tx(f.year)}).` : ' (n.d.).';
  let out = '';
  switch(src.type){
    case 'book': {
      const ed = f.edition ? ` (${tx(f.edition)})` : '';
      const pub = [f.place, f.publisher].filter(Boolean).map(tx).join(': ') || tx(f.publisher);
      out = `${tx(A)}${yr} ${it(cfSentenceCase(f.title||''))}${ed}. ${pub}.`;
      if(f.doi) out += ` ${tx(f.doi)}`;
      break;
    }
    case 'journal-article': {
      const issue = f.issue ? `(${tx(f.issue)})` : '';
      out = `${tx(A)}${yr} ${tx(cfSentenceCase(f.title||''))}. ${it(f.journal||'')}, ${it(f.volume||'')}${issue}${f.pages? ', '+tx(f.pages) : ''}.`;
      if(f.doi) out += ` ${tx(f.doi)}`;
      break;
    }
    case 'website': {
      const site = f.site ? `. ${tx(f.site)}` : '';
      const acc = f.accessed ? ` Retrieved ${tx(f.accessed)}, from` : '';
      out = `${tx(A)}${yr} ${it(cfSentenceCase(f.title||''))}${site}.${acc} ${tx(f.url||'')}`.trim();
      if(!f.url) out = out.replace(/\\s+$/,'');
      break;
    }
    case 'chapter': {
      const editors = cfParseAuthors(f.editors||'');
      const edStr = editors.length ? cfAuthorsAPA(editors).replace(/, &/g, ', &') : '';
      const edition = f.edition ? `, ${tx(f.edition)}` : '';
      const pp = f.pages ? `, pp. ${tx(f.pages)}` : '';
      const pub = [f.place, f.publisher].filter(Boolean).map(tx).join(': ') || tx(f.publisher);
      out = `${tx(A)}${yr} ${tx(cfSentenceCase(f.title||''))}. In ${tx(edStr)}${edStr?' (Eds.), ':''}${it(f.booktitle||'')}${edition? ' ('+tx(edition.trim().replace(/^,\\s*/,''))+pp+')' : pp? ' ('+tx(pp.trim().replace(/^,\\s*/,''))+')' : ''}. ${pub}.`;
      if(f.doi) out += ` ${tx(f.doi)}`;
      break;
    }
    case 'conference': {
      const pp = f.pages ? `, pp. ${tx(f.pages)}` : '';
      const pub = [f.place, f.publisher].filter(Boolean).map(tx).join(': ');
      out = `${tx(A)}${yr} ${tx(cfSentenceCase(f.title||''))}. In ${it(f.conference||'')}${pp}.${pub? ' '+pub+'.' : ''}`;
      if(f.doi) out += ` ${tx(f.doi)}`;
      break;
    }
    case 'thesis': {
      const kind = f.kind || 'Doctoral dissertation';
      out = `${tx(A)}${yr} ${it(cfSentenceCase(f.title||''))} [${tx(kind)}, ${tx(f.institution||'')}].`;
      if(f.url) out += ` ${tx(f.url)}`;
      break;
    }
    case 'news': {
      const date = f.date ? ` (${tx(f.date)}).` : yr;
      out = `${tx(A)}${date} ${tx(cfSentenceCase(f.title||''))}. ${it(f.publication||'')}.`;
      if(f.url) out += ` ${tx(f.url)}`;
      break;
    }
    case 'report': {
      const num = f.number ? ` (${tx(f.number)})` : '';
      out = `${tx(A)}${yr} ${it(cfSentenceCase(f.title||''))}${num}. ${tx(f.institution||'')}.`;
      if(f.url) out += ` ${tx(f.url)}`;
      break;
    }
  }
  return out;
}

// ---------- MLA 9 ----------
function cfFormatMLA(src){
  const f = src.fields;
  const authors = cfParseAuthors(f.authors||'');
  const A = cfAuthorsMLA(authors);
  switch(src.type){
    case 'book': {
      const ed = f.edition ? `${tx(f.edition)}, ` : '';
      const pub = tx(f.publisher||'');
      return `${tx(A)}${A?'. ':''}${it(cfTitleCase(f.title||''))}. ${ed}${pub}${f.year? ', '+tx(f.year) : ''}.`;
    }
    case 'journal-article': {
      const vol = f.volume ? `vol. ${tx(f.volume)}` : '';
      const iss = f.issue ? `no. ${tx(f.issue)}` : '';
      const yr = f.year ? tx(f.year) : '';
      const pp = f.pages ? `pp. ${tx(f.pages)}` : '';
      const bits = [vol, iss, yr, pp].filter(Boolean).join(', ');
      let out = `${tx(A)}${A?'. ':''}"${tx(cfTitleCase(f.title||''))}." ${it(f.journal||'')}, ${bits}.`;
      if(f.doi) out += ` ${tx(f.doi)}`;
      return out;
    }
    case 'website': {
      const site = f.site ? `${it(f.site)}, ` : '';
      const yr = f.year ? `${tx(f.year)}, ` : '';
      const acc = f.accessed ? ` Accessed ${tx(f.accessed)}.` : '';
      return `${tx(A)}${A?'. ':''}"${tx(cfTitleCase(f.title||''))}." ${site}${yr}${tx(f.url||'')}.${acc}`;
    }
    case 'chapter': {
      const editors = cfParseAuthors(f.editors||'').map(a => a.first ? `${a.first}${a.middle? ' '+a.middle : ''} ${a.last}` : a.last).join(', ');
      const pp = f.pages ? `, pp. ${tx(f.pages)}` : '';
      return `${tx(A)}${A?'. ':''}"${tx(cfTitleCase(f.title||''))}." ${it(cfTitleCase(f.booktitle||''))}, edited by ${tx(editors)}, ${tx(f.publisher||'')}${f.year? ', '+tx(f.year) : ''}${pp}.`;
    }
    case 'conference': {
      const pp = f.pages ? `, pp. ${tx(f.pages)}` : '';
      return `${tx(A)}${A?'. ':''}"${tx(cfTitleCase(f.title||''))}." ${it(f.conference||'')}${f.year? ', '+tx(f.year) : ''}${pp}.`;
    }
    case 'thesis': {
      return `${tx(A)}${A?'. ':''}${it(cfTitleCase(f.title||''))}. ${tx(f.year||'')}. ${tx(f.institution||'')}, ${tx(f.kind||'PhD dissertation')}.`;
    }
    case 'news': {
      const dt = f.date ? tx(f.date) : tx(f.year||'');
      let out = `${tx(A)}${A?'. ':''}"${tx(cfTitleCase(f.title||''))}." ${it(f.publication||'')}, ${dt}.`;
      if(f.url) out += ` ${tx(f.url)}.`;
      return out;
    }
    case 'report': {
      return `${tx(A)}${A?'. ':''}${it(cfTitleCase(f.title||''))}. ${tx(f.institution||'')}${f.number? ', '+tx(f.number) : ''}${f.year? ', '+tx(f.year) : ''}.${f.url? ' '+tx(f.url) : ''}`;
    }
  }
  return '';
}

// ---------- Chicago (author-date) ----------
function cfFormatChicago(src){
  const f = src.fields;
  const authors = cfParseAuthors(f.authors||'');
  const A = cfAuthorsChicago(authors);
  const yr = f.year ? `${tx(f.year)}.` : 'n.d.';
  switch(src.type){
    case 'book': {
      const ed = f.edition ? `${tx(f.edition)}. ` : '';
      const pub = [f.place, f.publisher].filter(Boolean).map(tx).join(': ');
      return `${tx(A)}. ${yr} ${it(cfTitleCase(f.title||''))}. ${ed}${pub}.`;
    }
    case 'journal-article': {
      const issue = f.issue ? `, no. ${tx(f.issue)}` : '';
      let out = `${tx(A)}. ${yr} "${tx(cfTitleCase(f.title||''))}." ${it(f.journal||'')} ${tx(f.volume||'')}${issue}${f.pages? ': '+tx(f.pages) : ''}.`;
      if(f.doi) out += ` ${tx(f.doi)}.`;
      return out;
    }
    case 'website': {
      const site = f.site ? ` ${it(f.site)}.` : '';
      const acc = f.accessed ? ` Accessed ${tx(f.accessed)}.` : '';
      return `${tx(A)}. ${yr} "${tx(cfTitleCase(f.title||''))}."${site}${acc} ${tx(f.url||'')}.`;
    }
    case 'chapter': {
      const editors = cfParseAuthors(f.editors||'').map(a => a.first ? `${a.first}${a.middle? ' '+a.middle : ''} ${a.last}` : a.last).join(' and ');
      const pp = f.pages ? `, ${tx(f.pages)}` : '';
      const pub = [f.place, f.publisher].filter(Boolean).map(tx).join(': ');
      return `${tx(A)}. ${yr} "${tx(cfTitleCase(f.title||''))}." In ${it(cfTitleCase(f.booktitle||''))}, edited by ${tx(editors)}${pp}. ${pub}.`;
    }
    case 'conference': {
      const pp = f.pages ? `, ${tx(f.pages)}` : '';
      return `${tx(A)}. ${yr} "${tx(cfTitleCase(f.title||''))}." Paper presented at ${tx(f.conference||'')}${f.place? ', '+tx(f.place) : ''}${pp}.`;
    }
    case 'thesis': {
      return `${tx(A)}. ${yr} "${tx(cfTitleCase(f.title||''))}." ${tx(f.kind||'PhD diss.')}, ${tx(f.institution||'')}.${f.url? ' '+tx(f.url)+'.' : ''}`;
    }
    case 'news': {
      const dt = f.date ? `${tx(f.date)}` : tx(f.year||'');
      return `${tx(A)}. ${yr} "${tx(cfTitleCase(f.title||''))}." ${it(f.publication||'')}, ${dt}.${f.url? ' '+tx(f.url)+'.' : ''}`;
    }
    case 'report': {
      const num = f.number ? ` ${tx(f.number)}` : '';
      return `${tx(A)}. ${yr} ${it(cfTitleCase(f.title||''))}.${num} ${tx(f.institution||'')}.${f.url? ' '+tx(f.url)+'.' : ''}`;
    }
  }
  return '';
}

// ---------- Harvard ----------
function cfFormatHarvard(src){
  const f = src.fields;
  const authors = cfParseAuthors(f.authors||'');
  const A = cfAuthorsHarvard(authors);
  const yr = f.year ? `(${tx(f.year)})` : '(n.d.)';
  switch(src.type){
    case 'book': {
      const ed = f.edition ? `${tx(f.edition)}. ` : '';
      const pub = [f.place, f.publisher].filter(Boolean).map(tx).join(': ');
      return `${tx(A)} ${yr} ${it(cfTitleCase(f.title||''))}. ${ed}${pub}.`;
    }
    case 'journal-article': {
      const issue = f.issue ? `(${tx(f.issue)})` : '';
      return `${tx(A)} ${yr} '${tx(cfTitleCase(f.title||''))}', ${it(f.journal||'')}, ${tx(f.volume||'')}${issue}${f.pages? ', pp. '+tx(f.pages) : ''}.${f.doi? ' '+tx(f.doi) : ''}`;
    }
    case 'website': {
      const acc = f.accessed ? ` (Accessed: ${tx(f.accessed)})` : '';
      return `${tx(A)} ${yr} ${it(cfTitleCase(f.title||''))}. ${tx(f.site||'')}. Available at: ${tx(f.url||'')}${acc}.`;
    }
    case 'chapter': {
      const editors = cfParseAuthors(f.editors||'').map(a => a.first ? `${cfInitials(a.first, a.middle)} ${a.last}` : a.last).join(' and ');
      const pp = f.pages ? `, pp. ${tx(f.pages)}` : '';
      const pub = [f.place, f.publisher].filter(Boolean).map(tx).join(': ');
      return `${tx(A)} ${yr} '${tx(cfTitleCase(f.title||''))}', in ${tx(editors)} (eds.) ${it(cfTitleCase(f.booktitle||''))}. ${pub}${pp}.`;
    }
    case 'conference': {
      const pp = f.pages ? `, pp. ${tx(f.pages)}` : '';
      return `${tx(A)} ${yr} '${tx(cfTitleCase(f.title||''))}', ${it(f.conference||'')}${f.place? ', '+tx(f.place) : ''}${pp}.`;
    }
    case 'thesis': {
      return `${tx(A)} ${yr} ${it(cfTitleCase(f.title||''))}. ${tx(f.kind||'PhD thesis')}. ${tx(f.institution||'')}.${f.url? ' Available at: '+tx(f.url)+'.' : ''}`;
    }
    case 'news': {
      const dt = f.date ? `, ${tx(f.date)}` : '';
      return `${tx(A)} ${yr} '${tx(cfTitleCase(f.title||''))}', ${it(f.publication||'')}${dt}.${f.url? ' Available at: '+tx(f.url)+'.' : ''}`;
    }
    case 'report': {
      const num = f.number ? ` (${tx(f.number)})` : '';
      return `${tx(A)} ${yr} ${it(cfTitleCase(f.title||''))}${num}. ${tx(f.institution||'')}.${f.url? ' Available at: '+tx(f.url)+'.' : ''}`;
    }
  }
  return '';
}

function cfFormat(src){
  switch(CF_STYLE){
    case 'apa':     return cfFormatAPA(src);
    case 'mla':     return cfFormatMLA(src);
    case 'chicago': return cfFormatChicago(src);
    case 'harvard': return cfFormatHarvard(src);
  }
  return '';
}

function cfAddSource(type){
  CF_SRC.push({ id: CF_NEXT_ID++, type: type || 'journal-article', fields: {} });
  cfRebuild();
}
function cfDelSource(id){
  CF_SRC = CF_SRC.filter(s => s.id !== id);
  cfRebuild();
}
function cfChangeType(id, t){
  const s = CF_SRC.find(x => x.id === id);
  if(s){ s.type = t; }
  cfRebuild();
}
function cfSetField(id, key, value){
  const s = CF_SRC.find(x => x.id === id);
  if(s){ s.fields[key] = value; }
  cfRender();
}
function cfSort(){
  CF_SRC.sort((a, b) => {
    const aa = (cfParseAuthors(a.fields.authors||'')[0]||{}).last || a.fields.title || '';
    const bb = (cfParseAuthors(b.fields.authors||'')[0]||{}).last || b.fields.title || '';
    return aa.toLowerCase().localeCompare(bb.toLowerCase());
  });
  cfRebuild();
}

function cfRebuildStyles(){
  document.querySelectorAll('.cf-style-btn').forEach(b => {
    b.classList.toggle('active', b.dataset.style === CF_STYLE);
  });
}

function cfRebuild(){
  const wrap = document.getElementById('cf-sources');
  wrap.innerHTML = '';
  CF_SRC.forEach((s) => {
    const card = document.createElement('div');
    card.className = 'cf-card';
    const head = document.createElement('div');
    head.className = 'cf-card-head';
    const sel = document.createElement('select');
    Object.keys(CF_TYPE_LABELS).forEach(k => {
      const o = document.createElement('option');
      o.value = k; o.textContent = CF_TYPE_LABELS[k];
      if(s.type === k) o.selected = true;
      sel.appendChild(o);
    });
    sel.onchange = () => cfChangeType(s.id, sel.value);
    const del = document.createElement('button');
    del.type = 'button'; del.className = 'cf-del'; del.textContent = '✕';
    del.title = 'Remove source';
    del.onclick = () => cfDelSource(s.id);
    head.appendChild(sel);
    head.appendChild(del);
    card.appendChild(head);

    const grid = document.createElement('div');
    grid.className = 'cf-fields';
    (CF_FIELDS[s.type] || []).forEach(([key, label, wide, placeholder]) => {
      const box = document.createElement('div');
      if(wide) box.className = 'cf-wide';
      const lbl = document.createElement('label'); lbl.textContent = label;
      const inp = (wide && key === 'authors') || (wide && key === 'editors')
        ? document.createElement('textarea')
        : document.createElement('input');
      if(inp.tagName === 'INPUT') inp.type = 'text';
      inp.value = s.fields[key] || '';
      inp.placeholder = placeholder || '';
      inp.oninput = () => cfSetField(s.id, key, inp.value);
      box.appendChild(lbl);
      box.appendChild(inp);
      grid.appendChild(box);
    });
    card.appendChild(grid);

    const rendered = document.createElement('div');
    rendered.className = 'cf-rendered';
    rendered.dataset.role = 'rendered';
    rendered.dataset.id = s.id;
    card.appendChild(rendered);

    wrap.appendChild(card);
  });
  cfRender();
}

function cfRender(){
  // Per-card rendered preview
  document.querySelectorAll('.cf-rendered').forEach(el => {
    const id = parseInt(el.dataset.id, 10);
    const s = CF_SRC.find(x => x.id === id);
    if(!s) return;
    const html = cfFormat(s);
    el.innerHTML = html || '<span class="cf-empty">Fill in the fields above…</span>';
  });
  // Combined bibliography preview
  const preview = document.getElementById('cf-preview');
  const out = document.getElementById('cf-out');
  if(!CF_SRC.length){
    preview.innerHTML = '<div class="cf-empty">Add a source above to see the bibliography…</div>';
    out.textContent = '{LBL_NO_INPUT}';
    return;
  }
  const entries = CF_SRC.map(cfFormat).filter(Boolean);
  preview.innerHTML = entries.map(e => `<div class="cf-entry">${e}</div>`).join('') ||
    '<div class="cf-empty">Fill in source fields to see the bibliography…</div>';
  const plain = entries.map(e => e.replace(/<\\/?i>/g, '').replace(/&amp;/g,'&').replace(/&lt;/g,'<').replace(/&gt;/g,'>')).join('\\n\\n');
  out.textContent = plain || '{LBL_NO_INPUT}';
}

document.addEventListener('DOMContentLoaded', () => {
  document.querySelectorAll('.cf-style-btn').forEach(b => {
    b.onclick = () => { CF_STYLE = b.dataset.style; cfRebuildStyles(); cfRender(); };
  });
  // Seed with a journal-article example
  CF_SRC.push({
    id: CF_NEXT_ID++, type: 'journal-article',
    fields: {
      authors: 'Smith, Jane M.\\nDoe, Carlos',
      year: '2023',
      title: 'Self-paced learning in mixed-ability classrooms',
      journal: 'Journal of Educational Research',
      volume: '116',
      issue: '4',
      pages: '215-238',
      doi: 'https://doi.org/10.1234/example'
    }
  });
  cfRebuildStyles();
  cfRebuild();
});
</script>
""",
    "help": {
        "en": """
<h2>What is this for?</h2>
<p>References are the bit of academic writing nobody likes and everybody loses marks on. This formatter takes the fields you already know (author, year, title, journal, publisher, pages…) and produces a clean citation in APA 7, MLA 9, Chicago author-date, or Harvard, ready to drop into your bibliography. No DOI lookups, no upload, no signup — everything stays in your browser.</p>

<h3>How to use it</h3>
<ul>
  <li><strong>Pick a style</strong> at the top — APA 7 is the default. Switch any time; the same fields render to all four styles instantly.</li>
  <li><strong>Choose the source type</strong> (book, journal article, website, chapter, conference paper, thesis, news article, report). The fields update to what each type needs.</li>
  <li><strong>Authors:</strong> one per line, in <code>Last, First Middle</code> form. A single line without a comma is treated as an organisation (e.g. <em>World Health Organization</em>).</li>
  <li><strong>Add multiple sources</strong> with "+ Add source" to build a full bibliography. Use "Sort A→Z" to alphabetise by first author's last name.</li>
  <li><strong>Copy</strong> the plain-text output (italics removed for clipboards that don't support rich text). Paste into Word / Google Docs and the words will be there; italicise titles by hand if your target document strips formatting.</li>
</ul>

<h3>Style notes</h3>
<ul>
  <li><strong>APA 7</strong> — sentence-case article titles, italic journal titles, "&" between final authors. Lists up to 20 authors before using "…". Uses (n.d.) for unknown year.</li>
  <li><strong>MLA 9</strong> — title-case titles, first author <code>Last, First</code> then subsequent authors <code>First Last</code>; "et al." for 3+ authors.</li>
  <li><strong>Chicago</strong> — author-date variant. Bibliography style. Notes-bibliography is a separate convention and isn't generated here.</li>
  <li><strong>Harvard</strong> — UK-style author-date. Single quotes around article titles; "et al." for 4+ authors. (Note: Harvard isn't a single fixed style — institutions vary in punctuation and ordering. Check your university's style guide.)</li>
</ul>

<h3>Common gotchas</h3>
<ul>
  <li><strong>Author order matters.</strong> Don't sort the authors of a multi-author paper — keep the order from the source.</li>
  <li><strong>Capitalisation differs by style.</strong> APA uses sentence case for article titles ("The impact of remote learning"); MLA uses title case ("The Impact of Remote Learning"). The tool converts automatically based on the style.</li>
  <li><strong>DOI is preferred over URL</strong> when both are available — DOIs are stable, URLs rot.</li>
  <li><strong>Italics don't survive plain-text copy.</strong> The HTML preview shows italics; the copy-button output is plain text. If your target system supports rich text, copy directly from the preview instead.</li>
  <li><strong>This is a formatter, not a fact-checker.</strong> Garbage in, garbage out — verify volumes, page numbers, and spelling against the original source.</li>
  <li><strong>Privacy.</strong> No data leaves your browser. Closing the tab clears everything.</li>
</ul>
""",
        "de": """
<h2>Wozu ist das gut?</h2>
<p>Quellenangaben mag niemand, und alle verlieren dort Punkte. Dieser Formatierer nimmt die Felder, die du sowieso schon hast (Autor, Jahr, Titel, Zeitschrift, Verlag, Seiten …), und produziert eine saubere Quellenangabe in APA 7, MLA 9, Chicago Autor-Datum oder Harvard — fertig zum Einfügen in dein Literaturverzeichnis. Keine DOI-Abfragen, kein Upload, keine Anmeldung — alles bleibt im Browser.</p>

<h3>So funktioniert's</h3>
<ul>
  <li><strong>Stil oben wählen</strong> — APA 7 ist Default. Du kannst jederzeit wechseln; dieselben Felder werden sofort in allen vier Stilen gerendert.</li>
  <li><strong>Quellentyp auswählen</strong> (Buch, Zeitschriftenartikel, Webseite, Buchkapitel, Konferenzbeitrag, Abschlussarbeit, Zeitungsartikel, Bericht). Die Felder passen sich an.</li>
  <li><strong>Autor:innen:</strong> eine pro Zeile, <code>Nachname, Vorname Mittelname</code>. Eine einzelne Zeile ohne Komma gilt als Organisation (z. B. <em>Weltgesundheitsorganisation</em>).</li>
  <li><strong>Mehrere Quellen</strong> mit „+ Add source" hinzufügen. „Sort A→Z" sortiert alphabetisch.</li>
  <li><strong>Plaintext kopieren.</strong> Kursivschrift wird beim Kopieren entfernt (Clipboard kann oft kein Rich Text). In Word/Docs einfügen und Titel ggf. von Hand kursiv setzen.</li>
</ul>

<h3>Stil-Hinweise</h3>
<ul>
  <li><strong>APA 7</strong> — Artikeltitel in Satzgroßschreibung, Zeitschriftentitel kursiv, "&" vor letztem Autor. Bis 20 Autor:innen aufgelistet, dann „…".</li>
  <li><strong>MLA 9</strong> — Title Case, erster Autor <code>Last, First</code>, weitere <code>First Last</code>; ab 3 Autor:innen "et al.".</li>
  <li><strong>Chicago</strong> — Autor-Datum-Variante (Bibliographie-Stil). Notes-Bibliography wird nicht generiert.</li>
  <li><strong>Harvard</strong> — britischer Autor-Datum-Stil. Einfache Anführungszeichen für Artikeltitel; "et al." ab 4 Autor:innen. (Harvard ist nicht einheitlich — prüfe das Leitbild deiner Uni.)</li>
</ul>

<h3>Typische Fehler</h3>
<ul>
  <li><strong>Reihenfolge der Autor:innen beibehalten.</strong> Bei mehreren Autor:innen nicht alphabetisch sortieren — Reihenfolge aus der Quelle übernehmen.</li>
  <li><strong>Großschreibung unterscheidet sich je Stil.</strong> APA: Satzgroßschreibung; MLA: Title Case. Wird automatisch umgesetzt.</li>
  <li><strong>DOI ist stabiler als URL.</strong> Wenn beides vorhanden, nimm den DOI.</li>
  <li><strong>Kursivschrift fehlt beim Kopieren.</strong> Die Vorschau zeigt Kursivschrift, der Kopier-Button gibt Plaintext. Kopiere aus der Vorschau, wenn Rich Text unterstützt wird.</li>
  <li><strong>Das ist ein Formatierer, kein Faktenprüfer.</strong> Verifiziere Seitenzahlen, Bände und Schreibweisen anhand der Originalquelle.</li>
  <li><strong>Datenschutz.</strong> Keine Daten verlassen den Browser. Tab schließen = alles weg.</li>
</ul>
""",
        "es": """
<h2>¿Para qué sirve?</h2>
<p>Las referencias son la parte del trabajo académico que nadie quiere hacer y donde todo el mundo pierde puntos. Este formateador toma los campos que ya conoces (autor, año, título, revista, editorial, páginas…) y produce una cita limpia en APA 7, MLA 9, Chicago autor-fecha o Harvard, lista para pegar en tu bibliografía. Sin búsquedas DOI, sin upload, sin registro — todo se queda en el navegador.</p>

<h3>Cómo se usa</h3>
<ul>
  <li><strong>Elige el estilo</strong> arriba — APA 7 es el default. Puedes cambiar en cualquier momento; los mismos campos se renderizan al instante en los cuatro estilos.</li>
  <li><strong>Elige el tipo de fuente</strong> (libro, artículo de revista, web, capítulo, paper de congreso, tesis, prensa, informe). Los campos se ajustan.</li>
  <li><strong>Autores:</strong> uno por línea, <code>Apellido, Nombre Segundo</code>. Una línea sin coma se trata como organización (p. ej. <em>Organización Mundial de la Salud</em>).</li>
  <li><strong>Añade varias fuentes</strong> con "+ Add source". "Sort A→Z" ordena alfabéticamente por primer apellido.</li>
  <li><strong>Copia el texto plano.</strong> Las cursivas no sobreviven al portapapeles plano; pega en Word/Docs y pon cursivas a mano si el destino no soporta rich text.</li>
</ul>

<h3>Notas por estilo</h3>
<ul>
  <li><strong>APA 7</strong> — títulos de artículo en mayúscula tipo oración, revistas en cursiva, "&" antes del último autor. Lista hasta 20 antes de poner "…".</li>
  <li><strong>MLA 9</strong> — title case; primer autor <code>Apellido, Nombre</code>, siguientes <code>Nombre Apellido</code>; "et al." con 3+ autores.</li>
  <li><strong>Chicago</strong> — variante autor-fecha (bibliografía). Notas-bibliografía es otra convención y no se genera aquí.</li>
  <li><strong>Harvard</strong> — estilo británico autor-fecha. Comillas simples en títulos de artículo; "et al." con 4+ autores. (Harvard no es un estilo único — revisa la guía de tu institución.)</li>
</ul>

<h3>Errores comunes</h3>
<ul>
  <li><strong>El orden de los autores importa.</strong> No los ordenes alfabéticamente — respeta el orden de la fuente.</li>
  <li><strong>Las mayúsculas dependen del estilo.</strong> APA usa mayúscula tipo oración; MLA usa title case. El conversor lo hace automáticamente.</li>
  <li><strong>El DOI es preferible a la URL</strong> cuando están ambos — los DOI son estables, las URL caducan.</li>
  <li><strong>Las cursivas se pierden en plain text.</strong> La preview muestra cursivas; el botón copiar da texto plano. Copia desde la preview si tu sistema soporta rich text.</li>
  <li><strong>Esto formatea, no verifica.</strong> Garbage in, garbage out — comprueba volúmenes, páginas y ortografía contra la fuente original.</li>
  <li><strong>Privacidad.</strong> Nada sale del navegador. Cerrar la pestaña borra todo.</li>
</ul>
""",
        "fr": """
<h2>À quoi ça sert ?</h2>
<p>Les références bibliographiques, personne n'aime les faire et tout le monde y perd des points. Ce formateur prend les champs que vous avez déjà (auteur, année, titre, revue, éditeur, pages…) et produit une référence propre en APA 7, MLA 9, Chicago auteur-date ou Harvard, prête à coller dans votre bibliographie. Pas d'appel DOI, pas d'upload, pas d'inscription — tout reste dans le navigateur.</p>

<h3>Comment l'utiliser</h3>
<ul>
  <li><strong>Choisissez le style</strong> en haut — APA 7 par défaut. Vous pouvez basculer à tout moment ; les mêmes champs sont rendus instantanément dans les quatre styles.</li>
  <li><strong>Choisissez le type de source</strong> (livre, article, site, chapitre, acte de colloque, thèse, presse, rapport). Les champs s'adaptent.</li>
  <li><strong>Auteurs :</strong> un par ligne, <code>Nom, Prénom Deuxième</code>. Une ligne sans virgule est traitée comme une organisation (ex. <em>Organisation mondiale de la santé</em>).</li>
  <li><strong>Plusieurs sources</strong> via « + Add source ». « Sort A→Z » trie par nom du premier auteur.</li>
  <li><strong>Copiez le texte brut.</strong> Les italiques disparaissent à la copie ; collez dans Word / Docs et remettez les italiques à la main si le destinataire n'accepte pas le format riche.</li>
</ul>

<h3>Notes par style</h3>
<ul>
  <li><strong>APA 7</strong> — titres d'article en bas de casse, revues en italique, « & » avant le dernier auteur. Jusqu'à 20 auteurs listés, puis « … ».</li>
  <li><strong>MLA 9</strong> — title case ; premier auteur <code>Nom, Prénom</code>, suivants <code>Prénom Nom</code> ; « et al. » à partir de 3 auteurs.</li>
  <li><strong>Chicago</strong> — variante auteur-date (style bibliographie). Notes-bibliographie est une autre convention non générée ici.</li>
  <li><strong>Harvard</strong> — auteur-date britannique. Guillemets simples pour les titres d'article ; « et al. » à partir de 4 auteurs. (Harvard n'est pas un style unique — vérifiez le guide de votre établissement.)</li>
</ul>

<h3>Pièges courants</h3>
<ul>
  <li><strong>L'ordre des auteurs compte.</strong> Ne triez pas alphabétiquement — gardez l'ordre de la source.</li>
  <li><strong>La capitalisation dépend du style.</strong> APA en bas de casse ; MLA en title case. Conversion automatique.</li>
  <li><strong>Préférez le DOI à l'URL</strong> quand les deux sont disponibles — les DOI sont stables, les URL pourrissent.</li>
  <li><strong>Les italiques ne survivent pas au texte brut.</strong> L'aperçu les affiche, le bouton copier donne du texte plat. Copiez depuis l'aperçu si le format riche est supporté.</li>
  <li><strong>C'est un formateur, pas un vérificateur.</strong> Garbage in, garbage out — vérifiez volumes, pages et orthographe sur la source.</li>
  <li><strong>Vie privée.</strong> Rien ne sort du navigateur. Fermer l'onglet efface tout.</li>
</ul>
""",
        "it": """
<h2>A cosa serve?</h2>
<p>Le bibliografie sono la parte del lavoro accademico che nessuno ama fare e dove tutti perdono punti. Questo formattatore prende i campi che già conosci (autore, anno, titolo, rivista, editore, pagine…) e produce una citazione pulita in APA 7, MLA 9, Chicago autore-data o Harvard, pronta da incollare in bibliografia. Niente lookup DOI, niente upload, niente registrazione — tutto resta nel browser.</p>

<h3>Come si usa</h3>
<ul>
  <li><strong>Scegli lo stile</strong> in alto — APA 7 di default. Puoi cambiare in qualsiasi momento; gli stessi campi vengono renderizzati istantaneamente nei quattro stili.</li>
  <li><strong>Scegli il tipo di fonte</strong> (libro, articolo, sito, capitolo, atto di convegno, tesi, articolo di giornale, rapporto). I campi si adattano.</li>
  <li><strong>Autori:</strong> uno per riga, <code>Cognome, Nome Secondo</code>. Una riga senza virgola viene trattata come ente (es. <em>Organizzazione mondiale della sanità</em>).</li>
  <li><strong>Più fonti</strong> con "+ Add source". "Sort A→Z" ordina per cognome del primo autore.</li>
  <li><strong>Copia il testo semplice.</strong> Il corsivo non sopravvive agli appunti plain text; incolla in Word/Docs e metti il corsivo a mano se la destinazione non supporta il rich text.</li>
</ul>

<h3>Note per stile</h3>
<ul>
  <li><strong>APA 7</strong> — titoli articolo in sentence case, riviste in corsivo, "&" prima dell'ultimo autore. Lista fino a 20 autori, poi "…".</li>
  <li><strong>MLA 9</strong> — title case; primo autore <code>Cognome, Nome</code>, seguenti <code>Nome Cognome</code>; "et al." con 3+ autori.</li>
  <li><strong>Chicago</strong> — variante autore-data (stile bibliografia). Notes-bibliography è una convenzione separata, non generata qui.</li>
  <li><strong>Harvard</strong> — autore-data britannico. Apici singoli per i titoli articolo; "et al." con 4+ autori. (Harvard non è uno stile unico — controlla la guida del tuo ateneo.)</li>
</ul>

<h3>Errori comuni</h3>
<ul>
  <li><strong>L'ordine degli autori conta.</strong> Non ordinarli alfabeticamente — mantieni l'ordine della fonte.</li>
  <li><strong>La maiuscolazione dipende dallo stile.</strong> APA in sentence case; MLA in title case. Conversione automatica.</li>
  <li><strong>DOI preferibile all'URL</strong> se ci sono entrambi — i DOI sono stabili, gli URL marciscono.</li>
  <li><strong>Il corsivo si perde nel testo semplice.</strong> L'anteprima lo mostra; il pulsante copia restituisce testo piano. Copia dall'anteprima se il rich text è supportato.</li>
  <li><strong>È un formattatore, non un verificatore.</strong> Garbage in, garbage out — verifica volumi, pagine e ortografia sulla fonte.</li>
  <li><strong>Privacy.</strong> Nulla esce dal browser. Chiudi la scheda e tutto sparisce.</li>
</ul>
""",
        "pt": """
<h2>Para que serve?</h2>
<p>Referências bibliográficas são a parte do trabalho acadêmico que ninguém gosta de fazer e onde todo mundo perde nota. Este formatador pega os campos que você já tem (autor, ano, título, periódico, editora, páginas…) e gera uma citação limpa em APA 7, MLA 9, Chicago autor-data ou Harvard, pronta pra colar na bibliografia. Sem buscar DOI, sem upload, sem cadastro — tudo fica no navegador.</p>

<h3>Como usar</h3>
<ul>
  <li><strong>Escolha o estilo</strong> em cima — APA 7 é o padrão. Você pode trocar a qualquer hora; os mesmos campos são renderizados nos quatro estilos na hora.</li>
  <li><strong>Escolha o tipo de fonte</strong> (livro, artigo de periódico, site, capítulo, paper de congresso, tese ou dissertação, matéria de jornal, relatório). Os campos se ajustam.</li>
  <li><strong>Autores:</strong> um por linha, <code>Sobrenome, Nome do Meio</code>. Uma linha sem vírgula é tratada como instituição (ex.: <em>Organização Mundial da Saúde</em>).</li>
  <li><strong>Várias fontes</strong> com "+ Add source". "Sort A→Z" ordena alfabeticamente pelo sobrenome do primeiro autor.</li>
  <li><strong>Copie o texto plano.</strong> Itálico não sobrevive ao clipboard simples; cole no Word/Docs e bote itálico no título manualmente se o destino não aceitar rich text.</li>
</ul>

<h3>Notas por estilo</h3>
<ul>
  <li><strong>APA 7</strong> — títulos de artigo em sentence case, periódicos em itálico, "&" antes do último autor. Lista até 20 autores, depois "…".</li>
  <li><strong>MLA 9</strong> — title case; primeiro autor <code>Sobrenome, Nome</code>, demais <code>Nome Sobrenome</code>; "et al." com 3+ autores.</li>
  <li><strong>Chicago</strong> — variante autor-data (estilo bibliografia). Notes-bibliography é outra convenção, não gerada aqui.</li>
  <li><strong>Harvard</strong> — autor-data britânico. Aspas simples nos títulos de artigo; "et al." com 4+ autores. (Harvard não é único — confira o guia da sua instituição.)</li>
</ul>

<h3>Cuidados comuns</h3>
<ul>
  <li><strong>A ordem dos autores importa.</strong> Não ordene alfabeticamente — mantenha a ordem da fonte.</li>
  <li><strong>A capitalização muda por estilo.</strong> APA usa sentence case; MLA usa title case. Conversão automática.</li>
  <li><strong>DOI é melhor que URL</strong> se tiver os dois — DOIs são estáveis, URLs apodrecem.</li>
  <li><strong>Itálico se perde no texto plano.</strong> O preview mostra; o botão copiar dá texto plano. Copie do preview se o destino aceita rich text.</li>
  <li><strong>Isto é formatador, não checador de fatos.</strong> Garbage in, garbage out — confira volumes, páginas e grafia na fonte.</li>
  <li><strong>Privacidade.</strong> Nada sai do navegador. Fechou a aba, foi tudo.</li>
</ul>
""",
        "pl": """
<h2>Do czego to służy?</h2>
<p>Bibliografie to ta część pisania akademickiego, której nikt nie lubi i na której wszyscy tracą punkty. Ten formater bierze pola, które już znasz (autor, rok, tytuł, czasopismo, wydawca, strony…) i tworzy czysty zapis w APA 7, MLA 9, Chicago autor-data lub Harvard, gotowy do wklejenia w bibliografię. Bez wyszukiwania DOI, bez wysyłania plików, bez rejestracji — wszystko zostaje w przeglądarce.</p>

<h3>Jak używać</h3>
<ul>
  <li><strong>Wybierz styl</strong> u góry — APA 7 jest domyślny. Możesz zmieniać w każdej chwili; te same pola renderują się natychmiast we wszystkich czterech stylach.</li>
  <li><strong>Wybierz typ źródła</strong> (książka, artykuł, strona WWW, rozdział, referat konferencyjny, praca dyplomowa, artykuł prasowy, raport). Pola się dostosują.</li>
  <li><strong>Autorzy:</strong> jeden na linię, <code>Nazwisko, Imię Drugie</code>. Linia bez przecinka jest traktowana jako organizacja (np. <em>Światowa Organizacja Zdrowia</em>).</li>
  <li><strong>Wiele źródeł</strong> przez "+ Add source". "Sort A→Z" sortuje alfabetycznie po nazwisku pierwszego autora.</li>
  <li><strong>Kopiuj zwykły tekst.</strong> Kursywa nie przeżywa schowka plain text; wklej do Worda/Docs i ręcznie pokursywuj tytuły, jeśli docelowy system nie obsługuje rich text.</li>
</ul>

<h3>Uwagi do stylów</h3>
<ul>
  <li><strong>APA 7</strong> — tytuły artykułów sentence case, czasopisma kursywą, "&" przed ostatnim autorem. Do 20 autorów, potem "…".</li>
  <li><strong>MLA 9</strong> — title case; pierwszy autor <code>Nazwisko, Imię</code>, następni <code>Imię Nazwisko</code>; "et al." od 3 autorów.</li>
  <li><strong>Chicago</strong> — wariant autor-data (styl bibliografii). Notes-bibliography to osobna konwencja, której tu nie generujemy.</li>
  <li><strong>Harvard</strong> — brytyjski autor-data. Pojedyncze cudzysłowy w tytułach artykułów; "et al." od 4 autorów. (Harvard nie jest jednolity — sprawdź przewodnik swojej uczelni.)</li>
</ul>

<h3>Częste pułapki</h3>
<ul>
  <li><strong>Kolejność autorów ma znaczenie.</strong> Nie sortuj alfabetycznie — zachowaj kolejność z źródła.</li>
  <li><strong>Wielkość liter zależy od stylu.</strong> APA: sentence case; MLA: title case. Konwersja jest automatyczna.</li>
  <li><strong>DOI lepszy niż URL</strong> jeśli masz oba — DOI są stabilne, URL gniją.</li>
  <li><strong>Kursywa znika w plain text.</strong> Podgląd ją pokazuje; przycisk kopiuj daje plain text. Kopiuj z podglądu, jeśli rich text jest obsługiwany.</li>
  <li><strong>To formater, nie weryfikator faktów.</strong> Garbage in, garbage out — sprawdź tomy, strony i pisownię w oryginale.</li>
  <li><strong>Prywatność.</strong> Nic nie opuszcza przeglądarki. Zamknij kartę = wszystko znika.</li>
</ul>
""",
        "ja": """
<h2>用途</h2>
<p>引用文献は誰も書きたくない、けれど誰もが減点される部分です。本フォーマッターは、すでに手元にあるフィールド（著者・年・タイトル・誌名・出版社・ページ…）から、APA 7／MLA 9／Chicago 著者-年／Harvard の整形済み引用を生成します。文献リストにそのまま貼り付けられます。DOI 検索なし、アップロードなし、登録なし — すべてがブラウザ内で完結します。</p>

<h3>使い方</h3>
<ul>
  <li><strong>スタイルを上で選ぶ</strong> — 既定は APA 7。切り替えると同じフィールドが瞬時に他のスタイルでも整形されます。</li>
  <li><strong>資料種別を選ぶ</strong>（書籍／学術論文／ウェブサイト／章／会議録／学位論文／新聞記事／報告書）。フィールドが自動で切り替わります。</li>
  <li><strong>著者:</strong> 1 行に 1 名、<code>姓, 名 ミドルネーム</code>。カンマのない 1 行は団体名として扱われます（例：<em>世界保健機関</em>）。</li>
  <li><strong>複数の資料</strong>は「+ Add source」で追加。「Sort A→Z」で第一著者の姓でアルファベット順に並べ替えます。</li>
  <li><strong>プレーンテキストでコピー</strong>。イタリックはクリップボードのプレーンテキストでは失われます。Word／Docs に貼り付け、リッチテキストを受け付けない場合は手動でイタリック化してください。</li>
</ul>

<h3>スタイル別の補足</h3>
<ul>
  <li><strong>APA 7</strong> — 論文タイトルはセンテンスケース、誌名はイタリック、最後の著者の前に "&"。20 名までは全員列挙、21 名以上は「…」。</li>
  <li><strong>MLA 9</strong> — タイトルケース。第一著者は <code>姓, 名</code>、以降は <code>名 姓</code>。3 名以上で "et al."。</li>
  <li><strong>Chicago</strong> — 著者-年（参考文献リスト形式）。脚注-参考文献形式は別規約で、本ツールでは生成しません。</li>
  <li><strong>Harvard</strong> — 英国式著者-年。論文タイトルはシングルクォート、4 名以上で "et al."。（Harvard は単一規格ではなく、機関によって異なります。所属のスタイルガイドを確認してください。）</li>
</ul>

<h3>よくある注意点</h3>
<ul>
  <li><strong>著者順は変えない。</strong> 多著者論文をアルファベット順に並べ替えない — 原典の順序を尊重します。</li>
  <li><strong>大文字小文字の規則はスタイル次第。</strong> APA はセンテンスケース、MLA はタイトルケース。本ツールが自動変換します。</li>
  <li><strong>DOI > URL。</strong> 両方ある場合は DOI が安定。URL は将来切れる可能性があります。</li>
  <li><strong>プレーンテキストではイタリックが失われます。</strong> プレビューには表示されますが、コピー出力はプレーン。リッチテキスト対応先ならプレビューから直接コピー。</li>
  <li><strong>これは整形ツールであり、事実確認はしません。</strong> ボリューム、ページ番号、綴りは必ず原典で確認してください。</li>
  <li><strong>プライバシー。</strong> データは一切ブラウザを離れません。タブを閉じればすべて消えます。</li>
</ul>
""",
        "nl": """
<h2>Waarvoor is dit?</h2>
<p>Bronvermeldingen zijn het stuk van academisch schrijven dat niemand leuk vindt en waar iedereen punten verliest. Deze formatter neemt de velden die je al kent (auteur, jaar, titel, tijdschrift, uitgever, pagina's…) en produceert een schone verwijzing in APA 7, MLA 9, Chicago auteur-datum of Harvard, klaar om in je bibliografie te plakken. Geen DOI-lookups, geen upload, geen account — alles blijft in de browser.</p>

<h3>Hoe gebruik je het</h3>
<ul>
  <li><strong>Kies de stijl</strong> bovenaan — APA 7 is default. Je kunt op elk moment wisselen; dezelfde velden renderen direct in alle vier de stijlen.</li>
  <li><strong>Kies het brontype</strong> (boek, tijdschriftartikel, website, hoofdstuk, conferentiebijdrage, scriptie/proefschrift, krantenartikel, rapport). De velden passen zich aan.</li>
  <li><strong>Auteurs:</strong> één per regel, <code>Achternaam, Voornaam Middennaam</code>. Een regel zonder komma wordt als organisatie gezien (bijv. <em>Wereldgezondheidsorganisatie</em>).</li>
  <li><strong>Meerdere bronnen</strong> met "+ Add source". "Sort A→Z" alfabetiseert op achternaam eerste auteur.</li>
  <li><strong>Kopieer plain text.</strong> Cursief overleeft een plain-text clipboard niet; plak in Word/Docs en zet titels handmatig cursief als de bestemming geen rich text accepteert.</li>
</ul>

<h3>Stijl-notities</h3>
<ul>
  <li><strong>APA 7</strong> — artikeltitels in sentence case, tijdschriften cursief, "&" voor laatste auteur. Tot 20 auteurs gelijst, daarna "…".</li>
  <li><strong>MLA 9</strong> — title case; eerste auteur <code>Achternaam, Voornaam</code>, daarna <code>Voornaam Achternaam</code>; "et al." vanaf 3 auteurs.</li>
  <li><strong>Chicago</strong> — auteur-datum-variant (bibliografiestijl). Notes-bibliography is een aparte conventie, niet hier gegenereerd.</li>
  <li><strong>Harvard</strong> — Britse auteur-datum. Enkele aanhalingstekens voor artikeltitels; "et al." vanaf 4 auteurs. (Harvard is geen enkele stijl — controleer de gids van je instelling.)</li>
</ul>

<h3>Veelvoorkomende valkuilen</h3>
<ul>
  <li><strong>Volgorde van auteurs telt.</strong> Sorteer auteurs van een paper niet alfabetisch — bewaar de volgorde van de bron.</li>
  <li><strong>Hoofdletters verschillen per stijl.</strong> APA: sentence case; MLA: title case. Automatisch omgezet.</li>
  <li><strong>DOI gaat voor URL</strong> als beide er zijn — DOIs zijn stabiel, URLs rotten.</li>
  <li><strong>Cursief overleeft plain text niet.</strong> De preview toont ze; de kopieerknop geeft platte tekst. Kopieer uit de preview als rich text ondersteund wordt.</li>
  <li><strong>Dit is een formatter, geen factchecker.</strong> Garbage in, garbage out — controleer volumes, pagina's en spelling tegen de bron.</li>
  <li><strong>Privacy.</strong> Niets verlaat de browser. Tab sluiten = alles weg.</li>
</ul>
""",
        "tr": """
<h2>Bu ne işe yarar?</h2>
<p>Kaynakça, akademik yazımın kimsenin yapmak istemediği ve herkesin puan kaybettiği kısmı. Bu biçimleyici, zaten elinde olan alanları (yazar, yıl, başlık, dergi, yayınevi, sayfa…) alır ve kaynakçana yapıştırılmaya hazır APA 7, MLA 9, Chicago yazar-tarih veya Harvard biçiminde temiz bir kayıt üretir. DOI sorgusu yok, yükleme yok, kayıt yok — her şey tarayıcında kalır.</p>

<h3>Nasıl kullanılır</h3>
<ul>
  <li><strong>Üstte stili seç</strong> — APA 7 varsayılan. İstediğin zaman değiştirebilirsin; aynı alanlar dört stilde de anında yeniden biçimlenir.</li>
  <li><strong>Kaynak türünü seç</strong> (kitap, makale, web sitesi, kitap bölümü, konferans bildirisi, tez, haber, rapor). Alanlar buna göre uyarlanır.</li>
  <li><strong>Yazarlar:</strong> her satıra bir tane, <code>Soyad, Ad Orta İsim</code>. Virgülsüz tek satır kurum kabul edilir (örn. <em>Dünya Sağlık Örgütü</em>).</li>
  <li><strong>Birden fazla kaynak</strong> için "+ Add source". "Sort A→Z" ilk yazarın soyadına göre sıralar.</li>
  <li><strong>Düz metin olarak kopyala.</strong> İtalik düz metin panosunda kalmaz; Word/Docs'a yapıştır, hedef rich text desteklemiyorsa başlıkları elle italikleştir.</li>
</ul>

<h3>Stil notları</h3>
<ul>
  <li><strong>APA 7</strong> — makale başlıkları cümle düzeninde, dergiler italik, son yazardan önce "&". 20 yazara kadar listelenir, sonra "…".</li>
  <li><strong>MLA 9</strong> — başlık düzeni; ilk yazar <code>Soyad, Ad</code>, sonrakiler <code>Ad Soyad</code>; 3+ yazarda "et al.".</li>
  <li><strong>Chicago</strong> — yazar-tarih varyantı (bibliyografya). Notes-bibliography ayrı bir konvansiyondur, burada üretilmez.</li>
  <li><strong>Harvard</strong> — İngiliz yazar-tarih. Makale başlıklarında tek tırnak; 4+ yazarda "et al.". (Harvard tek bir stil değildir — kurumunun rehberini kontrol et.)</li>
</ul>

<h3>Sık yapılan hatalar</h3>
<ul>
  <li><strong>Yazar sırası önemli.</strong> Bir makalenin yazarlarını alfabetik sıralama — kaynağın sırasını koru.</li>
  <li><strong>Büyük-küçük harf stile göre değişir.</strong> APA cümle düzeninde, MLA başlık düzeninde. Araç otomatik dönüştürür.</li>
  <li><strong>İkisi de varsa DOI > URL.</strong> DOI'ler stabildir, URL'ler çürür.</li>
  <li><strong>İtalik düz metinde kaybolur.</strong> Önizleme italik gösterir; kopya düz metindir. Rich text destekleniyorsa önizlemeden kopyala.</li>
  <li><strong>Bu bir biçimleyici, gerçek denetleyici değil.</strong> Çöp girer, çöp çıkar — cilt, sayfa ve yazımı kaynaktan doğrula.</li>
  <li><strong>Gizlilik.</strong> Veri tarayıcıdan çıkmaz. Sekmeyi kapat = her şey gider.</li>
</ul>
""",
        "id": """
<h2>Untuk apa ini?</h2>
<p>Daftar pustaka itu bagian dari penulisan akademik yang nggak ada yang suka kerjakan dan semua orang kehilangan poin di situ. Formatter ini ambil field yang sudah kamu punya (penulis, tahun, judul, jurnal, penerbit, halaman…) lalu hasilkan sitasi bersih dalam APA 7, MLA 9, Chicago penulis-tahun, atau Harvard — siap ditempel ke daftar pustaka. Tanpa lookup DOI, tanpa upload, tanpa daftar — semua tetap di browser.</p>

<h3>Cara pakai</h3>
<ul>
  <li><strong>Pilih gaya</strong> di atas — APA 7 default. Bisa ganti kapan saja; field yang sama langsung di-render ke empat gaya.</li>
  <li><strong>Pilih tipe sumber</strong> (buku, artikel jurnal, website, bab buku, makalah konferensi, tesis, artikel berita, laporan). Field menyesuaikan.</li>
  <li><strong>Penulis:</strong> satu per baris, <code>Nama Belakang, Nama Depan Tengah</code>. Baris tanpa koma diperlakukan sebagai organisasi (mis. <em>Organisasi Kesehatan Dunia</em>).</li>
  <li><strong>Banyak sumber</strong> dengan "+ Add source". "Sort A→Z" mengurutkan berdasarkan nama belakang penulis pertama.</li>
  <li><strong>Salin teks polos.</strong> Italic nggak bertahan di clipboard polos; tempel di Word/Docs lalu miringkan judul manual kalau target tidak terima rich text.</li>
</ul>

<h3>Catatan tiap gaya</h3>
<ul>
  <li><strong>APA 7</strong> — judul artikel sentence case, jurnal italic, "&" sebelum penulis terakhir. Sampai 20 penulis dilistkan, lalu "…".</li>
  <li><strong>MLA 9</strong> — title case; penulis pertama <code>Belakang, Depan</code>, berikutnya <code>Depan Belakang</code>; "et al." mulai 3 penulis.</li>
  <li><strong>Chicago</strong> — varian penulis-tahun (daftar pustaka). Notes-bibliography itu konvensi terpisah, tidak dihasilkan di sini.</li>
  <li><strong>Harvard</strong> — gaya Inggris penulis-tahun. Tanda kutip tunggal di judul artikel; "et al." mulai 4 penulis. (Harvard nggak tunggal — cek panduan kampusmu.)</li>
</ul>

<h3>Kesalahan umum</h3>
<ul>
  <li><strong>Urutan penulis penting.</strong> Jangan urutkan penulis paper berdasar alfabet — pertahankan urutan sumber.</li>
  <li><strong>Kapitalisasi beda per gaya.</strong> APA: sentence case; MLA: title case. Otomatis dikonversi.</li>
  <li><strong>DOI lebih baik dari URL</strong> kalau ada keduanya — DOI stabil, URL membusuk.</li>
  <li><strong>Italic hilang di teks polos.</strong> Preview menampilkannya; tombol salin keluar teks polos. Salin dari preview kalau rich text didukung.</li>
  <li><strong>Ini formatter, bukan fact-checker.</strong> Garbage in, garbage out — verifikasi volume, halaman, ejaan terhadap sumber asli.</li>
  <li><strong>Privasi.</strong> Tidak ada data keluar dari browser. Tutup tab = semua hilang.</li>
</ul>
""",
        "vi": """
<h2>Công cụ này để làm gì?</h2>
<p>Danh mục tài liệu tham khảo là phần của bài viết học thuật mà không ai thích làm và ai cũng mất điểm ở đó. Công cụ này lấy các trường bạn đã có sẵn (tác giả, năm, tiêu đề, tạp chí, nhà xuất bản, trang…) rồi tạo trích dẫn sạch sẽ theo APA 7, MLA 9, Chicago tác giả-năm hoặc Harvard, sẵn sàng dán vào thư mục. Không tra DOI, không tải lên, không đăng ký — mọi thứ ở lại trong trình duyệt của bạn.</p>

<h3>Cách dùng</h3>
<ul>
  <li><strong>Chọn phong cách</strong> ở trên — APA 7 là mặc định. Có thể đổi bất cứ lúc nào; cùng các trường sẽ được dựng lại ngay theo bốn phong cách.</li>
  <li><strong>Chọn loại nguồn</strong> (sách, bài báo tạp chí, website, chương sách, kỷ yếu hội thảo, luận văn, bài báo tin tức, báo cáo). Các trường tự thay đổi theo.</li>
  <li><strong>Tác giả:</strong> mỗi dòng một người, <code>Họ, Tên Đệm</code>. Một dòng không có dấu phẩy được coi là tổ chức (vd: <em>Tổ chức Y tế Thế giới</em>).</li>
  <li><strong>Nhiều nguồn</strong> bằng "+ Add source". "Sort A→Z" sắp xếp theo họ tác giả đầu tiên.</li>
  <li><strong>Sao chép bản văn bản thường.</strong> Chữ nghiêng không tồn tại trong clipboard văn bản thường; dán vào Word/Docs rồi đặt nghiêng thủ công nếu nơi đến không nhận rich text.</li>
</ul>

<h3>Ghi chú theo phong cách</h3>
<ul>
  <li><strong>APA 7</strong> — tiêu đề bài báo viết hoa kiểu câu, tên tạp chí in nghiêng, "&" trước tác giả cuối. Liệt kê tối đa 20 tác giả, rồi "…".</li>
  <li><strong>MLA 9</strong> — viết hoa kiểu tiêu đề; tác giả đầu tiên <code>Họ, Tên</code>, các tác giả sau <code>Tên Họ</code>; "et al." khi 3+ tác giả.</li>
  <li><strong>Chicago</strong> — biến thể tác giả-năm (kiểu thư mục). Notes-bibliography là quy ước riêng, không được tạo ở đây.</li>
  <li><strong>Harvard</strong> — phong cách Anh, tác giả-năm. Dấu nháy đơn quanh tiêu đề bài báo; "et al." khi 4+ tác giả. (Harvard không phải một phong cách thống nhất — hãy kiểm tra hướng dẫn của trường bạn.)</li>
</ul>

<h3>Lưu ý thường gặp</h3>
<ul>
  <li><strong>Thứ tự tác giả có ý nghĩa.</strong> Đừng sắp xếp lại theo bảng chữ cái — giữ thứ tự của nguồn.</li>
  <li><strong>Viết hoa khác nhau theo phong cách.</strong> APA: kiểu câu; MLA: kiểu tiêu đề. Công cụ tự chuyển đổi.</li>
  <li><strong>DOI tốt hơn URL</strong> nếu có cả hai — DOI ổn định, URL có thể hỏng theo thời gian.</li>
  <li><strong>Chữ nghiêng mất trong văn bản thường.</strong> Bản xem trước hiển thị; nút copy xuất văn bản thường. Hãy sao chép trực tiếp từ bản xem trước nếu hỗ trợ rich text.</li>
  <li><strong>Đây là công cụ định dạng, không phải kiểm tra sự kiện.</strong> Rác vào, rác ra — hãy đối chiếu tập, trang, chính tả với nguồn gốc.</li>
  <li><strong>Riêng tư.</strong> Dữ liệu không rời khỏi trình duyệt. Đóng tab là sạch hết.</li>
</ul>
""",
        "hi": """
<h2>यह किसके लिए है?</h2>
<p>References academic writing का वह हिस्सा है जिसे कोई करना नहीं चाहता और सब वहाँ marks खोते हैं। यह formatter वे fields लेता है जो आपके पास पहले से हैं (author, year, title, journal, publisher, pages…) और APA 7, MLA 9, Chicago author-date या Harvard में साफ़ citation बना देता है — सीधे bibliography में paste करने के लिए तैयार। कोई DOI lookup नहीं, कोई upload नहीं, कोई signup नहीं — सब आपके browser में रहता है।</p>

<h3>कैसे इस्तेमाल करें</h3>
<ul>
  <li><strong>ऊपर style चुनें</strong> — APA 7 default है। आप कभी भी switch कर सकते हैं; एक ही fields चारों styles में तुरंत render हो जाते हैं।</li>
  <li><strong>Source type चुनें</strong> (book, journal article, website, chapter, conference paper, thesis, news, report)। Fields बदल जाते हैं।</li>
  <li><strong>Authors:</strong> एक प्रति line, <code>Last, First Middle</code> format में। बिना comma वाली एक line organization मानी जाती है (जैसे <em>World Health Organization</em>)।</li>
  <li><strong>कई sources</strong> "+ Add source" से जोड़ें। "Sort A→Z" पहले author के last name से alphabetize करता है।</li>
  <li><strong>Plain text copy करें।</strong> Italics plain clipboard में survive नहीं करतीं; Word/Docs में paste करें और अगर destination rich text support नहीं करता तो titles को manually italic बनाएँ।</li>
</ul>

<h3>Style notes</h3>
<ul>
  <li><strong>APA 7</strong> — article titles sentence case में, journal italic में, अंतिम author से पहले "&"। 20 तक authors list, फिर "…"।</li>
  <li><strong>MLA 9</strong> — title case; पहला author <code>Last, First</code>, बाद वाले <code>First Last</code>; 3+ authors पर "et al."।</li>
  <li><strong>Chicago</strong> — author-date variant (bibliography style)। Notes-bibliography अलग convention है, यहाँ generate नहीं होती।</li>
  <li><strong>Harvard</strong> — British author-date। Article titles में single quotes; 4+ authors पर "et al."। (Harvard एक fixed style नहीं है — अपनी university का guide check करें।)</li>
</ul>

<h3>आम गलतियाँ</h3>
<ul>
  <li><strong>Authors का order मायने रखता है।</strong> Multi-author paper को alphabetize न करें — source का order बनाए रखें।</li>
  <li><strong>Capitalization style पर निर्भर है।</strong> APA: sentence case; MLA: title case। Tool अपने आप convert करता है।</li>
  <li><strong>दोनों उपलब्ध हैं तो DOI > URL।</strong> DOIs stable हैं, URLs समय के साथ टूट जाते हैं।</li>
  <li><strong>Plain text में italics नहीं रहतीं।</strong> Preview दिखाता है; copy button plain देता है। Rich text supported हो तो preview से copy करें।</li>
  <li><strong>यह formatter है, fact checker नहीं।</strong> Garbage in, garbage out — volumes, pages और spelling को source से verify करें।</li>
  <li><strong>Privacy।</strong> कोई data browser से बाहर नहीं जाता। Tab बंद = सब साफ़।</li>
</ul>
""",
        "sk": """
<h2>Načo to slúži?</h2>
<p>Zoznam použitej literatúry je tá časť akademickej práce, ktorú nikto nemá rád a každý na nej stráca body. Tento nástroj vezme polia, ktoré už máš (autor, rok, názov, časopis, vydavateľ, strany…), a vytvorí čistú citáciu v APA 7, MLA 9, Chicago autor-rok alebo Harvard — pripravenú vložiť do bibliografie. Žiadne vyhľadávanie DOI, žiadne nahrávanie, žiadna registrácia — všetko zostáva v prehliadači.</p>

<h3>Ako to použiť</h3>
<ul>
  <li><strong>Vyber štýl</strong> hore — predvolený je APA 7. Môžeš kedykoľvek prepnúť; tie isté polia sa hneď zobrazia vo všetkých štyroch štýloch.</li>
  <li><strong>Vyber typ zdroja</strong> (kniha, časopisecký článok, web, kapitola, konferenčný príspevok, diplomová alebo dizertačná práca, spravodajský článok, správa). Polia sa prispôsobia.</li>
  <li><strong>Autori:</strong> jeden na riadok, <code>Priezvisko, Meno Stredné</code>. Riadok bez čiarky je inštitúcia (napr. <em>Svetová zdravotnícka organizácia</em>).</li>
  <li><strong>Viac zdrojov</strong> cez „+ Add source". „Sort A→Z" abecedne zoradí podľa priezviska prvého autora.</li>
  <li><strong>Kopíruj v plain texte.</strong> Kurzíva v plain-text schránke neprežije; vlož do Wordu/Docs a v cieli s podporou rich textu kurzívu nastav ručne.</li>
</ul>

<h3>Poznámky k štýlom</h3>
<ul>
  <li><strong>APA 7</strong> — názvy článkov vetný typ, časopisy kurzívou, „&" pred posledným autorom. Do 20 autorov všetkých uvedie, potom „…".</li>
  <li><strong>MLA 9</strong> — title case; prvý autor <code>Priezvisko, Meno</code>, ďalší <code>Meno Priezvisko</code>; „et al." od 3 autorov.</li>
  <li><strong>Chicago</strong> — variant autor-rok (bibliografický štýl). Notes-bibliography je iná konvencia, tu sa negeneruje.</li>
  <li><strong>Harvard</strong> — britský autor-rok. Jednoduché úvodzovky pri názvoch článkov; „et al." od 4 autorov. (Harvard nie je jednotný štýl — pozri si príručku svojej univerzity.)</li>
</ul>

<h3>Časté chyby</h3>
<ul>
  <li><strong>Poradie autorov je dôležité.</strong> Neabecedne — zachovaj poradie zo zdroja.</li>
  <li><strong>Veľké/malé písmená podľa štýlu.</strong> APA: vetný typ; MLA: title case. Konverzia je automatická.</li>
  <li><strong>DOI je lepší než URL,</strong> ak máš oboje — DOI sú stabilné, URL hnijú.</li>
  <li><strong>Kurzíva sa stratí v plain texte.</strong> Náhľad ju zobrazuje; tlačidlo kopírovať dáva plain. Z náhľadu kopíruj, ak cieľ podporuje rich text.</li>
  <li><strong>Toto je formátovač, nie overovač faktov.</strong> Garbage in, garbage out — ročníky, strany a pravopis si over voči originálu.</li>
  <li><strong>Súkromie.</strong> Dáta neopúšťajú prehliadač. Zatvor záložku, všetko zmizne.</li>
</ul>
""",
        "cs": """
<h2>K čemu to slouží?</h2>
<p>Seznam použité literatury je ta část akademické práce, kterou nikdo nemá rád a každý na ní ztrácí body. Tento nástroj vezme pole, která už máš (autor, rok, název, časopis, vydavatel, strany…), a vytvoří čistou citaci v APA 7, MLA 9, Chicago autor-rok nebo Harvard — připravenou vložit do bibliografie. Žádné vyhledávání DOI, žádné nahrávání, žádná registrace — vše zůstává v prohlížeči.</p>

<h3>Jak to použít</h3>
<ul>
  <li><strong>Vyber styl</strong> nahoře — výchozí je APA 7. Můžeš kdykoli přepnout; stejná pole se okamžitě zobrazí ve všech čtyřech stylech.</li>
  <li><strong>Vyber typ zdroje</strong> (kniha, časopisecký článek, web, kapitola, konferenční příspěvek, diplomka nebo disertace, novinový článek, zpráva). Pole se přizpůsobí.</li>
  <li><strong>Autoři:</strong> jeden na řádek, <code>Příjmení, Jméno Prostřední</code>. Řádek bez čárky je instituce (např. <em>Světová zdravotnická organizace</em>).</li>
  <li><strong>Více zdrojů</strong> přes „+ Add source". „Sort A→Z" abecedně seřadí podle příjmení prvního autora.</li>
  <li><strong>Kopíruj v plain textu.</strong> Kurzíva v plain-text schránce nepřežije; vlož do Wordu/Docs a v cíli s podporou rich textu kurzívu nastav ručně.</li>
</ul>

<h3>Poznámky ke stylům</h3>
<ul>
  <li><strong>APA 7</strong> — názvy článků větný typ, časopisy kurzivou, „&" před posledním autorem. Do 20 autorů všechny uvede, pak „…".</li>
  <li><strong>MLA 9</strong> — title case; první autor <code>Příjmení, Jméno</code>, další <code>Jméno Příjmení</code>; „et al." od 3 autorů.</li>
  <li><strong>Chicago</strong> — varianta autor-rok (bibliografický styl). Notes-bibliography je jiná konvence, tady se nevytváří.</li>
  <li><strong>Harvard</strong> — britský autor-rok. Jednoduché uvozovky u názvů článků; „et al." od 4 autorů. (Harvard není jednotný styl — koukni do příručky své univerzity.)</li>
</ul>

<h3>Časté chyby</h3>
<ul>
  <li><strong>Pořadí autorů je důležité.</strong> Neřaď abecedně — zachovej pořadí ze zdroje.</li>
  <li><strong>Velká/malá písmena podle stylu.</strong> APA: větný typ; MLA: title case. Konverze je automatická.</li>
  <li><strong>DOI je lepší než URL,</strong> pokud máš obojí — DOI jsou stabilní, URL hnijí.</li>
  <li><strong>Kurziva se ztratí v plain textu.</strong> Náhled ji zobrazuje; tlačítko kopírovat dává plain. Z náhledu kopíruj, pokud cíl podporuje rich text.</li>
  <li><strong>Tohle je formátovač, ne ověřovač faktů.</strong> Garbage in, garbage out — ročníky, stránky a pravopis si ověř proti originálu.</li>
  <li><strong>Soukromí.</strong> Data neopouštějí prohlížeč. Zavři záložku, vše zmizí.</li>
</ul>
""",
    },
    "related": ["rubric-generator", "grade-calculator", "markdown-to-html"],
    "howto": {"flow": "format", "action": "format", "noun": {"en": "citation", "de": "Quellenangabe", "es": "cita", "fr": "citation", "it": "citazione", "pt": "citação", "pl": "cytowanie", "ja": "引用", "nl": "citatie", "tr": "kaynakça", "id": "sitasi", "vi": "trích dẫn", "hi": "citation", "sk": "citácia", "cs": "citace"}},
}
